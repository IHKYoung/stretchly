## TID-20260413-fullscreen-break-current-space-fix · 修复全屏工作区下 break 未覆盖当前界面

- Task-ID: TID-20260413-fullscreen-break-current-space-fix
- Goal: 修复 macOS 下“用户正在其它应用全屏工作时，Pauza 到点后没有覆盖当前全屏界面，只在别的屏幕或 Space 自己开始休息”的回归。
- Requirement Brief: 用户要求修掉一个严重 bug：当前正在浏览器/编辑器等全屏应用中工作时，到点后的 Pauza 没有覆盖当前界面，也没有有效提醒，而是在另一个屏幕自己开始休息。本任务据此只修复 desktop shell 的 fullscreen Space overlay 行为，不改 scheduler、React break 页面或设置 schema。
- Scope: 限定在 `apps/desktop/src-tauri/src/shell.rs` 的 macOS native overlay policy、对应测试以及本任务 docs/Architecture/CHANGELOG/CodeMap；不改 `engine.rs` 调度逻辑和前端页面。
- Source Basis: 已阅读 `docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/Architecture.md`、`docs/ReminderScheduling.md`、`apps/desktop/src-tauri/src/{shell.rs,engine.rs,state.rs}`，以及 `TID-20260408-tauri-window-fullscreen-fix`、`TID-20260409-break-window-macos-fullscreen-coverage`、`TID-20260411-break-activation-settings-input-fix` 的 specs 与相邻 logs，已确认 break 触发入口仍在 `show_break_window()`，回归点更接近 macOS native `collectionBehavior` 变更。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop shell 的 macOS break window overlay 行为、对应测试与 docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `npm --prefix apps/desktop run build` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `shell.rs`、测试与 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地桌面端 bug 修复，无数据或外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260413-fullscreen-break-current-space-fix/README.md
  - Logs: ../logs/2026-04-13.md
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop shell 的 macOS overlay 修复、对应测试与 workflow/docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本轮 root cause 修复、验证与文档闭环。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved
