## TID-20260409-break-surface-i18n-reduction · 收敛休息界面细节、接回完整多语言并审查 legacy app 资产

- Task-ID: TID-20260409-break-surface-i18n-reduction
- Goal: 完成当前 break / settings 收敛性打磨，抽离 i18n 为“语言消息文件 + 语言配置文件 + 自动注册”的长期结构，并审查 legacy `app/` 是否还能继续保留。
- Requirement Brief: 用户要求继续优化 break prompt 和设置页：玻璃感更通透、window 模式最小比例改成 16:9、自定义壁纸完整预览、补结束音效，并清理设置页中英文混杂与术语化表达；随后要把多语言模块完整提取出来，最后审查原始 `/app` 目录与其他冗余文件是否可删。
- Scope: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/**}`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,i18n.rs}`、本任务 specs/evidence、`docs/{UI,Architecture,SettingsInventory,CodeMap,CHANGELOG}.md`，以及 legacy `app/` 依赖审查结论。
- Source Basis: 已阅读 `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,i18n.rs,commands.rs}`、`app/{preferences.html,preferences-renderer.js}`、`app/utils/defaultSettings.js`、`app/locales/{en,zh-CN}.json`、`docs/{RepositoryGuidelines,CodeMap}.md`、`docs/{plans,logs}/2026-04-08.md` 与 `docs/specs/TID-20260408-break-prompt-purity-pass/*`。
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端前台、Tauri host settings schema、语言加载链路与 legacy 目录审查；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、typecheck/build/cargo check/docs validator 与 UI 证据；rollback_plan=回退前台、Rust 与 docs 改动；destructive_operations=本轮先审查 legacy `app/` 删除边界，不直接删除目录；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260409-break-surface-i18n-reduction/README.md
  - Logs: ../logs/2026-04-09.md
- Knowledge Capture: 已沉淀 break 结束音按 break 实例只播一次的前端调度方式、locale “消息文件 + 配置文件 + registry” 结构，以及 legacy `app/` 仍受根入口/测试/文档依赖约束的删除边界。
- Knowledge Route: local（计划沉淀到本任务 specs 与 `docs/{UI,Architecture,CodeMap,SettingsInventory}.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的 break/settings 前台、Tauri host、locale 注册与 legacy `app/` 审查，不扩展到 reminder state machine 或整包 Electron 迁移。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- phase_stop_conditions:
  - 结束音无法在当前宿主链路可靠触发
  - i18n 自动注册无法同时满足前端与 Rust host 一致性
  - 审查发现 legacy `app/` 仍是关键运行时依赖，不能给出整体删除结论

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 已记录复杂任务审批字段，并以本轮用户明确需求作为执行批准

## TID-20260409-break-message-copy-centralization · 统一管理消息页提示语

- Task-ID: TID-20260409-break-message-copy-centralization
- Goal: 让 break 消息页提示语有单独的可编辑入口，并切断桌面端 locale 构建对 legacy `app/` 的依赖，避免继续混用旧设计。
- Requirement Brief: 用户明确要求消息页提示语用单独文件统一管理，并进一步要求新的桌面端不要再混用 legacy locale 来源；本任务将桌面端 locale 真源收口到 `apps/desktop/src/locales/**`，同时把 break 页面专属文案独立到单文件。
- Scope: `apps/desktop/src/{App.tsx,lib/break-prompt.ts}`、`apps/desktop/src/locales/{break-message-copy.ts,messages/**,config/**,overrides/**,registry.generated.json}`、`scripts/sync_desktop_locales.py`、本任务 specs，以及 `docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`
- Source Basis: 已阅读 `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts}`、`apps/desktop/src/locales/{overrides/en.json,overrides/zh-CN.json,config/en.json,config/zh-CN.json,registry.generated.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`scripts/sync_desktop_locales.py`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`、`docs/{plans,logs}/2026-04-09.md` 与 `docs/specs/TID-20260409-break-surface-i18n-reduction/*`。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限桌面端 locale 构建链路、break 页面文案读取与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、`python3 scripts/sync_desktop_locales.py`、`npm --prefix apps/desktop run typecheck`、`npm --prefix apps/desktop run build`；rollback_plan=回退 `apps/desktop/src/locales/**`、`App.tsx`、`break-prompt.ts`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260409-break-message-copy-centralization/README.md
  - Logs: ../logs/2026-04-09.md
- Knowledge Capture: 已沉淀“桌面端 locale 真源必须和 legacy Electron 解耦”以及“break 页面专属文案应独立于通用 locale registry”的维护边界。
- Knowledge Route: local（沉淀到 `docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限桌面端 locale 构建链路、break 消息页文案入口与相关文档，不扩展到 Rust host 或 legacy Electron 清理。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次文案收口与 locale 真源去 legacy 化。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260409-settings-runtime-action-clarity · 整理设置页中的恢复提醒与重置节奏

- Task-ID: TID-20260409-settings-runtime-action-clarity
- Goal: 把设置页里语义冲突的运行时动作拆清楚，让“恢复提醒”只在暂停态出现，“重置节奏”独立成组。
- Requirement Brief: 用户明确指出设置页顶部把“恢复提醒”和“重置节奏”摆在一起会造成混淆；本轮需要把前者收敛为暂停态上下文动作，把后者拆为独立节奏控制，并保持宿主命令语义不变。
- Scope: `apps/desktop/src/App.tsx`、`apps/desktop/src/locales/overrides/{zh-CN,en}.json`、`apps/desktop/src/locales/registry.generated.json`、本任务 specs/evidence、`docs/{UI,SettingsInventory,CHANGELOG}.md`
- Source Basis: 已阅读 `apps/desktop/src/{App.tsx,i18n.ts}`、`apps/desktop/src/locales/overrides/{zh-CN,en}.json`、`apps/desktop/src-tauri/src/{commands.rs,state.rs}`、`docs/{RepositoryGuidelines,CodeMap,ReminderScheduling,SettingsInventory,UI}.md` 以及当日 plans/logs。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限设置页前台、浏览器 preview fallback 与 locale 覆盖层；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build、docs validator 与 UI 证据；rollback_plan=回退 `App.tsx`、locale 与 docs；destructive_operations=替换设置页中运行时动作的层级表达；operator_approval_required=no；rationale=无线上服务、提权、外部副作用或数据迁移。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260409-settings-runtime-action-clarity/README.md
  - Logs: ../logs/2026-04-09.md
  - Evidence: ../specs/TID-20260409-settings-runtime-action-clarity/evidence/README.md
- Knowledge Capture: 已沉淀“运行时动作不等于设置项”的界面边界：pause / focus 只保留上下文状态卡，`reset_breaks` 独立收进节奏控制区，并通过 preview mock 提供可复用的 UI 取证路径。
- Knowledge Route: local（已回写到本任务 specs 与 `docs/{UI,SettingsInventory}.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限设置页前台、文案、preview fallback 与相关 docs，不改宿主状态机或 tray。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 已记录 moderate 任务审批字段，并以本轮用户线程内明确需求作为执行批准

## TID-20260409-apps-single-source-refactor · 收口到 apps 单真源并停用 app 目录

- Task-ID: TID-20260409-apps-single-source-refactor
- Goal: 将默认桌面端运行、构建、测试和 locale 资产彻底收口到 `apps/desktop`，停止 `app/` 目录继续参与当前产品链路。
- Requirement Brief: 用户明确要求做重构而不是继续 patch 旧链路；本任务需要把 `apps/desktop` 变成唯一有效的桌面端真源，`app/` 退为 archived reference，并去掉 locale `overrides` 叠加结构。
- Scope: `package.json`、`apps/desktop/{src/locales/**,legacy-utils/**,README.md}`、`test/**`、`scripts/sync_desktop_locales.py`、`README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,SettingsInventory,UI,CHANGELOG}.md` 与本任务 specs/logs/plans。
- Source Basis: 已阅读 `package.json`、`scripts/sync_desktop_locales.py`、`apps/desktop/src/{App.tsx,i18n.ts,locales/break-message-copy.ts,locales/break-message-copy.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`test/{translations.js,utils.js}`、`apps/desktop/README.md`、`README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,SettingsInventory,UI,CHANGELOG}.md` 与当日相邻任务 docs。
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端源码归属、根脚本、测试引用、locale 生成链路与文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build/test 与 docs validator；rollback_plan=回退 `package.json`、`apps/desktop/**`、`test/**`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权、付费成本或外部副作用，也不直接删除 `app/`。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260409-apps-single-source-refactor/README.md
  - Logs: ../logs/2026-04-09.md
- Knowledge Capture: 已沉淀“桌面端单真源必须包括运行、测试和 locale 生成链路一起迁移”的规则，以及 “break 专属提示语应放在可编辑 JSON，而不是 override 叠加层” 的边界。
- Knowledge Route: local（沉淀到 `README.md`、`apps/desktop/README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,SettingsInventory,UI,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的根 scripts/tests、desktop locale、desktop README 与仓库主文档，不扩展到物理删除 `app/` 或重写 Tauri host 行为。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，按上层运行策略不得调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次重构、验证与文档闭环。
- phase_stop_conditions:
  - 发现仍有默认运行、构建或测试链路直接依赖 `app/**`
  - locale 扁平化后无法通过 sync / typecheck / build / test 验证
  - 若用户要求物理删除 `app/`，则升级为新的高风险清理任务

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 已记录 complex 任务审批字段，并以用户 2026-04-09 明确重构指令作为批准

## TID-20260409-developer-name-unification · 统一替换开发者显示名

- Task-ID: TID-20260409-developer-name-unification
- Goal: (TBD)
- Requirement Brief: (TBD)
- Scope: (TBD)
- Source Basis: (TBD)
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: (TBD)
- Status: INIT
- Links:
  - Specs: ../specs/TID-20260409-developer-name-unification/README.md
  - Logs: ../logs/2026-04-09.md
- Knowledge Capture: (TBD)
- Knowledge Route: none（如无沉淀，请写原因）
- Execution Mode: (TBD)
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: N/A（仅 fallback 时填写）
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: (TBD)
- Warmup Agent IDs: (TBD)
- Warmup Verification: (TBD)
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [ ] 形成 Requirement Brief
- [ ] 明确 Execution Safety Block
- [ ] 产出/落盘 specs（arch/ui/plan/testplan）
- [ ] 明确验收标准（AC）
- [ ] 明确风险与回滚
- [ ] （如需）等待用户 Approved
