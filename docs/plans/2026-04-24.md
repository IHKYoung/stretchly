## TID-20260424-microbreak-credit-due-gate · 修复恢复结算误提前抵扣微休息

- Task-ID: TID-20260424-microbreak-credit-due-gate
- Goal: 修复 smart reminder v2 中 recovery credit 的 due gate，让未到点的微休息/长休息不会在用户从 45s+ idle 返回时被提前结算。
- Requirement Brief: 用户反馈“微休息像是消失了，只剩长休息”；本任务在不改设置和 UI 的前提下，收紧 host 状态机里的 recovery credit 触发条件，并补回归测试。
- Scope: `apps/desktop/src-tauri/src/state.rs`、本任务 specs、`docs/logs/2026-04-24.md`、`docs/CHANGELOG.md`。
- Source Basis: `state.rs`、`engine.rs`、`App.tsx`、`docs/{ReminderScheduling,RepositoryGuidelines,CodeMap}.md`、`~/Library/Application Support/com.ahaknow.pauza/settings.json`、`docs/logs/2026-04-23.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop host reminder 调度与测试；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=无外部副作用的本地 bugfix。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260424-microbreak-credit-due-gate/README.md
  - Logs: ../logs/2026-04-24.md
- Knowledge Capture: yes
- Knowledge Route: local（沉淀“recovery credit 必须以 overdue pending break 为前提”的 reminder 调度规则）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop host 状态机、Rust 测试与 workflow/docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成修复与验证。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260424-smart-reminder-minimal-wait-model · 收缩智能提醒为固定阈值与最长等待

- Task-ID: TID-20260424-smart-reminder-minimal-wait-model
- Goal: 将当前复杂的 smart reminder v2 收回到“固定阈值 + 最长等待”的最小模型，只保留 smart/forced 两种到点策略，不再混入 recovery hold / recovery credit / staged threshold。
- Requirement Brief: 用户确认当前问题不在“是否需要更聪明”，而在 reminder v2 状态机过重；本任务收缩 host 调度，只保留明确空档与最大等待两个判断条件，并同步清理文案/文档。
- Scope: `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/locales/messages/{zh-CN,en}.json`、`apps/desktop/src/locales/registry.generated.json`、`docs/ReminderScheduling.md`、本任务 specs、`docs/logs/2026-04-24.md`、`docs/CHANGELOG.md`。
- Source Basis: `state.rs`、`App.tsx`、`engine.rs`、`docs/{ReminderScheduling,CHANGELOG,RepositoryGuidelines,CodeMap}.md`、`docs/logs/2026-04-23.md`、`docs/specs/TID-20260424-microbreak-credit-due-gate/plan.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop reminder 调度逻辑、locale 与 docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs`、locale 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、外部副作用或权限变更。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260424-smart-reminder-minimal-wait-model/README.md
  - Logs: ../logs/2026-04-24.md
- Knowledge Capture: yes
- Knowledge Route: local（沉淀“smart reminder 用 fixed threshold + max wait 保持最小状态机”的设计边界）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop host 调度、locale、docs 与 Rust 测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成调度收缩与验证。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260424-break-ideas-asset-migration · 拆分 break ideas 资产真源

- Task-ID: TID-20260424-break-ideas-asset-migration
- Goal: 将 break ideas 迁移为独立 JSON 资产与独立 registry，彻底去掉它们对 `messages/*.json` 和代码常量的依赖。
- Requirement Brief: 用户确认 break ideas 的唯一真源应该仍然是 `miniBreakIdeas / longBreakIdeas`，但物理上应从 UI messages 拆出独立管理；同时不希望把 ideas 写成 TS 常量，而是用 registry 数据标记 `official / legacy` 语言。
- Scope: `apps/desktop/src/{i18n.ts,lib/break-ideas.ts}`、`apps/desktop/src/locales/{messages,break-ideas/**,registry.generated.json}`、`scripts/{sync_desktop_locales.py,sync_desktop_break_ideas.py}`、`test/{desktopBreakIdeas,desktopBreakCopySource,translations}.js`、`docs/{RepositoryGuidelines,CodeMap,UI,Architecture,CHANGELOG}.md` 与本任务 specs/logs/plans。
- Source Basis: `apps/desktop/src/{i18n.ts,App.tsx,lib/break-ideas.ts}`、`apps/desktop/src/locales/{messages,config}/`、`scripts/sync_desktop_locales.py`、`test/{desktopBreakIdeas,desktopBreakCopySource,translations}.js`、`docs/{RepositoryGuidelines,CodeMap,UI,Architecture,CHANGELOG}.md`、`docs/specs/TID-20260409-break-ideas-source-random-fix/{README,plan,arch,testplan}.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 desktop locale 资产、前台 helper、sync scripts、测试与 docs；touches_running_service=no；backup_required=no；backup_plan=`python3 scripts/sync_desktop_break_ideas.py`、`python3 scripts/sync_desktop_locales.py`、`npm run typecheck`、`npm test`、`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 break-ideas 目录、`messages/*.json` 的 ideas 拆分、`lib/break-ideas.ts`、`i18n.ts`、sync scripts 与 docs；destructive_operations=从 `messages/*.json` 删除 `miniBreakIdeas / longBreakIdeas`；operator_approval_required=no；rationale=纯本地资产迁移，无外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260424-break-ideas-asset-migration/README.md
  - Logs: ../logs/2026-04-24.md
- Knowledge Capture: yes
- Knowledge Route: local（沉淀 break ideas 独立资产目录、registry official/legacy 标签与 runtime fallback 规则）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop locale 资产、前台 helper、sync scripts、测试与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成资产迁移与验证。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260424-structure-cleanup · 整理 reminder 与 break ideas 的结构残留

- Task-ID: TID-20260424-structure-cleanup
- Goal: 收掉当前 reminder / break ideas 结构里还在漂浮的兼容字段和主流程噪音，让前台模型、host 序列化边界与调度实现重新对齐。
- Requirement Brief: 用户在确认新结构方向后要求继续整理代码；本任务聚焦于把 `idleOpportunitySeconds` 从前台主模型和新序列化结果里移除，同时将 `tick()` 里的 due notification / due break 分支抽成 helper，并同步更新文档。
- Scope: `apps/desktop/src/{App.tsx}`、`apps/desktop/src-tauri/src/state.rs`、`docs/{SettingsInventory,Architecture,CHANGELOG}.md`、本任务 specs/logs/plans。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-ideas.ts}`、`apps/desktop/src-tauri/src/state.rs`、`docs/{SettingsInventory,Architecture,ReminderScheduling}.md`、`docs/specs/TID-20260424-smart-reminder-minimal-wait-model/arch.md`、`docs/specs/TID-20260424-break-ideas-asset-migration/README.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop 前台设置模型、Rust host 调度 helper、文档与测试；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`npm run typecheck`、`npm test`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `App.tsx`、`state.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地结构整理，无外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260424-structure-cleanup/README.md
  - Logs: ../logs/2026-04-24.md
- Knowledge Capture: yes
- Knowledge Route: local（沉淀“兼容字段只读不回写”和“`tick()` 主路径先收口 helper 再考虑拆文件”的边界）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop 前台设置模型、Rust host 调度 helper、docs 与测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成结构整理与验证。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260424-state-module-split · 拆分 state.rs 结构边界

- Task-ID: TID-20260424-state-module-split
- Goal: 将 `state.rs` 中已经能稳定归类的 settings / persistence / tests 逻辑拆出，先把 `PauzaSettings` schema、sanitize、`settings.json` 持久化边界和 Rust tests 拉直，降低 runtime 主文件的职责混杂度。
- Requirement Brief: 用户要求继续整理代码结构，并明确要求把 `state.rs` 里的 tests 单独拆出去；本任务继续保持低风险模块拆分，不改 reminder 行为，也不继续拆 `RuntimeState` / `PauzaState` 主状态机。
- Scope: `apps/desktop/src-tauri/src/state.rs`、新增 `apps/desktop/src-tauri/src/state/{settings,persistence,tests}.rs`、`docs/{CodeMap,Architecture,CHANGELOG}.md` 与本任务 specs/logs/plans。
- Source Basis: `apps/desktop/src-tauri/src/{state.rs,commands.rs,engine.rs,platform.rs,shell.rs}`、`docs/{CodeMap,Architecture,ReminderScheduling}.md`、`docs/specs/TID-20260424-structure-cleanup/plan.md`、`docs/logs/2026-04-24.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop Rust host 的模块边界、文档与测试；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs`、删除 `state/{settings,persistence,tests}.rs` 并恢复 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地结构整理，无外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260424-state-module-split/README.md
  - Logs: ../logs/2026-04-24.md
- Knowledge Capture: yes
- Knowledge Route: local（沉淀“先拆 settings / persistence / tests，再保留 runtime 主文件”的低风险整理顺序）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop Rust host `state` 模块边界、docs 与测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成模块拆分与验证。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved
