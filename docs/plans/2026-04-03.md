## TID-20260403-repo-cleanup-foundation · 仓库精简第一轮：剔除非核心外围资产

- Task-ID: TID-20260403-repo-cleanup-foundation
- Goal: 在不触碰产品主体的前提下，先删除仓库里无争议的外围资产，降低二次开发噪音。
- Requirement Brief: 删除 Docker/Snap 辅助壳、独立调试页和本地生成缓存，只保留产品核心代码与当前仍在使用的构建主路径。
- Scope: `Dockerfile`、`docker-compose.yml`、`publish-snap.md`、`danger-color-tester.html`、本地 `coverage/` 与 `scripts/__pycache__/`，以及对应 docs 索引更新。
- Source Basis: 已阅读 `AGENTS.md`、`package.json`、`Dockerfile`、`docker-compose.yml`、`publish-snap.md`、`danger-color-tester.html`、`app/process-renderer.js`、`docs/CodeMap.md`、`docs/RepositoryGuidelines.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=repo hygiene only；touches_running_service=no；backup_required=no；backup_plan=以 git diff 和 VCS 为边界；rollback_plan=恢复本任务删除的 tracked 文件并按需重新生成 `coverage/`；destructive_operations=删除非核心仓库文件与本地生成目录；operator_approval_required=no；rationale=用户已明确要求删除不必要资产，且本轮不触碰产品主体代码。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-repo-cleanup-foundation/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“基础瘦身”和“平台裁剪”应分两轮执行的边界，避免在技术路线未定前误删分发主资产。
- Knowledge Route: local（docs/specs/plans/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮仓库精简，范围限定为非核心外围资产与 docs 同步。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-settings-sidebar-layout · Tauri 设置页重构为侧边栏分类布局

- Task-ID: TID-20260403-settings-sidebar-layout
- Goal: 让 `apps/desktop` 设置页更像桌面原生偏好页，通过侧边栏类别切换在单页中清晰呈现现有设置。
- Requirement Brief: 将当前 overview hero + grouped settings + advanced accordion 收敛为 sidebar categories + 当前分类详情 + save rail；保留当前已有设置项，不擅自扩项。
- Scope: `apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/specs/TID-20260403-settings-sidebar-layout/*`、`docs/UI.md`、`docs/CHANGELOG.md`、当天 `logs/plans`。
- Source Basis: 已阅读 `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src/locales/{zh-CN,en}.json`、`apps/desktop/src-tauri/src/state.rs`、`app/preferences.html`、`app/utils/defaultSettings.js` 与相邻前台改版 specs。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅重构 Tauri 主设置页的信息架构和文案，不改 host 契约或 settings schema；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + build/UI 自检旁证；rollback_plan=回退 `apps/desktop/src/{App.tsx,locales/*}` 和本任务 docs；destructive_operations=替换现有主设置页布局；operator_approval_required=no；rationale=本轮没有运行中服务、破坏性数据操作或新增依赖风险。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-settings-sidebar-layout/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已明确当前 Tauri 设置页最合适的演进方式不是继续加 hero/accordion，而是采用 sidebar category IA，把已有设置按心智模型重组。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri 前台 settings page、locale 文案、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮设置页信息架构重排。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-tauri-migration-foundation · Tauri 2 迁移基础骨架

- Task-ID: TID-20260403-tauri-migration-foundation
- Goal: 为 Pauza 建立一套独立的 Tauri 2 跨平台桌面基础壳，并停止继续把 Electron 当作未来产品壳层。
- Requirement Brief: 新建 `apps/desktop`，先交付宿主层和新 dashboard，覆盖 tray/shortcut/notification/autostart 与浏览器 preview 兜底；旧 `app/**` 保留为迁移参考。
- Scope: `apps/desktop/**`、根 `package.json` 的 Tauri scripts、相关图标与 docs/specs/plans/logs/architecture/ui/changelog 更新。
- Source Basis: 已阅读 `AGENTS.md`、`package.json`、`app/main.js`、`app/breaksPlanner.js`、`app/utils/naturalBreaksManager.js`、`app/utils/dndManager.js`、`app/utils/appExclusionsManager.js`、`docs/CodeMap.md`、`docs/RepositoryGuidelines.md`、`docs/Architecture.md`、`docs/UI.md`，并审阅 `apps/desktop/**` 新壳实现。
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=新增平行 Tauri 壳层、不替换 Electron；touches_running_service=no；backup_required=no；backup_plan=保留旧壳作为参考并依赖 VCS 回退；rollback_plan=删除 `apps/desktop/**` 与根 desktop 脚本并回退 docs；destructive_operations=删除 Tauri 脚手架残留文件；operator_approval_required=no；rationale=用户已明确确认跨平台并接受迁移到 Tauri 2。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-tauri-migration-foundation/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“新壳先建宿主层骨架、旧 Electron 仅保留为 source basis、浏览器 preview 负责设计校验”的迁移策略。
- Knowledge Route: local（docs/specs/plans/logs/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成 Tauri 基础迁移、文档同步与验证闭环。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-tauri-usable-core · Tauri 可用核心闭环

- Task-ID: TID-20260403-tauri-usable-core
- Goal: 把 Tauri 2 从“迁移基础壳”推进到“能实际承担久坐干预”的最小可用闭环。
- Requirement Brief: 迁入设置持久化、调度状态机、pause/focus、自然休息、DND、app exclusions、tray/shortcut 与 break prompt，同时删掉展示型 dashboard，只保留极简设置页。
- Scope: `apps/desktop/src-tauri/src/{state,platform,engine,commands,shell}.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src-tauri/tauri.conf.json`，以及 docs/specs/evidence/changelog 更新。
- Source Basis: 已阅读 `AGENTS.md`、`app/breaksPlanner.js`、`app/utils/naturalBreaksManager.js`、`app/utils/dndManager.js`、`app/utils/appExclusionsManager.js`、`app/utils/defaultSettings.js`、`apps/desktop/src-tauri/src/*.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`docs/specs/TID-20260403-tauri-migration-foundation/*`。
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=为 Tauri 端新增可运行的久坐干预核心闭环、但不替换 Electron 正式入口；touches_running_service=no；backup_required=no；backup_plan=保留旧 Electron 与第一阶段 foundation 作为 source basis；rollback_plan=回退 `apps/desktop/**` 与 docs；destructive_operations=移除原展示型 dashboard 逻辑；operator_approval_required=no；rationale=用户已明确要求继续推进到“能用为止”。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-tauri-usable-core/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“先迁可用核心闭环，再补统计/生态”的 Tauri 产品推进顺序，以及“设置页/休息提示必须共用同一套极简 UI 骨架”的前台策略。
- Knowledge Route: local（docs/specs/plans/logs/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri 可用核心闭环实现、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-tauri-i18n-resource-layer · Tauri 多语言资源化

- Task-ID: TID-20260403-tauri-i18n-resource-layer
- Goal: 将 Tauri 设置页与宿主层的多语言管理收敛为可维护的共享 i18n 资源。
- Requirement Brief: 参考旧 Electron 的 `i18next + locales/*.json` 组织方式，恢复语言切换并将默认语言改回中文；前后端都不再内联大段文案。
- Scope: `apps/desktop/src/locales/*.json`、`apps/desktop/src/i18n.ts`、`apps/desktop/src-tauri/src/i18n.rs`，以及 `App.tsx` / `state.rs` / `shell.rs` / `commands.rs` 的文案调用点。
- Source Basis: 已阅读 `AGENTS.md`、`app/main.js`、`app/locales/*.json`、`apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/{state,shell,commands}.rs`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅重构 Tauri 端文案管理与默认语言，不调整调度行为；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS 回退；rollback_plan=回退 locale 资源与调用点；destructive_operations=移除临时内联文案方案；operator_approval_required=no；rationale=用户明确要求将多语言改成优雅的 i18n 管理方式。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-tauri-i18n-resource-layer/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“前后端共享 locale 资源 + lookup 层”比在 React/Rust 文件中分散维护文案更适合后续二次开发。
- Knowledge Route: local（docs/specs/plans/logs/ui/codemap）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri i18n 资源化重构。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-tauri-core-parity · Tauri 核心功能补齐并废弃 Electron 入口

- Task-ID: TID-20260403-tauri-core-parity
- Goal: 将 `apps/desktop` 从“可用核心闭环”推进到足以承担根仓库默认入口的 Tauri 核心版本，并将 Electron 降为 legacy 参考入口。
- Requirement Brief: 补齐 Rust host 的核心状态/命令/托盘/快捷键/break window 能力，收敛前台为极简设置页与 break prompt，根目录默认 npm scripts 改走 Tauri；旧 Electron 代码不删，只保留为 source basis 与回退边界。
- Scope: `package.json`、`apps/desktop/src-tauri/src/{state,shell,commands,engine,lib,platform,i18n}.rs`、`apps/desktop/src/{App.tsx,styles.css,locales/*,i18n.ts}` 及对应 docs。
- Source Basis: 已阅读 `package.json`、`app/main.js`、`app/breaksPlanner.js`、`app/utils/{defaultSettings,naturalBreaksManager,dndManager,appExclusionsManager,displayManager}.js`、`apps/desktop/src-tauri/src/{lib,commands,engine,platform,shell,state}.rs`、`apps/desktop/src/{App.tsx,styles.css}`，以及 `TID-20260403-tauri-migration-foundation`、`TID-20260403-tauri-usable-core`、`TID-20260403-tauri-i18n-resource-layer` 三组 specs。
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=切换默认开发/构建入口到 Tauri，并在 Tauri host 补齐核心运行时能力；touches_running_service=no；backup_required=no；backup_plan=保留旧 Electron 源码与 legacy scripts；rollback_plan=恢复根脚本到 Electron 并回退 `apps/desktop/**` 与 docs；destructive_operations=废弃默认 Electron 入口与展示型前台；operator_approval_required=no；rationale=用户已明确要求迁移到 Tauri 并继续迁全功能。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-tauri-core-parity/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“默认入口切换应先做 host parity，再切根脚本；Electron 先退为 legacy 而不是立即删库”的迁移策略。
- Knowledge Route: local（docs/specs/plans/logs/changelog/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当时 developer policy 未获用户显式授权，不得启动子 agent）
- Warmup Verification: BLOCKED: 本任务实施时尚未获得用户对多 agent 的显式授权，因此由单 agent 兜底完成 parity 迁移与验证；当前文档已保留该历史事实，不追溯性改写。
- phase_stop_conditions:
  - 默认入口切换影响超出开发环境
  - Tauri host 无法接住旧 Electron 的核心调度行为
  - 用户要求改变“极简设置页”交互冻结边界

### Incremental Product Notes
- 根据用户最新反馈，Tauri 前台的产品语言继续从“控制台/展示页”向“现代 macOS 小工具偏好页”收敛。
- 默认可见设置边界调整为：`节奏`、`信号`、`通用` 三组即可，高级项继续折叠。
- 视觉和交互词汇调整为：安静的 grouped settings、segmented control、switch、setting-card，以及更简洁的 break prompt。
- 该增量是交互/文档收口，不扩大本任务代码范围。

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-tauri-core-parity-docs · 占位

- Task-ID: TID-20260403-tauri-core-parity-docs
- Goal: 只修改 `docs/**`，将 `TID-20260403-tauri-core-parity` 的文档链补齐并完成当天门禁收口。
- Requirement Brief: 根据当前仓库已完成的 Tauri core parity 代码事实，补全 spec 五件套、evidence report、daily logs/plans 与总览文档；不修改任何生产代码。
- Scope: `docs/specs/TID-20260403-tauri-core-parity/*`、`docs/plans/2026-04-03.md`、`docs/logs/2026-04-03.md`、`docs/CHANGELOG.md`、`docs/CodeMap.md`、`docs/Architecture.md`、`docs/UI.md`，以及本 docs task 的 spec 收口。
- Source Basis: 已阅读 `package.json`、`apps/desktop/src-tauri/src/{state,shell,commands,engine,lib,platform}.rs`、`apps/desktop/src/{App.tsx,styles.css}`、`docs/specs/TID-20260403-tauri-core-parity/*`、相关 migration/usable-core/i18n specs、当天 logs/plans 与总览文档。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=docs only；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS；rollback_plan=回退本次文档修改；destructive_operations=none；operator_approval_required=no；rationale=只做文档与门禁资产更新。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-tauri-core-parity-docs/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“文档收口必须诚实记录证据缺口，而不是把无截图状态写成已完成”的门禁要求。
- Knowledge Route: local（docs/specs/plans/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback
- Warmup Verification: PASS: trivial docs-only task，按规则允许 `single-agent-fallback`，无需唤起额外子 agent；范围限定为本次 docs 收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-hig-tailwind-redesign · Tauri 前台改版：Tailwind + Shadcn + Apple HIG

- Task-ID: TID-20260403-hig-tailwind-redesign
- Goal: 用 Tailwind CSS v4 + shadcn 风格组件重做 Tauri 前台，让设置页和 break prompt 更符合 Apple HIG 的 grouped settings 体验。
- Requirement Brief: 仅重构 `apps/desktop` 的 React/Tailwind 视觉层与组件层，不改 Rust host 契约；主窗口收敛为 overview hero + grouped settings + save rail，break prompt 收敛为环形倒计时单卡片。
- Scope: `apps/desktop/{package.json,components.json,vite.config.ts,tsconfig.json,src/App.tsx,src/styles.css,src/components/ui/**,src/lib/utils.ts,src/locales/*.json}` 及 docs/specs/evidence/UI 索引更新。
- Source Basis: 已阅读 `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/Architecture.md`、`apps/desktop/src/{App.tsx,styles.css,i18n.ts,locales/*}`、`apps/desktop/{package.json,vite.config.ts,tsconfig.json}`、`apps/desktop/src-tauri/tauri.conf.json`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅 Tauri 前台视觉/交互层；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + build/screenshot 旁证；rollback_plan=回退 `apps/desktop/**` 本轮前台改版与 docs；destructive_operations=替换原 React 前台样式与组件结构、不触碰 Rust host；operator_approval_required=no；rationale=用户明确要求采用 Tailwind + Shadcn 重设计 UI 且无运行中服务/数据风险。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-hig-tailwind-redesign/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“Tailwind v4 + shadcn primitives 可以直接作为 Tauri 前台长期基座”，以及“break prompt 证据可通过 mocked `__TAURI_INTERNALS__` 在浏览器 preview 中采集”。
- Knowledge Route: local（docs/specs/plans/logs/ui/codemap）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri 前台 React/CSS、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri 前台重设计、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-macos-tray-menu-crash · 修复 macOS 托盘右键菜单闪退

- Task-ID: TID-20260403-macos-tray-menu-crash
- Goal: 修复 `apps/desktop` 在 macOS 上顶部菜单栏 tray icon 右键后一闪即逝/闪退的问题，同时保持既有 tray 菜单结构和动作语义不变。
- Requirement Brief: 仅调整 Tauri Rust host 的 tray context menu 构造，不改 Electron legacy 路径、React/Tailwind 前台或休息调度契约。
- Scope: `apps/desktop/src-tauri/src/{shell,engine}.rs` 与本任务 specs/logs/plans/changelog/architecture/ui 索引更新。
- Source Basis: 已阅读 `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/{Architecture,UI}.md`、`apps/desktop/src-tauri/src/{shell,state}.rs`、`apps/desktop/src-tauri/tauri.conf.json`，并核对 `muda` / `tauri` 的 menu/tray 源码实现。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅 macOS 本地 tray 右键菜单；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + dev runtime；rollback_plan=回退 `shell.rs` 的 macOS `Submenu` root 分支与 docs；destructive_operations=none；operator_approval_required=no；rationale=本轮仅修复桌面壳崩溃点，无数据或外部副作用。
- Status: REVIEW
- Links:
  - Specs: ../specs/TID-20260403-macos-tray-menu-crash/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已记录“macOS 下 Tauri/muda tray context menu 顶层必须使用 `Submenu`”，以及“后台 tick 秒级 `refresh_tray()` 会让已展开的原生菜单瞬间消失，应改为按内容变化刷新”。
- Knowledge Route: local（specs/logs/plans/Architecture/UI）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成 tray bug 修复、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-settings-fixed-split · 设置页固定 1:3 分栏与 16:9 最小窗口

- Task-ID: TID-20260403-settings-fixed-split
- Goal: 修正当前 Tauri 设置页的全局空间分配，让 sidebar/main 回到稳定的 `1/4 + 3/4` split view，并提高主窗口最小尺寸到接近 `16:9`；同时收紧圆角和冗长介绍文案。
- Requirement Brief: 用户明确反馈现有设置页“三栏 + 小窗口”非常别扭，随后又补充指出“圆角太多、介绍太多”；本轮只改 `App.tsx`、desktop 前台 primitives 与 `tauri.conf.json`，不改 host/schema。
- Scope: `apps/desktop/src/App.tsx`、`apps/desktop/src/components/ui/{button,card,input,select,segmented-control}.tsx`、`apps/desktop/src-tauri/tauri.conf.json`、`docs/specs/TID-20260403-settings-fixed-split/*`、`docs/plans/2026-04-03.md`、`docs/logs/2026-04-03.md`、`docs/UI.md`、`docs/CHANGELOG.md`
- Source Basis: 已阅读 `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/plans/2026-04-03.md`、`docs/logs/2026-04-03.md`、`docs/specs/TID-20260403-settings-sidebar-layout/{plan,ui}.md`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src/components/ui/{card,button,input,select,segmented-control}.tsx`、`apps/desktop/src-tauri/tauri.conf.json`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅主设置页前台布局、desktop primitives 和主窗口尺寸调整；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + build/screenshot 旁证；rollback_plan=回退 `App.tsx`、desktop primitives、`tauri.conf.json` 与本任务 docs；destructive_operations=替换现有三栏设置页、旧最小窗口尺寸和一批高圆角视觉基元；operator_approval_required=no；rationale=纯本地 UI layout/config 变更。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-settings-fixed-split/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已沉淀“桌面偏好页的全局布局问题不要靠局部卡片补救；应优先用主窗口尺寸边界 + 外层 split shell 一次性解决；视觉风格过软时，要从 primitives 下手而不是只改单页”。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: `apps/desktop/src/App.tsx`、desktop primitives、`apps/desktop/src-tauri/tauri.conf.json`、docs/evidence 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮设置页比例修正、圆角/文案收紧、窗口尺寸调整、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-settings-minimal-ui · 设置页极简重做：仅保留核心真设置

- Task-ID: TID-20260403-settings-minimal-ui
- Goal: 将 `apps/desktop` 设置页彻底收成极简 split view，只保留用户确认的核心真设置，并移除 overview / quick actions / 多余卡片结构。
- Requirement Brief: 用户明确确认设置页只需要微休息、长休息、提前提醒、延后、严格模式、智能暂停、打断风格、开机自启动、语言九类设置；本轮因此直接推翻当前前端结构，只保留 4 个高频分类入口和一个主内容面板。
- Scope: `apps/desktop/src/{App.tsx,styles.css,locales/{zh-CN,en}.json,components/ui/card.tsx}`，以及 `docs/specs/TID-20260403-settings-minimal-ui/*`、`docs/UI.md`、`docs/CHANGELOG.md`、当天 `logs/plans`。
- Source Basis: 已阅读 `AGENTS.md`、`docs/UI.md`、`docs/CHANGELOG.md`、`docs/specs/TID-20260403-settings-fixed-split/*`、`apps/desktop/src/{App.tsx,styles.css,i18n.ts,locales/{zh-CN,en}.json}`、`apps/desktop/src/components/ui/{card,badge,button,input,select,segmented-control}.tsx`、`apps/desktop/src-tauri/src/{state.rs,commands.rs}`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅改 Tauri 前端设置页与 locale/docs，不改 Rust host 契约；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、typecheck/build 与截图旁证；rollback_plan=回退前端文件与本任务 docs；destructive_operations=替换现有设置页结构与视觉；operator_approval_required=no；rationale=用户已明确要求前端推翻重做，且本轮无数据/外部副作用风险。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-settings-minimal-ui/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已明确 Pauza 这类工具的设置页不应继续承担“概览台 + 快捷动作 + 高级控制台”的职责，真正合适的结构是“少分类 + 单主面板 + 只露真设置”。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: `apps/desktop` 前端 settings IA、视觉收敛、locale、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮前端极简重做、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-break-surface-mode-restore · 恢复休息窗口/全屏设置

- Task-ID: TID-20260403-break-surface-mode-restore
- Goal: 恢复 Tauri 端休息提示的“窗口 / 全屏”设置，使 break surface mode 再次成为可保存的独立配置轴。
- Requirement Brief: 用户明确指出旧的“窗口 / 全屏”功能丢失；本轮因此补回 Tauri `PauzaSettings.fullscreen`、shell break window profile 以及设置页中的可见入口。
- Scope: `apps/desktop/src-tauri/src/{state.rs,shell.rs}`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`，以及本任务 docs、当天 `logs/plans`、`docs/UI.md`、`docs/Architecture.md`、`docs/CHANGELOG.md`。
- Source Basis: 已阅读 `app/preferences.html`、`app/utils/{defaultSettings,breakWindowProfile}.js`、`app/main.js`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,commands.rs}`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、相邻 specs/logs/UI/Architecture 文档。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅恢复 break surface mode 的设置真源、宿主行为与前端入口；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、cargo/typecheck/build 与 DOM snapshot 旁证；rollback_plan=回退相关 Rust/TS 文件与本任务 docs；destructive_operations=替换当前仅靠 breakPromptStyle 的 surface mode 逻辑；operator_approval_required=no；rationale=用户已明确要求恢复功能，本轮无数据或外部副作用风险。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-break-surface-mode-restore/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已明确 `breakPromptStyle` 和 `window/fullscreen` 是两根不同设置轴；如果只保留前者，用户会直接感知“窗口 / 全屏功能消失”。
- Knowledge Route: local（docs/specs/plans/logs/UI/Architecture/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri settings model、break window profile、前端入口、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 break surface mode 恢复、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260403-break-window-scale-fix · 修复休息窗口过小与双层卡片

- Task-ID: TID-20260403-break-window-scale-fix
- Goal: 修复 break window 过小和双层卡片的问题，让 break 页直接使用宿主窗口空间。
- Requirement Brief: 用户贴图指出当前微休息窗口“太扯淡”；本轮因此同时修正 break 页 React 结构和 Tauri host window-mode 默认尺寸，去掉窗口里再套小卡片的层级。
- Scope: `apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/shell.rs`、本任务 docs、当天 `logs/plans`、`docs/UI.md`、`docs/CHANGELOG.md`。
- Source Basis: 已阅读 `apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/shell.rs`、`docs/specs/TID-20260403-hig-tailwind-redesign/evidence/break-snapshot.md`、`docs/specs/TID-20260403-break-surface-mode-restore/*`、相邻 UI/logs 文档。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅改 break 页 React 结构和 Tauri host window-mode 默认尺寸；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、cargo/typecheck/build 和 preview snapshot；rollback_plan=回退 `App.tsx`、`shell.rs` 与本任务 docs；destructive_operations=替换现有双层容器结构和默认尺寸；operator_approval_required=no；rationale=用户明确要求修正 break window 观感，本轮无数据/外部副作用风险。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260403-break-window-scale-fix/README.md
  - Logs: ../logs/2026-04-03.md
- Knowledge Capture: 已明确 break 页不该在宿主窗口内再套固定宽度 card；break window 一旦已经是独立窗口，前台就应直接使用它的空间。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: break 页前台结构、host 默认尺寸、preview 证据与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 break window 尺度修复、验证与文档收口。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved
