## TID-20260420-release-013-packaging-and-publish · 整理未提交改动并发布 0.1.3 macOS 版本

- Task-ID: TID-20260420-release-013-packaging-and-publish
- Goal: 将当前未提交的 desktop / site / docs 变更整理成 `0.1.3`，统一版本真源、补齐发布文档、打本地 macOS 包，并完成 GitHub release / commit 的发布动作。
- Requirement Brief: 用户要求把现在没有提交的部分做一次文档整理并总结，作为 `0.1.3` 发布；本任务据此只收口当前工作树中的有效产品改动、发布脚本与文档，不把根目录翻译 scratch 文件混入正式版本。
- Scope: `apps/desktop` 的 macOS shell 修复、locale 文案与 registry、根版本真源、`apps/site` release workflow mirror 与 pinned 下载入口、`docs/**` 发布文档、macOS 本地打包、GitHub release 与代码仓库 commit/push。
- Source Basis: `git status --short`、`git diff --stat`、`README.md`、`docs/{CHANGELOG,Architecture,CodeMap,RepositoryGuidelines}.md`、`apps/site/{README.md,index.html,script.js,download/targets.js}`、`apps/site/docs/ReleasePlaybook.md`、`apps/site/scripts/publish_site_release.py`、`apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src/locales/messages/*.json`、`apps/desktop/src/locales/registry.generated.json`、`docs/specs/TID-20260413-fullscreen-break-current-space-fix/*`、`gh release list --repo IHKYoung/Pauza`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅影响本地桌面端打包产物、站点 pinned 下载链接、release 资产与 GitHub 代码推送；touches_running_service=no；backup_required=no；backup_plan=`npm test` + `npm run typecheck` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `npm --prefix apps/desktop run build` + `npm --prefix apps/desktop run tauri build -- --bundles dmg --no-sign` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退版本真源/文档/站点 pinned URL，删除或覆盖错误的 GitHub release 资产，并对错误 push 使用后续 `git revert`；destructive_operations=none；operator_approval_required=no；rationale=用户已明确要求发布 `0.1.3`，本轮不涉及历史重写、生产服务写入或提权操作。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260420-release-013-packaging-and-publish/README.md
  - Logs: ../logs/2026-04-20.md
- Knowledge Capture: 已沉淀两条后续可复用规则：一是 desktop release 前必须先重建 `registry.generated.json`，否则新增 locale 文案不会进入运行时；二是当前 Pauza 的“代码仓库”和“release 分发仓库”并非同一 GitHub repo，发布流程必须显式区分 commit/push 与 release/upload 两条路径。
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在当前工作树收口、版本整理、本地打包、GitHub release 与代码 push 的单 agent 执行
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 `0.1.3` 收口、打包、release 与提交闭环。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] moderate，但高风险外部动作已由用户在 2026-04-20 直接请求
