## TID-20260408-macos-icon-fidelity · 修复 macOS 顶部与 Dock 图标清晰度和尺寸

- Task-ID: TID-20260408-macos-icon-fidelity
- Goal: 修复 macOS 顶部 tray icon 发糊且偏大、Dock icon 相比普通 macOS 应用显得偏大的问题，并让图标资源链路可重复生成。
- Requirement Brief: 用户明确反馈菜单栏图标发糊且偏大、Dock 图标偏大；本轮聚焦于 Tauri macOS 宿主图标接入、tray SVG 小尺寸几何和图标生成脚本，不改业务逻辑、菜单项内容或 break 调度。
- Scope: `apps/desktop/src-tauri/src/shell.rs`、`graphics/tray-icon.svg`、`graphics/generate_icon_assets.py`、重新生成的 `apps/desktop/src-tauri/icons/{icon.png,icon.icns,tray-iconTemplate*.png}`、本任务 specs / plans / logs、`docs/CHANGELOG.md`。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/shell.rs`、`graphics/generate_icon_assets.py`、`graphics/tray-icon.svg`、`graphics/app-icon.svg`、`docs/specs/TID-20260407-app-icon-refresh/*`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`；并核对 `tray-icon-0.21.3/src/platform_impl/macos/mod.rs` 中 `icon_height = 18.0` 的实现，以及 Apple 的 High Resolution Guidelines 对 `@2x` / multi-representation 图像加载方式的说明。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 macOS 图标显示与本地资源生成链；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、重新生成资源和构建链结果为边界；rollback_plan=回退 `shell.rs`、`graphics/*` 与再生的 icon 产物；destructive_operations=替换 tray / dock 图标接入方式并刷新 icon 二进制资源；operator_approval_required=no；rationale=不涉及数据、权限、外部副作用或提权。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-macos-icon-fidelity/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: macOS `tray-icon` crate 会把状态栏图标强行映射到 `18pt`，仅靠喂更小 PNG 不能让图标变小，必须通过更保守的 glyph padding 或直接 patch 原生 `NSStatusItem` 图像；而 Dock icon 在 release `.app` 中应尽量交给系统 bundle icon 处理，运行时 patch 更适合作为未打包开发态的兜底。
- Knowledge Route: local（沉淀到 `graphics/generate_icon_assets.py`、本任务 specs / plans / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 macOS tray / dock 图标修复、资源重生成、验证与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成定位、实现、验证与文档。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 无高风险动作，无需等待额外 Approved

### 追加记录
- 本任务结案后，图标策略已同步进入长期文档：`docs/Architecture.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`，后续修改 macOS tray / Dock icon 应直接以这些文档为准。
- 用户在真实菜单栏截图中反馈 tray icon 视觉体积仍偏小后，本任务追加一轮小幅视觉校正：仅放大 `graphics/tray-icon.svg` glyph 约 `8%`，不改宿主层 patch 逻辑，继续复用既有 1x/2x template 生成链。

## TID-20260408-adaptive-reminder-state-machine · 设计并实现低打断久坐提醒状态机

- Task-ID: TID-20260408-adaptive-reminder-state-machine
- Goal: 把当前提醒逻辑从“到点即打断”改成“到点后先等空档，再择机提醒”的低打断状态机。
- Requirement Brief: 用户明确反馈固定时间提醒会在正在打字/输入/专注时强插；本轮只改 Tauri host 的提醒投递状态机、状态文案和对应测试，不重做设置页或新增系统权限。
- Scope: `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/locales/{zh-CN,en}.json`、本任务 specs/evidence、`docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md`、`docs/CHANGELOG.md`、当日 logs/plans。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/{state,engine,shell}.rs`、`apps/desktop/src/App.tsx`、`app/breaksPlanner.js`、`app/utils/{naturalBreaksManager,defaultSettings}.js`、`docs/{Architecture,UI,SettingsInventory,RepositoryGuidelines,CodeMap}.md` 与当日 logs/plans。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地提醒投递时机与状态文案；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、Rust tests、cargo check 与 docs 为回滚边界；rollback_plan=回退 `state.rs` 与 locale 文案；destructive_operations=替换默认 break delivery 逻辑；operator_approval_required=no；rationale=不涉及数据、权限、外部副作用或新增依赖。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-adaptive-reminder-state-machine/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: 已沉淀“固定倒计时 != 合适提醒时机；需要 due-hold 中间态”这一调度模式到 Architecture / SettingsInventory / 本任务 specs。
- Knowledge Route: local（沉淀到本任务 specs 与长期架构文档）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 Tauri host 提醒状态机、测试与文档闭环。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 无高风险动作，无需等待额外 Approved

### 追加记录
- 已在 `apps/desktop/src-tauri/src/state.rs` 落地低打断提醒状态机：break 到点后若 `idle_ms` 未达机会阈值，则进入等待空档；等待超过阈值时仅 soft nudge 一次；出现短暂停顿后再开始 break。
- 已同步更新 `docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md` 与 `docs/CHANGELOG.md`，说明 hidden adaptive settings 和新的用户可见状态语义。

## TID-20260408-reminder-state-machine-doc · 整理提醒状态机与调度逻辑文档

- Task-ID: TID-20260408-reminder-state-machine-doc
- Goal: 把下一轮久坐提醒的目标状态机与整体调度逻辑整理成一份正式设计文档，作为后续实现真源。
- Requirement Brief: 用户要求先不要继续改代码，而是先把“智能提醒 / 强制提醒 / 严格模式 / 自然休息”的目标模型、状态迁移和调度优先级完整写下来，落到 `docs/` 根目录。
- Scope: `docs/ReminderScheduling.md`、`docs/CodeMap.md`、本任务 specs、当日 logs/plans。
- Source Basis: 已阅读 `docs/{RepositoryGuidelines,CodeMap,Architecture}.md`、`docs/specs/TID-20260408-adaptive-reminder-state-machine/plan.md`、`docs/{plans,logs}/2026-04-08.md`，并检查 `apps/desktop/src-tauri/src/platform.rs` 与 `state.rs` 中当前 idle / adaptive reminder 相关字段与耦合点。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅新增/更新文档，不改运行时代码；touches_running_service=no；backup_required=no；backup_plan=以 Git diff 与 workflow docs validator 为边界；rollback_plan=回退本任务新增/更新的 docs；destructive_operations=none；operator_approval_required=no；rationale=纯文档任务。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-reminder-state-machine-doc/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: 已沉淀“提醒方式、严格模式、自然休息和 UI 风格必须解耦”的目标模型，以及下一轮提醒状态机的目标状态、调度优先级与伪代码。
- Knowledge Route: local（沉淀到 `docs/ReminderScheduling.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次提醒调度设计文档、索引与 workflow 文档同步。
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback
- Warmup Verification: trivial 文档任务且 Required Roles 仅 `scribe`，符合 `single-agent-fallback` 直行条件；无需额外 `spawn_agent`。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

### 追加记录
- 已新增 `docs/ReminderScheduling.md`，用 Mermaid 状态图和 tick 流程图定义目标模型。
- 根据用户复核，文档进一步收敛为三个用户概念：`智能提醒`、`强制提醒`、`自然休息`；其中 `强制提醒` 直接吸收原先 `严格模式` 的语义，不再并列暴露。
- 同步明确：`breakPromptStyle` 剔出调度决策，`soft nudge` 从第一版核心流程移除。

## TID-20260408-reminder-mode-simplification · 收敛提醒模式并实现智能强制自然休息模型

- Task-ID: TID-20260408-reminder-mode-simplification
- Goal: 把提醒产品模型最终收敛成用户可理解且一致的三件事：智能提醒、强制提醒、自然休息，并让 Tauri host、设置页和文档全部对齐这一模型。
- Requirement Brief: 用户确认当前实现要继续做减法，核心只保留“是否仍在操作电脑”“超过久坐时间后如何开始提醒”“离开够久是否视为已休息”；本轮需要把既有 adaptive/strict/style 混合模型统一收敛到 `智能提醒 / 强制提醒 / 自然休息`，并同步更新 host 状态机、设置页、文案、测试与长期文档。
- Scope: `apps/desktop/src-tauri/src/{state,platform}.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/{Architecture,UI,SettingsInventory,CodeMap,CHANGELOG}.md`、本任务 specs/evidence、当日 logs/plans。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/{state,platform,shell}.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/{ReminderScheduling,RepositoryGuidelines,CodeMap,Architecture,SettingsInventory}.md`、`docs/specs/TID-20260408-{adaptive-reminder-state-machine,reminder-state-machine-doc}/plan.md` 与当日 logs/plans。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地提醒投递时机、break 可绕过性与设置页入口；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、Rust tests/check、前端构建与 workflow docs validator 为边界；rollback_plan=回退 `state.rs`、`platform.rs`、`App.tsx`、locale 与 docs 改动；destructive_operations=替换当前 adaptive/strict 设置模型并收紧强制提醒下的动作权限；operator_approval_required=no；rationale=不涉及数据、提权、外部服务或新增依赖。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-reminder-mode-simplification/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: “提醒模式”和“窗口视觉风格”必须拆开；同时只要保留 `强制提醒`，独立 `严格模式` 就会与之重复，应直接并入同一个用户概念。
- Knowledge Route: local（沉淀到 `state.rs` 的 settings/schema、task specs、`docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 reminder mode 收敛、host 状态机改造、前端设置页收敛、测试与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 用户已在对话中明确批准实现

### 追加记录
- Host 设置真源已收敛为全局 `reminder_mode` 与单一 `idle_opportunity_seconds`，移除了 soft nudge 与 per-break strict/idle 设置分叉。
- `platform.rs` 已把 idle 探测从 `natural_breaks` 解耦，保证智能提醒始终能判断“用户是否仍在操作电脑”。
- 设置页只保留 `智能提醒 / 强制提醒 / 自然休息`，`breakPromptStyle / break_prompt_style` 已从主实现与设置 schema 中移除。

## TID-20260408-tauri-window-fullscreen-fix · 修复 Tauri 窗口最小尺寸与无标题栏全屏

- Task-ID: TID-20260408-tauri-window-fullscreen-fix
- Goal: 修复 Tauri 打包后主窗口过小和 break fullscreen 顶部留白的问题，让窗口模式至少受 `800x600` 约束，并让 fullscreen 走无标题栏策略。
- Requirement Brief: 用户明确指出当前 Tauri 版本在窗口模式和 fullscreen 上都存在宿主层问题；本轮只改主窗口尺寸边界与 break fullscreen 进入/退出逻辑，不改 break 页面视觉结构和业务状态机。
- Scope: `apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/src/shell.rs`、本任务 specs / evidence、`docs/{plans,logs}/2026-04-08.md`、`docs/{UI,CHANGELOG}.md`
- Source Basis: `apps/desktop/src-tauri/{tauri.conf.json,src/shell.rs}`、`apps/desktop/src/App.tsx`、`docs/UI.md`、`docs/logs/2026-04-04.md`、`docs/specs/TID-20260404-fullscreen-break-close-fix/*`、Tauri 2 `webview_window.rs` 的 `set_simple_fullscreen()` 文档与 tao macOS fullscreen 说明
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅桌面窗口宿主边界与 fullscreen 进入/退出策略；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、`cargo check`、前端 build 与代码路径审查；rollback_plan=回退 `apps/desktop/src-tauri/{tauri.conf.json,src/shell.rs}` 与本任务 docs；destructive_operations=替换当前 fullscreen 进入/退出策略与主窗口尺寸边界；operator_approval_required=no；rationale=纯本地桌面壳层修复，不涉及数据、提权、外部副作用或新增依赖。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-tauri-window-fullscreen-fix/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: `set_fullscreen()` 在 macOS 上对应 native borderless fullscreen/new space 语义；如果用户想要稳定的“无标题栏铺满”，应优先用 `set_simple_fullscreen()`，而 close path 也必须同步兜底 simple/native fullscreen。
- Knowledge Route: local（沉淀到 `shell.rs` helper、本任务 specs 与 `docs/UI.md` / `docs/CHANGELOG.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次主窗口尺寸边界与 break fullscreen 宿主修复、验证和文档收口。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成定位、实现、验证与文档。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] 无高风险动作，无需等待额外 Approved

### 追加记录
- 主设置窗口边界已改为默认 `960x640`、最小 `800x600`，不再沿用当前错误的 `580x520` / `480x440`。
- `shell.rs` 已新增 `set_break_window_fullscreen()`：macOS 进入 fullscreen 改走 `set_simple_fullscreen(true)`，退出时先 `set_simple_fullscreen(false)` 再 `set_fullscreen(false)`，避免 simple/native fullscreen 混用导致顶部空白或 teardown 漏退。
- 用户补充截图后，已确认当前 window 模式 break 仍错误走旧的 microbreak 右下角小浮窗逻辑，和本轮“至少占屏幕 `80%` 且居中”的预期不一致。
- follow-up 方案已收敛为：`shell.rs` 的非全屏 break profile 统一改为居中大窗，尺寸按当前工作区约 `80% x 80%` 计算，并对极小工作区做边界保护，不再区分 microbreak / break 的右下角角落卡片。
- follow-up 机械验证已完成：`cargo check` PASS、`npm --prefix apps/desktop run build` PASS、`python3 scripts/validate_workflow_docs.py --mode manual` PASS。

## TID-20260408-break-prompt-parity-upgrade · 补齐 Tauri break prompt 的原版体验能力

- Task-ID: TID-20260408-break-prompt-parity-upgrade
- Goal: 让当前 Tauri break prompt 重新具备接近原版的背景氛围与可配置能力，至少补回自定义壁纸、开始音、随机交互语和线性倒计时。
- Requirement Brief: 用户明确指出当前 Tauri break window 相比原版少了“自定义壁纸、声音、随机交互语，以及更像原版的倒数条”，要求沿当前 Tauri break prompt 继续优化补齐，但不回退到 Electron，也不改 reminder/window shell 状态机。
- Scope: `apps/desktop/src/App.tsx`、`apps/desktop/src/lib/break-prompt.ts`、`apps/desktop/src/assets/audio/*.wav`、`apps/desktop/src/locales/*.json`、`apps/desktop/src-tauri/src/state.rs`、本任务 specs 和长期文档；不包含 shell / engine / tray / reminder state machine。
- Source Basis: 旧版 `app/{break.html,microbreak.html,break-renderer.js,microbreak-renderer.js,main.js}`、`app/utils/{defaultBreakIdeas.js,defaultMicrobreakIdeas.js,defaultSettings.js}`，以及当前 Tauri `apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/state.rs`、`docs/{UI,Architecture,SettingsInventory}.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=本地桌面 break prompt、偏好页和 settings schema；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、`typecheck`、前端 build、`cargo check` 和 Playwright 截图；rollback_plan=回退前台 helper、locale、state schema 与 docs；destructive_operations=替换 break prompt 视觉结构并扩展配置模型；operator_approval_required=no；rationale=纯本地体验增强，无运行中服务或外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-break-prompt-parity-upgrade/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: 已把 Tauri break prompt parity 方案、自定义壁纸 data URL 持久化取舍和新增配置边界同步到长期文档。
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 的 developer policy 不允许未显式授权的 sub-agent delegation；因此由 orchestrator 直接完成本任务闭环，并在 logs/specs 中补齐依据、验证和证据。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved

## TID-20260408-break-prompt-purity-pass · 收敛休息界面并清理 break prompt 文案层

- Task-ID: TID-20260408-break-prompt-purity-pass
- Goal: 把当前 break prompt 收敛成纯净休息界面，只保留交互语、数字倒计时和条形进度，并把 break prompt 文案全部收回 locale 资源层。
- Requirement Brief: 用户明确指出当前休息界面“像 Demo”，不接受“微休息”等标签化提示和左右分栏结构；本轮只改 Tauri break prompt 与对应 locale/i18n 层，不动 host 调度和设置 schema。
- Scope: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、本任务 specs、`docs/{plans,logs}/2026-04-08.md`、`docs/{UI,CodeMap,CHANGELOG}.md`
- Source Basis: 已阅读 `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、`app/{break.html,microbreak.html,break-renderer.js,microbreak-renderer.js}`、`docs/{RepositoryGuidelines,CodeMap,UI}.md`、当日 logs/plans，以及 `docs/specs/TID-20260408-break-prompt-parity-upgrade/{plan,ui}.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 Tauri 前台 break prompt 与 locale 层；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、前端 typecheck/build 与 docs validator；rollback_plan=回退 `App.tsx`、`i18n.ts`、`break-prompt.ts`、locale 与 docs；destructive_operations=替换 break prompt 布局并删除 prompt 硬编码；operator_approval_required=no；rationale=无数据、权限或外部副作用。
- Status: DONE
- Links:
  - Specs: ../specs/TID-20260408-break-prompt-purity-pass/README.md
  - Logs: ../logs/2026-04-08.md
- Knowledge Capture: 已明确 break prompt 的“高级感”来自减法而不是更多模块；对这类休息界面，圆环 + 分栏通常只会增强组件感，条形进度更适合作为辅助计时。
- Knowledge Route: local（沉淀到本任务 specs 与 `docs/UI.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 Tauri break prompt、locale/i18n 层与 docs 收敛。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成 break prompt 收敛、验证与文档闭环。
- phase_stop_conditions: N/A（仅 sequential-phases 时填写）

### Plan Checklist
- [x] 形成 Requirement Brief
- [x] 明确 Execution Safety Block
- [x] 产出/落盘 specs（arch/ui/plan/testplan）
- [x] 明确验收标准（AC）
- [x] 明确风险与回滚
- [x] （如需）等待用户 Approved
