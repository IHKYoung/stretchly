## TID-20260409-break-surface-i18n-reduction · 收敛休息界面细节、接回完整多语言并审查 legacy app 资产

- Task-ID: TID-20260409-break-surface-i18n-reduction
- Goal: 修正当前 break/settings 的视觉与交互细节，抽离可扩展的 i18n 结构，并确认 legacy `app/` 的减法边界。
- Requirement Brief: 用户明确要求继续提高 break 卡片的通透感、把 window 模式最小比例收敛到 16:9、让设置页自定义壁纸完整显示、补上结束提示音，并彻底避免设置页中英混杂和术语化文案；随后要求把 i18n 模块按“语言文件 + 语言配置文件”重做，并审查原始 `/app` 目录及其他不需要文件是否还能删。
- Assumptions: 纯净 break 布局维持不变；结束音优先复用前端音频播放链路；旧 `app/locales/*.json` 与 `app/preferences.html` 仍是语言资产与语言清单的重要依据。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,i18n.rs,commands.rs}`、`app/{preferences.html,preferences-renderer.js}`、`app/utils/defaultSettings.js`、`app/locales/{en,zh-CN}.json`、`docs/{RepositoryGuidelines,CodeMap}.md`、`docs/{plans,logs}/2026-04-08.md`、`docs/specs/TID-20260408-break-prompt-purity-pass/*`
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端前台、Tauri host settings schema、语言加载链路与 legacy 目录审查；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、typecheck/build/cargo check/docs validator 与 UI 证据；rollback_plan=回退前台、Rust 与 docs 改动；destructive_operations=本轮先审查 legacy `app/` 删除边界，不直接删除目录；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Change Summary: 已完成 break card 玻璃质感增强、window 模式 16:9 尺寸约束、设置页自定义壁纸 `object-contain` 预览、微休息/休息开始音与结束音设置及结束音播放；同时新增 `scripts/sync_desktop_locales.py`，把桌面端 i18n 改成“每语言消息文件 + 每语言配置文件 + 自动生成 registry”的结构，并确认 legacy `app/` 由于根入口、legacy scripts、测试与文档依赖仍不可整体删除。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run build` PASS（仅保留大体积 chunk 警告）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；UI 证据已落盘 `docs/specs/TID-20260409-break-surface-i18n-reduction/evidence/{break-glass-preview.png,settings-surface-preview.png}`。
- Knowledge Capture: 已沉淀 break 结束音只播一次的实现边界、language registry 自动注册方案，以及 legacy `app/` 仍受根入口/测试/文档约束的保留结论。
- Knowledge Route: local（沉淀到本任务 specs 与长期 docs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的 break/settings 前台、Tauri host、locale 注册与 legacy `app/` 审查，不扩展到 reminder state machine 或整包 Electron 迁移。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- Risk & Rollback: 主要风险在于结束音时序与 i18n 自动注册前后端一致性；若任一方案不稳，优先局部回退新增链路，保留纯净 break 布局与 settings 中文文案修正。
- Retention Review: 本轮新增的 locale registry、结束音字段与界面收敛均属于长期产品能力，应保留；未执行 `app/` 删除，仅形成删减审计结论，避免误删用户当前仍依赖的 legacy 资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-message-copy-centralization · 统一管理消息页提示语

- Task-ID: TID-20260409-break-message-copy-centralization
- Goal: 让 break 消息页提示语有单独可编辑的入口，并切断桌面端 locale 构建对 legacy `app/` 的依赖。
- Requirement Brief: 用户要求消息页提示语不要再散落或写死，最好由单独文件统一管理；随后又明确要求新的桌面端不要继续混用旧设计，因此本任务同时把 desktop locale 真源收口到 `apps/desktop/src/locales/**`，并把 break 页面专属文案抽到独立文件。
- Assumptions: 共享 `registry.generated.json` 结构保持不变；当前 break 页面专属文案仅需覆盖 `zh-CN` / `en`；legacy `app/locales` 仍保留给旧壳自身使用。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts}`、`apps/desktop/src/locales/{overrides/en.json,overrides/zh-CN.json,config/en.json,config/zh-CN.json,registry.generated.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`scripts/sync_desktop_locales.py`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`、`docs/{plans,logs}/2026-04-09.md`、`docs/specs/TID-20260409-break-surface-i18n-reduction/*`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限桌面端 locale 构建链路、break 页面文案读取与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、`python3 scripts/sync_desktop_locales.py`、`npm --prefix apps/desktop run typecheck`、`npm --prefix apps/desktop run build`；rollback_plan=回退 `apps/desktop/src/locales/**`、`App.tsx`、`break-prompt.ts`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Change Summary: 新增 `apps/desktop/src/locales/break-message-copy.ts` 作为 break 消息页文案唯一入口，并改造 `App.tsx` / `break-prompt.ts` 只从该文件读取主提示语、默认提示语、等待完成文案与 CTA 标签；同时改写 `scripts/sync_desktop_locales.py`，让桌面端 locale registry 只依赖 `apps/desktop/src/locales/{messages,config,overrides}`，不再从 `app/locales` 或 `app/preferences.html` 回灌，并同步更新相关 docs。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（生成 54 种语言的 desktop locale registry）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（仅保留大体积 chunk 警告）；`python3 scripts/validate_workflow_docs.py --mode manual` FAIL（当前仍被无关任务 `TID-20260409-settings-runtime-action-clarity` 的既有占位字段阻塞，本任务相关文档已补齐）。
- Knowledge Capture: 已沉淀“desktop locale 真源必须与 legacy Electron 解耦”的维护规则，以及“break 页面专属文案应独立于通用 locale registry”的收口方式。
- Knowledge Route: local（沉淀到 docs 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限桌面端 locale 构建链路、break 消息页文案入口与相关文档，不扩展到 Rust host 或 legacy Electron 清理。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次文案收口与 locale 真源去 legacy 化。
- Risk & Rollback: 主要风险在于 break 页面仍残留旧 key 读取或 locale 目录存在孤儿文件；若出现问题，优先回退 `break-message-copy.ts` 与新的 locale generator，再重新生成 registry 并验证前端 build。
- Retention Review: `break-message-copy.ts` 与新的 desktop locale 真源边界都属于长期结构，应保留；旧 `ui.break.*` override 文案已失去维护价值，不应继续作为 break 页面入口。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-settings-runtime-action-clarity · 整理设置页中的恢复提醒与重置节奏

- Task-ID: TID-20260409-settings-runtime-action-clarity
- Goal: 收紧设置页运行时动作的语义，让“恢复提醒”只服务于暂停恢复，而“重置节奏”成为独立的节奏控制。
- Requirement Brief: 用户指出顶部的“恢复提醒”和“重置节奏”放在一起容易误解为同类动作；本轮需要整理设置页，把恢复动作收敛成 pause-only 上下文入口，并把重置动作独立出来。
- Assumptions: `resume_breaks` 只代表解除 pause；`clear_focus_session` 需要独立命名；`reset_breaks` 不会自动解除暂停；用户当前希望继续优化设置页，而不是改 reminder 状态机。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts}`、`apps/desktop/src/locales/overrides/{zh-CN,en}.json`、`apps/desktop/src-tauri/src/{commands.rs,state.rs}`、`docs/{RepositoryGuidelines,CodeMap,ReminderScheduling,SettingsInventory,UI}.md`、当日 plans/logs
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限设置页前台、浏览器 preview fallback 与 locale 覆盖层；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build、docs validator 与 UI 证据；rollback_plan=回退 `App.tsx`、locale 与 docs；destructive_operations=替换设置页中运行时动作的层级表达；operator_approval_required=no；rationale=无线上服务、提权、外部副作用或数据迁移。
- Change Summary: 已在 `apps/desktop/src/App.tsx` 中加入顶部上下文状态卡：pause 态只显示 `恢复提醒`，focus 态改为 `结束专注`；同时把 `重置节奏` 独立收进 `节奏` 页顶部的 `节奏控制` 区，并补齐中英文说明文案与浏览器 preview mock 路径。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS；`python3 scripts/validate_agent_configs.py` SKIP（no multi-agent config）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（保留既有 bundle size warning，不阻断构建）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；Playwright CLI 证据见 `docs/specs/TID-20260409-settings-runtime-action-clarity/evidence/README.md`，默认态 / paused / focus 三张截图均已落盘，console 为 0 error / 0 warning。
- Knowledge Capture: 已沉淀“运行时动作不等于设置项”的设置页组织方式：pause / focus 通过顶部上下文状态卡承载，`reset_breaks` 作为独立节奏控制呈现，并通过 preview query 参数支持可复用的 UI 取证。
- Knowledge Route: local（已回写到本任务 specs 与长期 UI/Settings 文档）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限设置页前台、文案、preview fallback 与相关 docs，不改宿主状态机或 tray。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- Risk & Rollback: 已通过默认态 / paused / focus 三种界面证据确认动作边界清晰；若后续需要回退，优先恢复 `App.tsx` 动作区与 locale 文案，不触碰宿主状态机。
- Retention Review: 本次改动同时澄清了产品边界与浏览器 preview 取证路径，属于值得长期保留的设置页组织规则；无临时脚本、调试开关或一次性绕过逻辑需要清理。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-apps-single-source-refactor · 收口到 apps 单真源并停用 app 目录

- Task-ID: TID-20260409-apps-single-source-refactor
- Goal: 将默认桌面端运行、构建、测试和 locale 资产彻底收口到 `apps/desktop`，停止 `app/` 目录继续参与当前产品链路。
- Requirement Brief: 用户明确要求做真正的重构，而不是继续 patch 旧 Electron 目录；本任务因此把根 scripts/tests、desktop locale 和 break 文案入口统一收口到 `apps/desktop`，并把 `app/` 退为 archived reference。
- Assumptions: `apps/desktop` 已具备当前默认运行能力；`app/` 可先退化为归档参考而不是立刻物理删除；`break-message-copy.json` 足以承接 break 页面专属提示语。
- Source Basis: `package.json`、`scripts/sync_desktop_locales.py`、`apps/desktop/src/{App.tsx,i18n.ts,locales/break-message-copy.ts,locales/break-message-copy.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`test/{translations.js,utils.js}`、`apps/desktop/README.md`、`README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,SettingsInventory,UI,CHANGELOG}.md`、当日 plans/logs 与 `docs/specs/TID-20260409-break-message-copy-centralization/*`
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端源码归属、根脚本、测试引用、locale 生成链路与文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build/test 与 docs validator；rollback_plan=回退 `package.json`、`apps/desktop/**`、`test/**`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权、付费成本或外部副作用，也不直接删除 `app/`。
- Change Summary: 已把根 `package.json` 的默认入口和构建脚本完全收口到 `apps/desktop`；根级 Vitest 测试改为从 `apps/desktop/legacy-utils/**` 与 `apps/desktop/src/locales/messages/**` 读取；desktop locale registry 改为只依赖 `messages + config`，`overrides/` 目录被移除；break 消息页文案改为 `break-message-copy.json` 可编辑真源；同时同步重写 root README、desktop README 和核心架构/索引文档，使 `app/` 明确退为 archived reference。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（54 languages）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（仅保留既有 chunk size warning）；`npm test` PASS（18 files / 319 tests）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；附加审计：`rg -n "from '../app/|from \"../app/|app/locales|app/main\\.js|overrides/" --glob '!docs/**' --glob '!app/**' --glob '!apps/desktop/node_modules/**' --glob '!apps/desktop/dist/**' --glob '!build/**' .` 返回 0 条结果。
- Knowledge Capture: 已沉淀“单真源重构不能只改运行入口，必须同时迁移测试、locale generator 和维护文档”的规则，以及 “break 页面专属提示语应落在独立 JSON 真源，而不是 override 叠加层” 的长期结构。
- Knowledge Route: local（已回写到 README、desktop README、核心 docs 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的根 scripts/tests、desktop locale、desktop README 与仓库主文档，不扩展到物理删除 `app/` 或重写 Tauri host 行为。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，按上层运行策略不得调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次重构、验证与文档闭环。
- Risk & Rollback: 主要风险在于根级测试路径迁移与 locale 扁平化后出现漏改；若出现问题，优先回退 `package.json`、`apps/desktop/{legacy-utils,src/locales}`、`test/**` 与相关 docs，再重新跑 locale sync / typecheck / build / test 确认恢复。
- Retention Review: 本轮收口的根 scripts、desktop locale 扁平结构、`break-message-copy.json` 与文档口径都是长期应保留的架构资产；`app/` 目录暂不删除，避免在当前脏工作区下产生误删风险。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-developer-name-unification · 统一替换开发者显示名

- Task-ID: TID-20260409-developer-name-unification
- Goal: (TBD)
- Requirement Brief: (TBD)
- Assumptions: (TBD)
- Source Basis: (TBD)
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: (TBD)
- Change Summary: (TBD)
- Verification: (TBD)
- Knowledge Capture: (TBD)
- Knowledge Route: none（如无沉淀，请写原因）
- Execution Mode: (TBD)
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: N/A（仅 fallback 时填写）
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: (TBD)
- Warmup Agent IDs: (TBD)
- Warmup Verification: (TBD)
- Risk & Rollback: (TBD)
- Retention Review: (TBD)
- Retention Decision: (TBD)

### Execution Checklist
- [ ] 初始化/预热完成并记录回执
- [ ] 实现/改动完成
- [ ] 测试与验证通过（含证据）
- [ ] 更新 CHANGELOG（如含代码/行为变更）
- [ ] 结案汇报
