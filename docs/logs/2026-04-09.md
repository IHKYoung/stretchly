## TID-20260409-break-surface-i18n-reduction · 收敛休息界面细节、接回完整多语言并审查 legacy app 资产

- Task-ID: TID-20260409-break-surface-i18n-reduction
- Goal: 修正当前 break/settings 的视觉与交互细节，抽离可扩展的 i18n 结构，并确认 legacy `app/` 的减法边界。
- Requirement Brief: 用户明确要求继续提高 break 卡片的通透感、把 window 模式最小比例收敛到 16:9、让设置页自定义壁纸完整显示、补上结束提示音，并彻底避免设置页中英混杂和术语化文案；随后要求把 i18n 模块按“语言文件 + 语言配置文件”重做，并审查原始 `/app` 目录及其他不需要文件是否还能删。
- Assumptions: 纯净 break 布局维持不变；结束音优先复用前端音频播放链路；旧 `app/locales/*.json` 与 `app/preferences.html` 仍是语言资产与语言清单的重要依据。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,i18n.rs,commands.rs}`、`app/{preferences.html,preferences-renderer.js}`、`app/utils/defaultSettings.js`、`app/locales/{en,zh-CN}.json`、`docs/{RepositoryGuidelines,CodeMap}.md`、`docs/{plans,logs}/2026-04-08.md`、`docs/specs/TID-20260408-break-prompt-purity-pass/*`
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端前台、Tauri host settings schema、语言加载链路与 legacy 目录审查；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、typecheck/build/cargo check/docs validator 与 UI 证据；rollback_plan=回退前台、Rust 与 docs 改动；destructive_operations=本轮先审查 legacy `app/` 删除边界，不直接删除目录；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Change Summary: 已完成 break card 玻璃质感增强、window 模式 16:9 尺寸约束、设置页自定义壁纸 `object-contain` 预览、微休息/休息开始音与结束音设置及结束音播放；同时新增 `scripts/sync_desktop_locales.py`，把桌面端 i18n 改成“每语言消息文件 + 每语言配置文件 + 自动生成 registry”的结构，并确认 legacy `app/` 由于根入口、legacy scripts、测试与文档依赖仍不可整体删除。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run build` PASS（仅保留大体积 chunk 警告）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；UI 证据已落盘 `docs/specs/TID-20260409-break-surface-i18n-reduction/evidence/{break-glass-preview.png,settings-surface-preview.png}`。
- Knowledge Capture: 已沉淀 break 结束音只播一次的实现边界、language registry 自动注册方案，以及 legacy `app/` 仍受根入口/测试/文档约束的保留结论。
- Knowledge Route: local（沉淀到本任务 specs 与长期 docs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的 break/settings 前台、Tauri host、locale 注册与 legacy `app/` 审查，不扩展到 reminder state machine 或整包 Electron 迁移。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- Risk & Rollback: 主要风险在于结束音时序与 i18n 自动注册前后端一致性；若任一方案不稳，优先局部回退新增链路，保留纯净 break 布局与 settings 中文文案修正。
- Retention Review: 本轮新增的 locale registry、结束音字段与界面收敛均属于长期产品能力，应保留；未执行 `app/` 删除，仅形成删减审计结论，避免误删用户当前仍依赖的 legacy 资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-message-copy-centralization · 统一管理消息页提示语

- Task-ID: TID-20260409-break-message-copy-centralization
- Goal: 让 break 消息页提示语有单独可编辑的入口，并切断桌面端 locale 构建对 legacy `app/` 的依赖。
- Requirement Brief: 用户要求消息页提示语不要再散落或写死，最好由单独文件统一管理；随后又明确要求新的桌面端不要继续混用旧设计，因此本任务同时把 desktop locale 真源收口到 `apps/desktop/src/locales/**`，并把 break 页面专属文案抽到独立文件。
- Assumptions: 共享 `registry.generated.json` 结构保持不变；当前 break 页面专属文案仅需覆盖 `zh-CN` / `en`；legacy `app/locales` 仍保留给旧壳自身使用。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts}`、`apps/desktop/src/locales/{overrides/en.json,overrides/zh-CN.json,config/en.json,config/zh-CN.json,registry.generated.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`scripts/sync_desktop_locales.py`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`、`docs/{plans,logs}/2026-04-09.md`、`docs/specs/TID-20260409-break-surface-i18n-reduction/*`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限桌面端 locale 构建链路、break 页面文案读取与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、`python3 scripts/sync_desktop_locales.py`、`npm --prefix apps/desktop run typecheck`、`npm --prefix apps/desktop run build`；rollback_plan=回退 `apps/desktop/src/locales/**`、`App.tsx`、`break-prompt.ts`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权或外部副作用。
- Change Summary: 新增 `apps/desktop/src/locales/break-message-copy.ts` 作为 break 消息页文案唯一入口，并改造 `App.tsx` / `break-prompt.ts` 只从该文件读取主提示语、默认提示语、等待完成文案与 CTA 标签；同时改写 `scripts/sync_desktop_locales.py`，让桌面端 locale registry 只依赖 `apps/desktop/src/locales/{messages,config,overrides}`，不再从 `app/locales` 或 `app/preferences.html` 回灌，并同步更新相关 docs。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（生成 54 种语言的 desktop locale registry）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（仅保留大体积 chunk 警告）；`python3 scripts/validate_workflow_docs.py --mode manual` FAIL（当前仍被无关任务 `TID-20260409-settings-runtime-action-clarity` 的既有占位字段阻塞，本任务相关文档已补齐）。
- Knowledge Capture: 已沉淀“desktop locale 真源必须与 legacy Electron 解耦”的维护规则，以及“break 页面专属文案应独立于通用 locale registry”的收口方式。
- Knowledge Route: local（沉淀到 docs 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限桌面端 locale 构建链路、break 消息页文案入口与相关文档，不扩展到 Rust host 或 legacy Electron 清理。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次文案收口与 locale 真源去 legacy 化。
- Risk & Rollback: 主要风险在于 break 页面仍残留旧 key 读取或 locale 目录存在孤儿文件；若出现问题，优先回退 `break-message-copy.ts` 与新的 locale generator，再重新生成 registry 并验证前端 build。
- Retention Review: `break-message-copy.ts` 与新的 desktop locale 真源边界都属于长期结构，应保留；旧 `ui.break.*` override 文案已失去维护价值，不应继续作为 break 页面入口。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-settings-runtime-action-clarity · 整理设置页中的恢复提醒与重置节奏

- Task-ID: TID-20260409-settings-runtime-action-clarity
- Goal: 收紧设置页运行时动作的语义，让“恢复提醒”只服务于暂停恢复，而“重置节奏”成为独立的节奏控制。
- Requirement Brief: 用户指出顶部的“恢复提醒”和“重置节奏”放在一起容易误解为同类动作；本轮需要整理设置页，把恢复动作收敛成 pause-only 上下文入口，并把重置动作独立出来。
- Assumptions: `resume_breaks` 只代表解除 pause；`clear_focus_session` 需要独立命名；`reset_breaks` 不会自动解除暂停；用户当前希望继续优化设置页，而不是改 reminder 状态机。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts}`、`apps/desktop/src/locales/overrides/{zh-CN,en}.json`、`apps/desktop/src-tauri/src/{commands.rs,state.rs}`、`docs/{RepositoryGuidelines,CodeMap,ReminderScheduling,SettingsInventory,UI}.md`、当日 plans/logs
- Level: trivial
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限设置页前台、浏览器 preview fallback 与 locale 覆盖层；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build、docs validator 与 UI 证据；rollback_plan=回退 `App.tsx`、locale 与 docs；destructive_operations=替换设置页中运行时动作的层级表达；operator_approval_required=no；rationale=无线上服务、提权、外部副作用或数据迁移。
- Change Summary: 已在 `apps/desktop/src/App.tsx` 中加入顶部上下文状态卡：pause 态只显示 `恢复提醒`，focus 态改为 `结束专注`；同时把 `重置节奏` 独立收进 `节奏` 页顶部的 `节奏控制` 区，并补齐中英文说明文案与浏览器 preview mock 路径。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS；`python3 scripts/validate_agent_configs.py` SKIP（no multi-agent config）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（保留既有 bundle size warning，不阻断构建）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；Playwright CLI 证据见 `docs/specs/TID-20260409-settings-runtime-action-clarity/evidence/README.md`，默认态 / paused / focus 三张截图均已落盘，console 为 0 error / 0 warning。
- Knowledge Capture: 已沉淀“运行时动作不等于设置项”的设置页组织方式：pause / focus 通过顶部上下文状态卡承载，`reset_breaks` 作为独立节奏控制呈现，并通过 preview query 参数支持可复用的 UI 取证。
- Knowledge Route: local（已回写到本任务 specs 与长期 UI/Settings 文档）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限设置页前台、文案、preview fallback 与相关 docs，不改宿主状态机或 tray。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档闭环。
- Risk & Rollback: 已通过默认态 / paused / focus 三种界面证据确认动作边界清晰；若后续需要回退，优先恢复 `App.tsx` 动作区与 locale 文案，不触碰宿主状态机。
- Retention Review: 本次改动同时澄清了产品边界与浏览器 preview 取证路径，属于值得长期保留的设置页组织规则；无临时脚本、调试开关或一次性绕过逻辑需要清理。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-apps-single-source-refactor · 收口到 apps 单真源并停用 app 目录

- Task-ID: TID-20260409-apps-single-source-refactor
- Goal: 将默认桌面端运行、构建、测试和 locale 资产彻底收口到 `apps/desktop`，停止 `app/` 目录继续参与当前产品链路。
- Requirement Brief: 用户明确要求做真正的重构，而不是继续 patch 旧 Electron 目录；本任务因此把根 scripts/tests、desktop locale 和 break 文案入口统一收口到 `apps/desktop`，并把 `app/` 退为 archived reference。
- Assumptions: `apps/desktop` 已具备当前默认运行能力；`app/` 可先退化为归档参考而不是立刻物理删除；`break-message-copy.json` 足以承接 break 页面专属提示语。
- Source Basis: `package.json`、`scripts/sync_desktop_locales.py`、`apps/desktop/src/{App.tsx,i18n.ts,locales/break-message-copy.ts,locales/break-message-copy.json}`、`apps/desktop/src-tauri/src/i18n.rs`、`test/{translations.js,utils.js}`、`apps/desktop/README.md`、`README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,SettingsInventory,UI,CHANGELOG}.md`、当日 plans/logs 与 `docs/specs/TID-20260409-break-message-copy-centralization/*`
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=仅限本地桌面端源码归属、根脚本、测试引用、locale 生成链路与文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build/test 与 docs validator；rollback_plan=回退 `package.json`、`apps/desktop/**`、`test/**`、`scripts/sync_desktop_locales.py` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权、付费成本或外部副作用，也不直接删除 `app/`。
- Change Summary: 已把根 `package.json` 的默认入口和构建脚本完全收口到 `apps/desktop`；根级 Vitest 测试改为从 `apps/desktop/legacy-utils/**` 与 `apps/desktop/src/locales/messages/**` 读取；desktop locale registry 改为只依赖 `messages + config`，`overrides/` 目录被移除；break 消息页文案改为 `break-message-copy.json` 可编辑真源；同时同步重写 root README、desktop README 和核心架构/索引文档，使 `app/` 明确退为 archived reference。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（54 languages）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（仅保留既有 chunk size warning）；`npm test` PASS（18 files / 319 tests）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；附加审计：`rg -n "from '../app/|from \"../app/|app/locales|app/main\\.js|overrides/" --glob '!docs/**' --glob '!app/**' --glob '!apps/desktop/node_modules/**' --glob '!apps/desktop/dist/**' --glob '!build/**' .` 返回 0 条结果。
- Knowledge Capture: 已沉淀“单真源重构不能只改运行入口，必须同时迁移测试、locale generator 和维护文档”的规则，以及 “break 页面专属提示语应落在独立 JSON 真源，而不是 override 叠加层” 的长期结构。
- Knowledge Route: local（已回写到 README、desktop README、核心 docs 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本任务涉及的根 scripts/tests、desktop locale、desktop README 与仓库主文档，不扩展到物理删除 `app/` 或重写 Tauri host 行为。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，按上层运行策略不得调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次重构、验证与文档闭环。
- Risk & Rollback: 主要风险在于根级测试路径迁移与 locale 扁平化后出现漏改；若出现问题，优先回退 `package.json`、`apps/desktop/{legacy-utils,src/locales}`、`test/**` 与相关 docs，再重新跑 locale sync / typecheck / build / test 确认恢复。
- Retention Review: 本轮收口的根 scripts、desktop locale 扁平结构、`break-message-copy.json` 与文档口径都是长期应保留的架构资产；`app/` 目录暂不删除，避免在当前脏工作区下产生误删风险。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-developer-name-unification · 统一替换开发者显示名

- Task-ID: TID-20260409-developer-name-unification
- Goal: 将当前产品与仓库元数据中的开发者显示名统一从 `Jan Hovancik` 替换为 `Clarke Young`。
- Requirement Brief: 用户明确要求统一替换当前开发者显示名；本任务只覆盖 locale 显示值、package/metainfo/README 等元数据，不改 `LICENSE` 版权归属，也不改 URL/邮箱。
- Assumptions: 用户提供的新姓名 `Clarke Young` 是本轮唯一需要替换的身份信息；旧 URL/邮箱并非本轮目标。
- Source Basis: `package.json`、`README.md`、`net.hovancik.Pauza.metainfo.xml`、`app/preferences.html`、`apps/desktop/src/locales/messages/{en,zh-CN,tr}.json`、`docs/CHANGELOG.md`、当日 plans/logs
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限开发者显示文案、应用元数据、desktop/archived locale 与相关 docs；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、翻译测试与 docs validator；rollback_plan=回退 locale JSON、`package.json`、`README.md`、metainfo 与重新生成的 registry；destructive_operations=none；operator_approval_required=no；rationale=无运行时行为改动、无外部副作用、无历史重写。
- Change Summary: 已将 desktop locale 与 archived locale 中 `preferences.about.clarkeY` 的显示值统一替换为 `Clarke Young`，并修正 Turkish locale 的 `developedBy` 顺序；同时更新 `package.json` 的 `author` 与 `publisherDisplayName`、`README.md` Contributors 里的当前开发者显示名，以及 `net.hovancik.Pauza.metainfo.xml` 的 `developer_name`；`LICENSE` 未改动。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（54 languages）；`npm test` PASS（18 files / 319 tests）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；附加审计：`rg -n "Jan Hovancik" package.json README.md net.hovancik.Pauza.metainfo.xml apps/desktop/src/locales/messages app/locales apps/desktop/src/locales/registry.generated.json` 仅剩 `LICENSE` 命中未改。
- Knowledge Capture: 已沉淀“开发者显示名替换应只改产品/元数据出口，不应顺手改动 `LICENSE` 历史版权归属”的边界，以及 Turkish locale 需要单独处理 about 文案顺序的注意点。
- Knowledge Route: local（沉淀到 `docs/CHANGELOG.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 locale、package/metainfo/README 与相关 docs，不扩展到链接、邮箱或版权归属调整。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次显示名替换、验证与文档闭环。
- Risk & Rollback: 主要风险是 locale source 已更新但 registry 未重生成，或者 Turkish locale 替换后语序错位；若出现问题，优先回退 locale JSON、package/metainfo/README 与 registry 产物。
- Retention Review: 当前开发者显示名应与产品实际维护者保持一致，因此本轮 locale、package 和 metainfo 的变更都应保留；`LICENSE` 未动，避免引入不必要的法律归属变更。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-apps-locale-single-source-refactor · 收口 apps 文案到单一 messages 真源

- Task-ID: TID-20260409-apps-locale-single-source-refactor
- Goal: 停止 `break-message-copy.*` 过渡链路，让当前桌面端所有可见文案统一收口到 `apps/desktop/src/locales/messages/*.json`，并让 `config/*.json` 回到只承载语言元信息的职责。
- Requirement Brief: 用户明确要求这一步按“重构”执行，而不是继续 patch；当前桌面端 break 消息页不应再有独立 JSON/TS helper 文案入口，所有 copy 都应统一进入 `messages`，`app/` 也不该重新参与桌面端文案链路。
- Assumptions: 当前 `desktopReady` 语言仍为 `zh-CN` / `en`；其它语言可继续通过 `i18n.ts` fallback 获得 break prompt 文案；本轮不需要新增更多语言的人工翻译。
- Source Basis: `apps/desktop/src/{App.tsx,lib/break-prompt.ts,i18n.ts,locales/break-message-copy.ts,locales/break-message-copy.json}`、`apps/desktop/src/locales/{messages/{en,zh-CN}.json,config/{en,zh-CN}.json}`、`test/translations.js`、`apps/desktop/README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`、`docs/{plans,logs}/2026-04-09.md`、同日 `apps-single-source-refactor` / `break-message-copy-centralization` specs
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限桌面端 locale 真源、break prompt 文案读取路径、locale registry、回归测试与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、typecheck/build/test 与 docs validator；rollback_plan=回退前台源码、message JSON、删除的 `break-message-copy.*`、测试与 docs；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、提权、付费成本或外部副作用。
- Change Summary: 已将 break prompt 专属文案并入 `apps/desktop/src/locales/messages/{en,zh-CN}.json` 的 `ui.breakCopy.*`，并改造 `App.tsx` / `break-prompt.ts` 直接通过 `i18n.ts` 的 `t()` / `tList()` 读取；`apps/desktop/src/locales/break-message-copy.ts` 与 `break-message-copy.json` 已删除，新增 `test/desktopBreakCopySource.js` 守住单真源边界，并同步更新 desktop README、RepositoryGuidelines、Architecture、UI、CodeMap 与 CHANGELOG。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（仅保留既有 chunk size warning）；`npm test` PASS（19 files / 317 tests）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；附加审计：`rg -n "getBreakMessageCopy|getBreakPrompts|break-message-copy" apps/desktop/src --glob '!apps/desktop/dist/**'` 返回 0 条运行时命中。
- Knowledge Capture: 已沉淀“break prompt 专属文案也必须留在 `messages` 单真源内，不能为了方便编辑再开并行 JSON/helper”以及“`config/*.json` 只保留语言元信息”的架构边界。
- Knowledge Route: local（沉淀到 `apps/desktop/README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限桌面端文案真源、break prompt 读取链路、locale registry、测试与相关 docs，不扩展到 `app/` 物理删除或更多语言翻译补齐。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，按上层运行策略不得调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次重构、验证与文档闭环。
- Risk & Rollback: 主要风险在于删除过渡文件后出现残留 import、`ui.breakCopy.*` key 漏配或 docs 口径没同步；若出现问题，优先回退前台源码、message JSON、测试与 docs，必要时再恢复 `break-message-copy.*` 临时兜底。
- Retention Review: 本轮收口的 `ui.breakCopy.*`、新增回归测试与更新后的文档口径都属于长期应保留的架构资产；独立 `break-message-copy.*` 过渡文件已完成使命，应移除而不是继续保留。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-janh-key-rename · 统一开发者 about key 为 clarkeY

- Task-ID: TID-20260409-janh-key-rename
- Goal: 把开发者姓名相关 locale key 从 `preferences.about.janH` 统一重命名为 `preferences.about.clarkeY`，并同步 legacy HTML 引用、desktop registry 与同日文档。
- Requirement Brief: 用户明确要求“将所有的 key, janH 都替换为 clarkeY”；本任务覆盖 desktop locale、archived locale、generated registry、legacy `app/preferences.html` 与同日相关 docs，确保后续 grep 不再出现旧 key。
- Assumptions: 当前不需要保留 `janH` 兼容 alias；旧 Electron 目录虽然已归档，但其 locale 和 HTML 引用仍要保持自洽。
- Source Basis: desktop / archived locale JSON、`apps/desktop/src/locales/registry.generated.json`、`app/preferences.html`、同日 `developer-name-unification` specs，以及今日 plans/logs 中相关任务记录。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 locale key 名、desktop registry、legacy HTML 引用与相关 docs；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、locale sync、tests、docs validator 与 grep 审计；rollback_plan=回退 locale JSON、`app/preferences.html`、registry 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权、外部副作用或数据迁移。
- Change Summary: 已将 desktop locale、archived locale 与 generated desktop registry 中的开发者姓名 key 统一从 `janH` 重命名为 `clarkeY`，legacy `app/preferences.html` 的 `data-i18next` 也同步改为 `preferences.about.clarkeY`；同时更新了同日 `developer-name-unification` 相关 spec 和本任务 docs，避免继续记录错误的当前 key 名。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm test` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；附加审计：`rg -n "janH" . --glob '!apps/desktop/node_modules/**' --glob '!apps/desktop/dist/**' --glob '!.git/**'` 返回 0 条结果。
- Knowledge Capture: 已沉淀“显示值替换和 key rename 是两件事，后一件必须把 locale source、generated registry、legacy HTML 与 docs 一起改干净”的维护规则。
- Knowledge Route: local（沉淀到同日 specs / plans / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 locale key、legacy HTML、generated registry 与相关 docs，不扩展到产品元数据或 `LICENSE`。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 key rename、验证与文档闭环。
- Risk & Rollback: 主要风险在于 locale source、registry、legacy HTML 与 docs 之间出现局部漏改；若出现问题，优先回退相关 JSON、HTML、registry 与 docs，然后重新跑 sync / tests / validator。
- Retention Review: `clarkeY` 作为新的唯一 locale key 应长期保留；旧 `janH` key 不再有兼容价值，应彻底退出仓库。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-apps-reduction-cleanup · 清理 apps 未使用迁移层并保留 app 参考目录

- Task-ID: TID-20260409-apps-reduction-cleanup
- Goal: 继续对仓库做减法，移除不再参与当前桌面端开发链路的迁移层、旧测试、旧样例目录、本地生成目录、README 展示素材以及根级社区/发布元数据文件；保留 `apps/desktop` 作为唯一有效实现，保留 `app/` 作为原版参考目录，并把未来可能还要用的信息沉淀到 `docs/`。
- Requirement Brief: 用户明确要求“用不到的都可以移除”，并进一步点名 `CODE_OF_CONDUCT.md`、`CONTRIBUTING.md`、`LICENSE`、`net.hovancik.Pauza.desktop`、`net.hovancik.Pauza.metainfo.xml`、`README.md`；本任务仅清理非主体开发资产，不删除 `app/` 原版参考源码，也不改当前 Tauri 主体逻辑。
- Assumptions: `legacy-utils` 不再被当前运行时使用；`coverage/`、`output/` 为可再生本地目录；根目录旧 PNG 只承担 README 展示或可再生副产物用途，不是主体开发依赖。
- Source Basis: `package.json`、`vitest.config.ts`、`apps/desktop/src/**`、`apps/desktop/src-tauri/**`、`README.md`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`LICENSE`、`net.hovancik.Pauza.desktop`、`net.hovancik.Pauza.metainfo.xml`、`graphics/generate_icon_assets.py`、`docs/{RepositoryGuidelines,CodeMap,Architecture,CHANGELOG}.md` 与同日 cleanup / locale tasks。
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限仓库结构、根 README、测试入口与开发索引文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、`rg` 审计、typecheck/build/test 与 workflow docs gate；rollback_plan=从 VCS 恢复被删除目录、素材、README 与 docs，然后重跑验证命令；destructive_operations=删除非运行时目录、旧测试和 tracked 素材；operator_approval_required=no；rationale=用户已明确授权清理，且不涉及线上服务、提权或外部副作用。
- Change Summary: 已删除 `apps/desktop/legacy-utils/**` 与依赖它的根级旧测试；同时清理 `coverage/`、`examples/`、`output/`、根目录旧 README 展示素材，并将根级 `README.md`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`LICENSE`、`net.hovancik.Pauza.desktop`、`net.hovancik.Pauza.metainfo.xml` 的有用信息提炼到 `docs/RootMetadataArchive.md` 后移除；`docs/{RepositoryGuidelines,CodeMap,CHANGELOG,RootMetadataArchive}.md` 与本任务 specs/plans 已同步到当前目录结构。
- Verification: `test ! -e coverage && test ! -e examples && test ! -e output && test -f docs/RootMetadataArchive.md && test ! -e README.md && test ! -e CONTRIBUTING.md && test ! -e CODE_OF_CONDUCT.md && test ! -e LICENSE && test ! -e net.hovancik.Pauza.desktop && test ! -e net.hovancik.Pauza.metainfo.xml` PASS（`ROOT_CLEANUP_OK`）；`rg -n "legacy-utils" package.json apps/desktop test --glob '!apps/desktop/node_modules/**'` PASS（0 命中）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（`✓ 1823 modules transformed.`、`✓ built in 1.61s`，保留 chunk size warning）；`npm test` PASS（`2 passed files / 52 passed tests`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“当前 repo 应只保留主体实现、原版参考和必要打包真源；迁移兼容层、旧测试、生成目录、README 展示素材和根级社区/发布元数据都不应长期混在主目录里，如确有参考价值则转入 `docs/`”的清理边界。
- Knowledge Route: local（沉淀到 `docs/RootMetadataArchive.md`、RepositoryGuidelines、CodeMap、CHANGELOG 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限删除非运行时目录/素材、根级社区/发布元数据文件，更新当前文档口径与执行当前默认验证，不扩展到删除 `app/` 或重写 Tauri 业务逻辑。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，按上层运行策略不得调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次仓库减法、验证与文档闭环。
- Risk & Rollback: 主要风险在于误删当前仍被 README、脚本或 docs 引用的 tracked 文件，或清掉 legacy-utils 后残留旧测试/脚本引用；若出现问题，优先从 VCS 恢复目录、素材与 README/docs，并重跑默认验证链路。
- Retention Review: 本轮删除的迁移兼容层、旧测试、README 展示素材、本地生成目录以及根级社区/发布元数据文件都不值得继续放在仓库根目录；保留下来的应该是 `docs/RootMetadataArchive.md` 与更新后的开发索引文档，因为它们能在不干扰主体开发的前提下保留未来可能还会用到的信息。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-version-unify-010 · 统一当前有效版本为 0.1.0

- Task-ID: TID-20260409-version-unify-010
- Goal: 将当前仍然生效的产品/仓库版本入口统一为 `0.1.0`，避免根 package 继续停留在旧的 `1.20.0`。
- Requirement Brief: 用户明确要求把“目前的版本统一一下为 `0.1.0`”；本任务只覆盖当前有效版本真源，不改历史 release 记录、archive 文档或依赖包自身版本号。
- Assumptions: `apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/Cargo.toml` 已是正确的 `0.1.0`；只有根 `package.json` / `package-lock.json` 仍残留 `1.20.0`。
- Source Basis: `package.json`、`package-lock.json`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/Cargo.toml` 与当日 plans/logs。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限当前仓库/打包元数据版本号；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、版本 grep、`npm test`、build 与 docs validator；rollback_plan=回退 `package.json`、`package-lock.json` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=不涉及运行时逻辑、数据迁移、权限或外部副作用。
- Change Summary: 已将根 `package.json` 与 `package-lock.json` 顶层版本从 `1.20.0` 统一改为 `0.1.0`；`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/Cargo.toml` 原本已是 `0.1.0`，本轮保持不变，并同步补齐本任务 specs / plans / logs。
- Verification: `node -p "require('./package.json').version" && node -p "require('./package-lock.json').version" && node -p "require('./package-lock.json').packages[''].version" && node -p "require('./apps/desktop/package.json').version" && node -e "const fs=require('fs');const conf=JSON.parse(fs.readFileSync('./apps/desktop/src-tauri/tauri.conf.json','utf8'));console.log(conf.version)"` 输出均为 `0.1.0`；`rg -n '^version = "0\\.1\\.0"$' apps/desktop/src-tauri/Cargo.toml` 命中第 `3` 行；`rg -n '1\\.20\\.0' package.json package-lock.json apps/desktop/package.json apps/desktop/src-tauri/tauri.conf.json apps/desktop/src-tauri/Cargo.toml` 退出码 `1`（无命中）；`npm test` PASS（2 files / 52 tests）；`npm --prefix apps/desktop run build` PASS（Vite build 成功，仅保留 chunk size warning）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“当前有效版本真源应以根 package/lock + desktop package + tauri conf + cargo manifest 为准，历史 release 文本不应被混入版本统一任务”这一边界。
- Knowledge Route: local（沉淀到本任务 specs / plans / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限版本元数据文件与相关 docs，不扩展到历史 release 文本或 archive 目录。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次版本统一、验证与文档闭环。
- Risk & Rollback: 主要风险在于根 `package.json` 与 `package-lock.json` 同步不完整；若出现问题，优先回退这两个文件与本任务 docs，再重跑 grep / build / test / validator。
- Retention Review: 统一后的 `0.1.0` 版本字段应长期保留，因为它与当前 desktop / tauri / cargo 真源一致；版本统一任务文档也应保留，便于后续继续沿同一版本基线推进。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-root-readme-run-build-guide · 在根 README 汇总运行与打包命令

- Task-ID: TID-20260409-root-readme-run-build-guide
- Goal: 在根 `README.md` 汇总当前可用的运行、测试、打包命令，并明确 macOS 产物路径。
- Requirement Brief: 用户要求把当前有效的运行/打包命令总结到根 README；本任务只重建精简 README 与同步索引文档，不修改任何脚本实现或 Tauri bundle 配置。
- Assumptions: 根 README 当前缺失；当前真实命令真源以 `package.json`、`apps/desktop/package.json` 和 `apps/desktop/src-tauri/tauri.conf.json` 为准。
- Source Basis: `package.json`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/logs/2026-04-08.md`。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限仓库文档入口；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、README grep 与 workflow docs validator 为边界；rollback_plan=回退 README 与相关 docs 索引；destructive_operations=none；operator_approval_required=no；rationale=纯文档任务，不影响运行时逻辑、数据或外部系统。
- Change Summary: 新建根 `README.md`，统一汇总 `npm start` / `npm run dev` / `npm run desktop:dev`、测试命令、icon/locale 辅助命令、`npm run desktop:build` / `pack` / `dist`、macOS 当前架构打包、universal 打包与产物路径；同时同步 `docs/RepositoryGuidelines.md` 和 `docs/CodeMap.md`，明确根 README 现在是当前有效命令入口。
- Verification: `test -f README.md` PASS；`rg -n 'npm start|npm run dev|npm run desktop:build|npm run pack|universal-apple-darwin|target/release/bundle' README.md` PASS；`rg -n 'README.md' docs/RepositoryGuidelines.md docs/CodeMap.md` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“根 README 只保留当前有效命令入口，命令描述必须以实际 scripts 与 Tauri 配置为准”这一维护规则。
- Knowledge Route: local（沉淀到 README、索引文档与本任务 specs/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 README 与文档索引更新，不扩展到构建脚本或历史 metadata 恢复。
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（trivial 文档任务，按 `scribe` 单角色 fallback 执行）
- Warmup Verification: trivial 文档任务，`Required Roles = scribe`，符合仓库规则允许的 `single-agent-fallback`；本轮由单 agent 完成 README 与索引同步。
- Risk & Rollback: 主要风险是 README 命令与真实脚本不一致，或索引文档继续保留“根 README 已清理”的旧口径；若发现问题，直接回退本次新增/修改的文档文件即可。
- Retention Review: 新 README 值得长期保留，因为它承担当前仓库最常用的运行/打包入口；本次同步到索引文档的维护规则也应保留，避免后续再出现命令口径漂移。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [ ] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-language-selector-multilang-fix · 修复设置页多语言选择

- Task-ID: TID-20260409-language-selector-multilang-fix
- Goal: 修复设置页多语言选择，让完整 locale 列表可选且选择后真正生效。
- Requirement Brief: 用户指出设置页当前不能选择多语言；本任务只修复语言列表和前后端语言归一化，不修改 locale 内容。
- Assumptions: locale config/messages 已完整存在；问题根因在 `desktopReady` 过滤和前后端 normalize 回退。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/i18n.ts`、`apps/desktop/src-tauri/src/i18n.rs`、`apps/desktop/src/locales/config/*.json`。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=设置页语言选择与前后端语言归一化；touches_running_service=no；backup_required=no；backup_plan=以 Vitest、TypeScript、Rust tests 与 build 为边界；rollback_plan=回退 `App.tsx`、前后端 `i18n` 与测试；destructive_operations=none；operator_approval_required=no；rationale=本地设置行为修复，不涉及外部副作用。
- Change Summary: 前端 `DESKTOP_LANGUAGE_CONFIGS` 不再按 `desktopReady` 过滤；前后端 `normalizeLanguage()/normalize_language()` 不再把现有 locale code 强制回退到 `zh-CN/en`；设置页 `语言` 控件改为下拉 `Select`，可承载完整语言列表；并新增 `test/desktopSettingsControls.js` 覆盖语言列表与归一化行为。
- Verification: `npm test` PASS（3 files / 55 tests，包含 `test/desktopSettingsControls.js`）；`npm --prefix apps/desktop run typecheck` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（12 passed）；`npm --prefix apps/desktop run build` PASS。
- Knowledge Capture: 已沉淀“desktop locale 列表和语言归一化不能再被 `desktopReady` 过滤绑死，完整语言列表必须通过下拉选择承载”这一规则。
- Knowledge Route: local（沉淀到 `App.tsx`、前后端 `i18n`、测试与本任务 specs/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限设置页语言选择、前后端语言归一化与对应测试，不扩展到 locale 内容翻译。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成多语言选择修复、验证与文档闭环。
- Risk & Rollback: 主要风险是前后端语言归一化不一致；若出现回退异常，直接回退 `App.tsx`、`i18n.ts`、`i18n.rs` 与新增测试。
- Retention Review: 此次修复值得长期保留，因为它把“已有 locale 资源却不能选/选了不生效”的结构性问题彻底拆掉了。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-schedule-input-redesign · 重构微休息与休息的预设和手动输入

- Task-ID: TID-20260409-break-schedule-input-redesign
- Goal: 重构微休息与休息的 preset 和手动输入，让 4 组参数都保留 5 个候选项，并可先清空再自定义输入。
- Requirement Brief: 用户指出候选项分布不合理、手动输入不能先清空再录入；本任务只修复节奏页 preset 与数字输入交互，不改后端 schema。
- Assumptions: 当前节奏页只有 4 组 preset 型输入；用户需要同时保留快捷 preset 和自定义数值能力。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/components/ui/segmented-control.tsx`、当日用户反馈。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=设置页节奏输入交互；touches_running_service=no；backup_required=no；backup_plan=以 Vitest、TypeScript 与 build 为边界；rollback_plan=回退 `App.tsx`、settings helper 与测试；destructive_operations=none；operator_approval_required=no；rationale=纯前端表单交互修复。
- Change Summary: 新增 `apps/desktop/src/lib/settings-controls.ts`，统一收口 4 组节奏 preset 和数字草稿提交 helper；将四组 preset 调整为更均衡的 5 个候选项：微休息间隔 `[10,20,30,45,60]`、微休息时长 `[20,30,45,60,90]`、长休息每几轮 `[2,3,4,5,6]`、长休息时长 `[5,10,15,20,30]`；输入框改为草稿式交互，允许临时清空，并在 blur/Enter 时提交 clamp 后的值。
- Verification: `npm test` PASS（3 files / 55 tests，覆盖 preset 常量与 `commitDraftNumber()`）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS。
- Knowledge Capture: 已沉淀“preset 负责常用值，输入框允许临时空态并在 blur/Enter 时提交 clamp 值”这一设置交互规则。
- Knowledge Route: local（沉淀到 settings helper、测试与本任务 specs/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限节奏页 preset 和数字输入交互，不扩展到后端 schema。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成节奏输入交互修复、验证与文档闭环。
- Risk & Rollback: 主要风险是输入草稿和父级受控值不同步；若出现回填异常，直接回退 `App.tsx`、`settings-controls.ts` 与测试。
- Retention Review: 此次 helper 化值得长期保留，因为 preset 分布和数字提交逻辑以后还会继续迭代，拆成独立 helper 后更容易回归验证。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-settings-next-cycle-apply-fix · 修复设置更新只在下一轮休息生效

- Task-ID: TID-20260409-settings-next-cycle-apply-fix
- Goal: 修复设置更新的生效边界，让当前 break 和已排队 next break 不再因保存设置被打断或重置。
- Requirement Brief: 用户指出调整微休息/休息设置后，不应该打断当前休息状态机或重置节奏；本任务只修复 Rust 侧 `update_settings()` 的 schedule 重排策略。
- Assumptions: bug 根因在 `update_settings()` 每次保存都无条件 `reset_schedule(now)`；active break flow 应保持，future schedule 应在下一轮使用新 settings。
- Source Basis: `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src-tauri/src/engine.rs`、`apps/desktop/src-tauri/src/commands.rs`、`docs/Architecture.md`。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=本地 break 调度状态机；touches_running_service=no；backup_required=no；backup_plan=以 Rust tests、前端 tests/build 为边界；rollback_plan=回退 `state.rs` 与新增测试；destructive_operations=none；operator_approval_required=no；rationale=调度逻辑修复，不涉及外部系统。
- Change Summary: `update_settings()` 现在会先判断是否存在 `current_break`、`next_break_due_ms` 或 `next_break_wait_started_ms`；只有在没有 active break flow 且当前未处于阻塞态时，才允许 `reset_schedule(now)` 重新排程；这样改动微休息/长休息设置后，当前 break 和已排队的下一次 break 会保持不变，新设置从下一轮 `schedule_next_slot()` 开始生效。并新增两条 Rust 单测覆盖“当前 break 保持”和“已排队 next break 保持”。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（12 passed，含 `update_settings_keeps_current_break_running` 与 `update_settings_keeps_already_scheduled_next_break`）；`npm test` PASS（3 files / 55 tests）；`npm --prefix apps/desktop run build` PASS。
- Knowledge Capture: 已沉淀“settings 更新只在没有 active break flow 且未被阻塞时才允许重排 schedule”这一状态机边界。
- Knowledge Route: local（沉淀到 `state.rs`、Rust tests 与本任务 specs/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 `update_settings()` 的 schedule 生效边界与对应 Rust 测试，不扩展到显式 reset/pause/focus 语义。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成状态机修复、验证与文档闭环。
- Risk & Rollback: 主要风险是“无 active break flow 时不重排 schedule”可能导致 future schedule 丢失；本轮通过保留“无 active flow 且未阻塞时仍 reset”的边界来兜底。若出现回归，回退 `state.rs` 与新增测试即可。
- Retention Review: 此次状态机边界修复必须长期保留，因为它直接决定设置保存不会破坏当前节奏。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-tray-live-countdown · 修复托盘实时倒计时

- Task-ID: TID-20260409-tray-live-countdown
- Goal: 修复顶部 tray 倒计时看起来卡住的问题，让当前 break / focus / pause / 下一次 break 的剩余时间按秒真实刷新。
- Requirement Brief: 用户反馈顶部 tray 的倒计时像卡住一样不友好；本任务只修复 tray 标题的实时倒计时与 tooltip 展示，不改调度状态机和菜单动作。
- Assumptions: 用户反馈的“顶部 tray”指 macOS menubar 顶部标题区域；菜单不需要每秒重建，只需要顶部时间实时变化。
- Source Basis: `apps/desktop/src-tauri/src/{shell,engine,state}.rs` 与 Tauri tray API 源码 `/Users/changkunyang/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tauri-2.10.3/src/tray/mod.rs`。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 tray 标题/tooltip 刷新链路；touches_running_service=no；backup_required=no；backup_plan=以 Rust tests、desktop build 与 docs validator 为边界；rollback_plan=回退 `shell.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=宿主展示层修复，不涉及外部系统、权限或数据迁移。
- Change Summary: `refresh_tray()` / `refresh_tray_if_needed()` 现在除了菜单刷新外，还会单独同步 tray title/tooltip；新增 `tray_snapshot()`、`sync_tray_text()`、`tray_title()`、`tray_countdown_ms()`、`format_tray_countdown()` 等 helper，让顶部 tray 标题以 `m:ss / h:mm:ss` 真实倒计时显示当前 break、focus、定时 pause 或下一次 scheduled break 的剩余时间；阻塞态会隐藏误导性的 scheduled countdown；同时新增 Rust 单测覆盖格式、当前 break 优先级和阻塞态隐藏逻辑。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（15 passed，含 `shell::tests::format_tray_countdown_uses_live_seconds`、`shell::tests::tray_title_prefers_current_break_remaining`、`shell::tests::blocked_states_hide_schedule_countdown`）；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“tray 菜单刷新和 tray 文本刷新必须分层：前者按状态/分钟桶变化，后者按秒更新 title”这一宿主展示规则。
- Knowledge Route: local（沉淀到 `shell.rs`、Rust tests 与本任务 specs/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 tray title/tooltip 展示层与对应 Rust 测试，不扩展到调度状态机或菜单动作。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成 tray 实时倒计时修复、验证与文档闭环。
- Risk & Rollback: 主要风险是平台不支持的 title/tooltip API 或误把菜单也改成每秒刷新；若出现宿主回归，直接回退 `shell.rs` 里的 live title/tooltip helper 与新增测试。
- Retention Review: 此次修复值得长期保留，因为它把“倒计时像冻结”的 tray 体验问题和“菜单不要每秒闪烁”的稳定性边界分开了。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-tray-menu-live-status-and-option · 修复托盘菜单实时状态并开放倒计时开关

- Task-ID: TID-20260409-tray-menu-live-status-and-option
- Goal: 修复右键 tray 菜单中的状态时间卡住问题，并把顶部 tray 倒计时做成可配置开关；同时补齐该设置项在当前多语言链路下的文案回退。
- Requirement Brief: 用户指出顶部 tray 虽然已经有真实倒计时，但右键展开的原生菜单里时间仍停留在打开瞬间；同时要求顶部倒计时可在设置页控制。后续回归里又发现新设置项直接显示 `ui.showTimeToBreakInTray` key，本任务因此一并修复菜单实时同步、设置项开关和多语言兼容。
- Assumptions: 右键菜单只需要原地更新已有 `status` / `status-detail` 文本，不需要每秒重建整个 native menu；`showTimeToBreakInTray` 仍是设置真源字段名；现有多语言文件里该文案普遍仍位于 legacy `contributorPreferences.showTimeToBreakInTray` 层级，可先做兼容回退再逐步迁移。
- Source Basis: `apps/desktop/src-tauri/src/{shell.rs,state.rs}`、`apps/desktop/src/{App.tsx,i18n.ts}`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`apps/desktop/src/locales/registry.generated.json`、`docs/specs/TID-20260409-tray-menu-live-status-and-option/*`、当日相邻 tray/settings 任务 logs/plans。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 tray 菜单状态文本、tray title 显示开关、设置 schema/设置页与 locale 取值回退；touches_running_service=no；backup_required=no；backup_plan=依赖 locale sync、TypeScript typecheck、desktop build、Rust tests 与 docs validator；rollback_plan=回退 `shell.rs`、`state.rs`、`App.tsx`、locale 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=全部是本地宿主显示与设置修复，不涉及外部系统、权限或数据迁移。
- Change Summary: `apps/desktop/src-tauri/src/shell.rs` 现在在 1s tick 中对已注册的 `status` / `status-detail` 菜单项直接 `set_text()`，右键展开后的菜单状态也会实时更新；`apps/desktop/src-tauri/src/state.rs` 与 `apps/desktop/src/App.tsx` 新增并暴露 `showTimeToBreakInTray` 开关；`apps/desktop/src/App.tsx` 同时为该设置项增加 `ui.showTimeToBreakInTray -> contributorPreferences.showTimeToBreakInTray` 的兼容回退，`apps/desktop/src/locales/messages/{en,zh-CN}.json` 也补入了新的 `ui` 层级 key，避免设置页直接露出原始 key。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages，已重建 `apps/desktop/src/locales/registry.generated.json`）；`npm --prefix apps/desktop run typecheck` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（16 passed，覆盖 `shell::tests::tray_title_respects_setting_toggle` 等 tray 回归）；`npm --prefix apps/desktop run build` PASS（保留既有 Vite chunk size warning）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“tray 菜单实时状态应通过原地更新菜单项文本实现，而不是靠菜单重建”以及“新设置页文案切到 `ui.*` 时，需要给旧 locale 层级保留兼容回退，否则会直接把 key 暴露给用户”的维护规则。
- Knowledge Route: local（沉淀到本任务 specs、CHANGELOG 与 SettingsInventory）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 tray 菜单状态展示、设置页开关、locale 兼容回退与相关 docs，不扩展到调度状态机或更多设置页重组。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 tray 宿主修复、验证与文档闭环。
- Risk & Rollback: 主要风险在于 1s tick 下原生菜单项更新带来新的平台兼容性问题，或设置页新 key 与旧 locale 层级再次脱节；若出现问题，优先回退 `shell.rs` / `App.tsx` / locale 变更并重新执行 tray 与 build 验证。
- Retention Review: `showTimeToBreakInTray` 设置项、tray 菜单原地文本更新方式，以及 locale 兼容回退都属于当前产品长期需要保留的实现边界；不应再退回到“靠菜单重建刷时间”或“直接暴露 raw key”的状态。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-ideas-source-random-fix · 接回 break ideas 真源并删除过渡 prompts

- Task-ID: TID-20260409-break-ideas-source-random-fix
- Goal: 让 break 页交互语重新从 `miniBreakIdeas` / `longBreakIdeas` 读取，并删除 `ui.breakCopy.prompts` 过渡字段与相关回退。
- Requirement Brief: 用户明确指出 break 交互语现在不是从 `apps/desktop/src/locales/messages` 里的 `miniBreakIdeas/longBreakIdeas` 随机得到，而是固定落在一条；随后又明确要求 `ui.breakCopy.prompts` 这种过渡字段应直接删除，类似过渡层也不要继续保留。本任务因此同时修复 prompt 真源、固定周期索引缺陷和过渡字段清理。
- Assumptions: 顶层 locale ideas 结构在当前语言里保持完整；`breakIdeasEnabled = false` 时仍用 `ui.breakCopy.defaultPrompt.*`；同一 break 内 prompt 需要稳定，不能因为页面每秒刷新而抖动。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts}`、新增 `apps/desktop/src/lib/break-ideas.ts`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`app/main.js`、`app/utils/{defaultMicrobreakIdeas.js,defaultBreakIdeas.js,ideasLoader.js,shuffled.js}`、`docs/UI.md` 与当日相邻任务 docs。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 break prompt 文案来源、前端 helper、locale JSON、回归测试与相关 docs；touches_running_service=no；backup_required=no；backup_plan=依赖 locale sync、typecheck、test、build 与 docs validator；rollback_plan=回退 `i18n.ts`、`break-ideas.ts`、`App.tsx`、locale JSON、测试与 docs；destructive_operations=删除 `ui.breakCopy.prompts` 过渡字段；operator_approval_required=no；rationale=纯前端文案与算法修复，不涉及外部系统或数据迁移。
- Change Summary: 新增 `apps/desktop/src/lib/break-ideas.ts`，让 break 页交互语直接从 `miniBreakIdeas` / `longBreakIdeas` 的 `.text` 列表读取，并使用稳定混洗索引替代原先的简单取模；`apps/desktop/src/i18n.ts` 新增 `tBreakIdeaList()`；`apps/desktop/src/App.tsx` 现改用新的 helper；`apps/desktop/src/locales/messages/{en,zh-CN}.json` 中的 `ui.breakCopy.prompts` 已删除；`test/desktopBreakIdeas.js` 与 `test/desktopBreakCopySource.js` 补了真源与“过渡字段不存在”的回归断言。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm test` PASS（4 files / 58 tests）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（保留既有 Vite chunk size warning）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“break prompt 的唯一真源应是 `miniBreakIdeas/longBreakIdeas`，而不是并行的 `ui.breakCopy.prompts` 过渡数组”，以及“固定周期调度不能用简单时间取模来选 prompt，否则会稳定撞到同一条”的规则。
- Knowledge Route: local（沉淀到本任务 specs、UI.md、CodeMap.md 与 CHANGELOG）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 break prompt 真源、选择 helper、locale JSON、测试与相关 docs，不扩展到 break 页视觉、CTA 或调度状态机。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 prompt 真源修复、验证与文档闭环。
- Risk & Rollback: 主要风险是 ideas 路径接错导致页面总是回到 default prompt，或删除过渡字段后还有残留代码继续读旧 key；若出现问题，优先回退 `break-ideas.ts` / `i18n.ts` / `App.tsx` / locale JSON 与相关测试，然后重新跑 sync / test / build。
- Retention Review: 当前保留下来的 `break-ideas.ts` 和 `tBreakIdeaList()` 属于长期应保留的实现边界；`ui.breakCopy.prompts` 这类过渡字段已被证明会制造双真源和错误路径，不应继续保留。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-smart-reminder-wait-guard · 缩短智能提醒阈值并阻止无限等待

- Task-ID: TID-20260409-smart-reminder-wait-guard
- Goal: 缩短 smart reminder 的默认空档阈值、在设置页暴露该阈值，并为 smart 模式补上“最多等待多久就必须开始”的 host 侧保护。
- Requirement Brief: 用户明确指出智能提醒当前阈值偏长，而且在持续输入/操作时可能一直等待；本任务直接收口成“默认更短 + 用户可调 + 最迟开始”的组合修复。
- Assumptions: `idle_opportunity_seconds` 之前未在前台设置页暴露，当前用户侧的主要可感知问题是默认值偏长和状态机无限等待，而不是需要重做 `ReminderMode` 模型。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/{state.rs,engine.rs,platform.rs}`、`apps/desktop/src/App.tsx`、`apps/desktop/src/i18n.ts`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`test/desktopSettingsControls.js`、`docs/{UI,Architecture,SettingsInventory}.md`、`docs/logs/2026-04-08.md` 与当日相邻任务记录。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 smart reminder host 状态机、设置页智能暂停控件、locale 文案与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 locale sync、npm test、cargo test、typecheck、build 与 docs validator；rollback_plan=回退 `state.rs`、`App.tsx`、locale、测试与 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、提权、数据迁移或外部副作用。
- Change Summary: `state.rs` 将 `idle_opportunity_seconds` 默认值从 `12s` 收紧到 `6s`，并新增按 break kind 计算的 smart wait cap：仍未出现空档时，微休息不会无限等待，达到有限等待上限后会自动开始；同时在 `App.tsx` 的“智能暂停”分组重新暴露 `idleOpportunitySeconds` 控件，让用户可直接调节空档阈值；`messages/{en,zh-CN}.json` 同步补入新的设置文案，并把 smart reminder 的提示语改成“先等短空档，但不会无限等待”；`test/desktopSettingsControls.js` 和 Rust 单测新增覆盖。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm test` PASS（4 files / 59 tests）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（17 Rust tests）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“智能提醒的合理模型不是无限 defer，而是短空档阈值 + 有界等待上限”的规则，以及“之前 hidden 的阈值在用户已经感知到问题时应直接变成设置项”。
- Knowledge Route: local（已回写到 `docs/{UI,Architecture,SettingsInventory,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 smart reminder host 状态机、设置页智能暂停阈值、locale 与相关文档，不扩展到更多提醒模式拆分或复杂 UI 重排。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次状态机、设置页、测试与文档闭环。
- Risk & Rollback: 主要风险是 waiting 状态文案或 snapshot 判定与 tick 节拍脱节，或设置页新增控件造成 locale key 漏配；若出现回归，回退 `state.rs`、`App.tsx`、locale JSON、测试与 docs，并重新生成 registry 后复验。
- Retention Review: 本次新增的 bounded wait 规则与设置页阈值控件都属于长期产品语义，应保留；未引入临时调试开关或一次性脚本。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-smart-reminder-ui-simplification · 收回智能提醒阈值设置项并保留内部策略

- Task-ID: TID-20260409-smart-reminder-ui-simplification
- Goal: 撤回设置页中 smart reminder 的阈值调节项，让前台重新只保留“智能提醒 / 强制提醒”两种模式选择。
- Requirement Brief: 用户明确反馈不需要太多可设置值；本任务将 smart reminder 的 `等待空档` 秒数控件从前台移除，只保留内部 `6s + bounded wait cap` 策略。
- Assumptions: 当前用户的核心诉求是减少前台参数心智，而不是要求继续调整 host 的默认阈值或等待上限。
- Source Basis: 已阅读 `apps/desktop/src/App.tsx`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`apps/desktop/src/i18n.ts`、`test/desktopSettingsControls.js`、`docs/{UI,Architecture,SettingsInventory,CHANGELOG}.md` 与同日 `smart-reminder-wait-guard` 记录。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限设置页 smart reminder 控件、locale 与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 locale sync、npm test、typecheck、build 与 docs validator；rollback_plan=回退 `App.tsx`、locale、测试与 docs；destructive_operations=none；operator_approval_required=no；rationale=不改 host 状态机，不涉及线上服务、权限或数据迁移。
- Change Summary: 从 `App.tsx` 中移除“智能暂停”分组下的 `等待空档` 数字控件；`messages/{en,zh-CN}.json` 同步删除 `ui.waitForPause*` 文案；`desktopSettingsControls.js` 去掉对应 locale 断言；当前产品口径收回为“前台只保留 reminder mode，`6s + bounded wait cap` 继续作为 host 内部策略”。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm test` PASS（4 files / 58 tests）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“少配置、强默认”更适合当前提醒产品，smart reminder 的策略值不应轻易暴露为前台设置。
- Knowledge Route: local（已回写到 `docs/{UI,Architecture,SettingsInventory,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限设置页 smart reminder 控件、locale 与相关文档，不扩展到 host 策略参数或 reminder mode 模型改写。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次设置减法、验证与文档闭环。
- Risk & Rollback: 主要风险是 locale registry 残留已删除 key 或文档口径与当前 UI 不一致；若出现回归，回退 `App.tsx`、locale、测试与 docs，并重新同步 registry 后复验。
- Retention Review: 当前减法符合产品方向，应保留；不需要临时兼容层或额外开关。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-smart-reminder-decay-thresholds · 将智能提醒改为递减阈值曲线

- Task-ID: TID-20260409-smart-reminder-decay-thresholds
- Goal: 把 smart reminder 改成“先等更明显空档，再逐步放宽阈值，最后到 deadline 直接开始”的递减阈值曲线。
- Requirement Brief: 用户明确认可“递减阈值 + 最终截止”的设计思路；本任务直接把当前 host 侧逻辑从单一阈值模型收敛为分阶段曲线，并把设计写入长期文档。
- Assumptions: 对当前产品来说，“少配置、强默认”优先级高于继续暴露更多提醒参数；因此这轮只改内置策略，不重新开放设置项。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`docs/{Architecture,UI,SettingsInventory,ReminderScheduling,CHANGELOG}.md`、当日 plans/logs，以及同日 `smart-reminder-wait-guard` / `smart-reminder-ui-simplification` 任务 docs。
- Level: moderate
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 smart reminder host 状态机、locale 文案与 reminder 设计文档；touches_running_service=no；backup_required=no；backup_plan=依赖 locale sync、cargo test、npm test、typecheck、build 与 docs validator；rollback_plan=回退 `state.rs`、locale、文档与测试；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、提权、数据迁移或外部副作用。
- Change Summary: `state.rs` 删除了“单一 idle opportunity 阈值 + 单一 wait cap”的判定方式，改成按 break kind 的内置递减曲线：微休息使用 `6s -> 3s -> 1s -> 45s deadline`，休息使用 `8s -> 4s -> 1s -> 90s deadline`；同时新增 Rust 单测覆盖“微休息阈值放宽”“长休息前期更宽阈值”和“最终 deadline 触发”三条主路径。`messages/{en,zh-CN}.json`、`docs/{Architecture,UI,SettingsInventory,ReminderScheduling,CHANGELOG}.md` 也同步改成这套策略口径。
- Verification: `python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（19 Rust tests）；`npm test` PASS（4 files / 58 tests）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“智能提醒的最佳默认不是固定阈值，也不是把一堆数暴露给用户，而是用 per-kind 递减阈值曲线在礼貌等待和最终确定性之间找平衡”。
- Knowledge Route: local（已回写到 `docs/{Architecture,UI,SettingsInventory,ReminderScheduling,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 smart reminder host 状态机、locale 文案与设计文档，不扩展到更多 UI 设置项或提醒模式拆分。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次状态机、测试与文档闭环。
- Risk & Rollback: 主要风险是曲线阶段切换时机写错，导致某一阶段太早或太晚开始；若出现回归，回退 `state.rs`、locale、文档与测试，并重新验证 Rust/前端链路。
- Retention Review: 这套曲线是当前产品的长期默认策略，应保留；不需要引入临时开关或额外兼容层。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-window-macos-fullscreen-coverage · 修复 macOS 全屏工作区下 break 窗口覆盖当前工作屏幕

- Task-ID: TID-20260409-break-window-macos-fullscreen-coverage
- Goal: 修复 macOS 全屏工作区里 break 窗口没有覆盖当前工作屏幕的问题，让现有 break 提示能在当前 active Space 正常出现。
- Requirement Brief: 用户明确反馈提醒窗口/全屏在 macOS 全屏工作时没有覆盖当前工作屏幕；本任务只修 Tauri host 的原生窗口展示策略，不改前端 break 页面布局、调度状态机或 monitor 选择模型。
- Assumptions: 根因主要在 macOS 的 Space / window level / collection behavior，而不是 React 前台样式或休息调度逻辑。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/{shell.rs,commands.rs,state.rs}`、`docs/{Architecture,CHANGELOG}.md`、当日相邻任务记录，并审阅本地 Tauri 2 / tao 的 macOS window API 源码以确认 `ns_window()`、`collectionBehavior`、`setLevel` 与 `visible_on_all_workspaces` 的实际语义。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 macOS break 宿主窗口展示层与相关文档；touches_running_service=no；backup_required=no；backup_plan=依赖 `cargo test`、desktop build 与 docs validator；rollback_plan=回退 `shell.rs` 中的 macOS native helper、相关测试与文档；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、权限、数据迁移或外部副作用。
- Change Summary: `apps/desktop/src-tauri/src/shell.rs` 现在会在 macOS 上为 break 窗口额外 patch 原生 `NSWindow`：在现有 `visible_on_all_workspaces` 之外，再补 `CanJoinAllSpaces | FullScreenAuxiliary` collection behavior，并按 fullscreen / windowed 分别提升到更高的 native level；窗口显示后还会主动 `orderFrontRegardless`，避免停留在后台 Space。与此同时新增 Rust 单测守住这套原生 overlay policy，并把设计同步写入 `docs/Architecture.md` 与 `docs/CHANGELOG.md`。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（20 tests，新增 `shell::tests::macos_break_window_native_overlay_policy_uses_expected_levels`）；`npm --prefix apps/desktop run build` PASS（保留既有 Vite chunk size warning）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“macOS 下仅依赖 `set_visible_on_all_workspaces(true)` 不足以覆盖全屏 Space；break overlay 还需要 `FullScreenAuxiliary` 与更高 native window level”的平台规则。
- Knowledge Route: local（沉淀到 `docs/Architecture.md`、`docs/CHANGELOG.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 `shell.rs` 的 macOS break window native helper、对应测试与文档闭环，不扩展到前端 UI、状态机或多屏策略。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 macOS break window 展示层修复、验证与文档闭环。
- Risk & Rollback: 主要风险是原生层级设得过高，导致普通场景下 break window 过于强势，或 selector 调用错误影响窗口显示；若出现回归，回退 `shell.rs` 中的 macOS helper 与单测，并重新跑 `cargo test` / desktop build。
- Retention Review: 这组 macOS native helper 属于长期应保留的平台修复；它明确记录了 break overlay 在全屏 Space 下的额外约束，后续不应再退回仅靠 Tauri 抽象层的实现。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-cta-tightening · 收紧 break 完成与延后按钮时机

- Task-ID: TID-20260409-break-cta-tightening
- Goal: break 不再允许提前完成，`Later` 只允许在倒计时开始后的前 10 秒。
- Requirement Brief: 用户明确要求取消 break 的提前完成，并把延后窗口收紧为倒计时开始后的前 10 秒；本任务同时收紧 host 侧判定和前端 CTA 显示。
- Assumptions: 当前用户只要求收紧 finish / postpone，不要求改 skip、manual finish 设置项或 break 时长。
- Source Basis: 已阅读 `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/App.tsx`、`docs/{UI,SettingsInventory,CHANGELOG}.md` 与当日相邻 break 任务记录。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 break CTA 时机规则、对应测试与文档；touches_running_service=no；backup_required=no；backup_plan=依赖 Rust/前端测试、desktop build 与 docs validator；rollback_plan=回退 `state.rs`、`App.tsx`、测试与文档；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、权限、数据迁移或外部副作用。
- Change Summary: `state.rs` 现在要求 `finish_current_break` 只有在 `manualAwaiting` 时才能成功，普通倒计时阶段不会再被提前结束；`can_postpone` 也从原来的“前 30% 时间窗口”改成固定的前 `10_000ms`。`App.tsx` 的 break CTA 同步改成只有在 `manualAwaiting` 才显示主按钮，普通倒计时阶段不再渲染 `Done`。`docs/{UI,SettingsInventory,CHANGELOG}.md` 与本任务 specs 已同步更新。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（含 `postpone_is_only_available_in_first_ten_seconds`、`finish_current_break_requires_manual_awaiting`）；`npm test` PASS（4 files / 58 tests）；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“break CTA 的最终约束必须以后端为准；提前完成与延后窗口都不能只靠前端按钮控制”的规则。
- Knowledge Route: local（沉淀到 `docs/{UI,SettingsInventory,CHANGELOG}.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 break CTA 时机、前端显示、测试与文档，不扩展到 skip、调度或多语言结构。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 CTA 规则修复、验证与文档闭环。
- Risk & Rollback: 主要风险是前后端规则不一致，或者 10 秒边界实现错误；若出现回归，回退 `state.rs`、`App.tsx`、测试与文档，并重新验证 Rust/前端链路。
- Retention Review: 这组规则属于长期产品边界，应保留；不需要临时兼容层或额外设置项。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-window-native-exception-guard · 修复 macOS 休息窗口原生异常闪退

- Task-ID: TID-20260409-break-window-native-exception-guard
- Goal: 修复进入 break 前的 macOS 原生 `NSWindow` patch 触发 foreign exception abort，让 patch 失败时退化为 no-op，而不是直接终止进程。
- Requirement Brief: 用户提供崩溃信息并要求优先处理 fatal break crash；本任务只修复 `shell.rs` 的 macOS break window 原生 patch 与展示时机，不改 React break 页面、调度状态机或 tray 结构。
- Assumptions: 根因在 Objective-C exception 穿透 Rust event loop；native patch 属于增强项，失败时允许降级为 no-op。
- Source Basis: `apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src-tauri/src/{engine.rs,commands.rs}`、`docs/specs/TID-20260408-tauri-window-fullscreen-fix/*`、`docs/specs/TID-20260409-break-window-macos-fullscreen-coverage/*`、`~/Library/Logs/DiagnosticReports/pauza-desktop-2026-04-09-224258.ips`、`~/Library/Logs/DiagnosticReports/pauza-desktop-2026-04-09-232038.ips`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 macOS break 宿主窗口 patch 与任务文档；touches_running_service=no；backup_required=no；backup_plan=依赖 crash report 对照、`cargo check`、`cargo test`、desktop build 与 workflow validators；rollback_plan=回退 `apps/desktop/src-tauri/src/shell.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=不涉及运行中服务、数据迁移、权限或外部副作用。
- Change Summary: 已将 break window 的 macOS native patch 包进统一 exception guard，Objective-C exception 发生时只输出 `eprintln!` 并降级为 no-op；同时把 `configure_break_window_native_behavior()` / `present_break_window()` 延后到 `window.show()` 之后执行，并补齐 evidence、Architecture、CHANGELOG、daily docs。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（含 `shell::tests::macos_break_window_native_overlay_policy_uses_expected_levels`）；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；`python3 scripts/validate_agent_configs.py` PASS。
- Knowledge Capture: 已沉淀“macOS 原生 AppKit patch 必须视为 best-effort 增强项，任何异常都只能降级为 no-op，不能继续成为 Rust event loop 的 abort 点”的边界。
- Knowledge Route: local（沉淀到 `docs/Architecture.md`、`docs/CHANGELOG.md`、evidence README 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 crash report 复盘、`shell.rs` 修复、构建验证、evidence 与文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次崩溃定位、修复、验证、evidence 与文档闭环。
- Risk & Rollback: 主要风险是 native patch 降级后 macOS 全屏 Space 覆盖增强可能变保守；若出现回归，优先回退 `shell.rs` 中新增的 exception guard / 调用时机调整与对应 docs，并重跑 Rust/前端验证链路。
- Retention Review: 这次修复明确了“原生 patch 只能增强不能致命”的工程边界，应长期保留；crash report 分析与 evidence 文档也值得保留，便于后续继续缩小 AppKit 风险面。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260409-break-window-layout-and-controls-refresh · 重做 break 界面布局并移除跳过按钮

- Task-ID: TID-20260409-break-window-layout-and-controls-refresh
- Goal: 将 break 页面收口为更安静的单列休息界面，并移除主界面的 `Skip`，只保留与当前 break 状态一致的动作。
- Requirement Brief: 用户明确要求重做 break 界面布局并移除跳过按钮；本任务只改 `App.tsx` / `break-prompt.ts` 的 break 页面结构、视觉层次和按钮出口，不改 host 状态机与命令面。
- Assumptions: 当前 break 页面需要更克制、更像桌面工具；`Skip` 不应继续作为主界面常驻动作。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/lib/break-prompt.ts`、`docs/UI.md`、`docs/CHANGELOG.md` 与同日相邻 break / CTA 任务记录。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 break 页面布局、CTA 出口、相关文档与验证；touches_running_service=no；backup_required=no；backup_plan=依赖 `npm test`、desktop build 与 workflow docs validator；rollback_plan=回退 `App.tsx`、`break-prompt.ts` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=仅前台界面收敛，无运行中服务、权限或外部副作用。
- Change Summary: break 页面已从展示型双栏收口为居中的单列内容区，主视觉改为单条提示语、数字倒计时和细条形进度；主界面不再直接暴露 `Skip`，CTA 只按 `manualAwaiting` / `canPostpone` 渲染允许动作，并同步补齐 UI/CHANGELOG 与本任务 specs。
- Verification: `npm test` PASS；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已沉淀“break 页面只保留单条 prompt、数字倒计时、线性进度和状态允许的 CTA，不再在主界面堆展示型卡片或常驻 skip 操作”的界面边界。
- Knowledge Route: local（沉淀到 `docs/UI.md`、`docs/CHANGELOG.md` 与本任务 specs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 break 页面布局、CTA 出口、前台验证与文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 break 页面重构、验证与文档闭环。
- Risk & Rollback: 主要风险是去掉 `Skip` 后页面出口过窄，或者单列收敛破坏当前主题/壁纸层次；若出现回归，优先回退 `App.tsx`、`break-prompt.ts` 与相关 docs，再重新跑前台验证。
- Retention Review: 这次界面减法属于长期产品方向，应保留；不需要再为旧双栏结构或主界面 `Skip` 保留兼容层。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
