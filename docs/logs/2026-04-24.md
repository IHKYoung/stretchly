## TID-20260424-microbreak-credit-due-gate · 修复恢复结算误提前抵扣微休息

- Task-ID: TID-20260424-microbreak-credit-due-gate
- Goal: 修复 smart reminder v2 中 recovery credit 误提前结算未到点微休息/长休息的问题。
- Requirement Brief: 用户反馈“现在没有轻休息了，一直都是长休息”；经排查确认本机设置正常，根因在 `state.rs` 的 idle return recovery credit 缺少 due gate。
- Assumptions: 当前设计仍允许长时间离开触发 natural break full reset；本轮只修“未到点也被 credit/defer”的实现漂移，不改 smart 提醒阈值策略。
- Source Basis: `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src-tauri/src/engine.rs`、`apps/desktop/src/App.tsx`、`docs/ReminderScheduling.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/logs/2026-04-23.md`、`~/Library/Application Support/com.ahaknow.pauza/settings.json`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop host reminder 调度、Rust 测试与 workflow docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、权限提升或外部副作用。
- Change Summary: 将 `apply_recovery_credit()` 的准入条件从 `next_break_kind` 收紧为 `pending_due_kind(now)`，使 `45s+` idle return 只会结算 overdue pending break；新增两个回归测试，分别覆盖“未到点返回不会自动抵扣 microbreak”和“未到点返回不会提前顺延 long break”。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml microbreak_is_not_credited_before_due_when_user_returns -- --nocapture` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml long_break_is_not_deferred_before_due_when_user_returns -- --nocapture` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml microbreak_is_credited_after_user_returns -- --nocapture` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml long_break_is_deferred_after_user_returns -- --nocapture` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（32 passed）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（将“recovery credit 必须只结算 overdue break”的规则沉淀到测试和 workflow docs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop host 状态机、Rust 测试与 workflow/docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 完成修复与验证。
- Risk & Rollback: 主要风险是误伤 natural break full reset 或 overdue return 的正常 credit；若出现回归，回退 `apply_recovery_credit()` 的 due gate 与新增测试。
- Retention Review: 本轮值得长期保留的是这条边界规则本身。recovery credit 属于“已到点 break 的返回补偿”，不应吞掉任意未到点的 pending break；把它固化为单测能防止 reminder v2 后续继续漂移。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260424-smart-reminder-minimal-wait-model · 收缩智能提醒为固定阈值与最长等待

- Task-ID: TID-20260424-smart-reminder-minimal-wait-model
- Goal: 将当前复杂的 smart reminder v2 收回到固定阈值 + 最长等待的最小模型，避免 recovery hold / recovery credit / staged threshold 把 smart/forced 的状态边界搅乱。
- Requirement Brief: 用户确认当前主要痛点是“状态机过重、递减阈值设计不好”，而不是缺少更多智能性；本任务收缩 host 调度，只保留明确空档与最大等待两个开始条件。
- Assumptions: 用户接受 smart mode 只是“到点后先等空档”的最小实现；`BreakActive` 优先级与 blocker freeze 语义应继续保留，不应随这次简化被回滚掉。
- Source Basis: `apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/engine.rs`、`docs/ReminderScheduling.md`、`docs/CHANGELOG.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/logs/2026-04-23.md`、`docs/specs/TID-20260424-microbreak-credit-due-gate/plan.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop host reminder 调度、locale 与 docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs`、locale 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、权限提升或外部副作用。
- Change Summary: 已把 smart reminder 从“recovery hold / recovery credit + staged threshold”收缩为 fixed threshold + max wait；`state.rs` 中到点后只保留“idle 达标即开始 / 等满即开始 / 否则等待空档”三条路径，同时删除恢复补偿相关辅助函数与测试分支，并同步更新 locale、`apps/desktop/src/locales/registry.generated.json`、`ReminderScheduling.md` 与 `docs/CHANGELOG.md`。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（27 passed）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；`python3 scripts/sync_desktop_locales.py` PASS；尝试 `cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml` 时发现当前 toolchain 未安装 `rustfmt` 组件，因此本轮未做自动格式化。
- Knowledge Capture: yes
- Knowledge Route: local（将“smart reminder 保持 fixed threshold + max wait 的最小边界”沉淀到调度文档与测试）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop host 调度、locale、docs 与 Rust 测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 完成实现与验证。
- Risk & Rollback: 主要风险是 fixed threshold / max wait 参数不合适，或 recovery 旧分支删减不彻底导致行为/文案漂移；若出现回归，回退本任务在 `state.rs`、locale 与 docs 的改动。
- Retention Review: 本轮值得保留的是“smart 不再承担恢复补偿”的边界本身。比起继续加状态，固定阈值 + 最长等待更容易解释、测试和维护。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260424-break-ideas-asset-migration · 拆分 break ideas 资产真源

- Task-ID: TID-20260424-break-ideas-asset-migration
- Goal: 将 `miniBreakIdeas / longBreakIdeas` 从 `locales/messages/*.json` 迁移到独立的 break-ideas 资产目录，并用数据化 registry 标记 `official / legacy` 语言，避免 break ideas 继续和 UI 文案耦合。
- Requirement Brief: 用户明确要求 break ideas 不应继续躺在 UI message bundle 里，也不应被抽成代码常量；本任务把 `miniBreakIdeas / longBreakIdeas` 迁移为独立 JSON 真源，同时保留现有 50 语言资产，先只用 registry 标记 `zh-CN / zh-TW / en` 为 official、其它为 legacy。
- Assumptions: 本轮先迁移全部现有语言 break ideas，不在这一步删除任何 legacy 语言文件；未来如果某个语言不再维护，可以直接删掉其 break-ideas bundle，让运行时通过 fallback 补位。
- Source Basis: `apps/desktop/src/{i18n.ts,App.tsx,lib/break-ideas.ts}`、`apps/desktop/src/locales/{messages,config}/`、`scripts/sync_desktop_locales.py`、`test/{desktopBreakIdeas,desktopBreakCopySource,translations}.js`、`docs/{RepositoryGuidelines,CodeMap,UI,Architecture,CHANGELOG}.md`、`docs/specs/TID-20260409-break-ideas-source-random-fix/{README,plan,arch,testplan}.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 desktop 前台 break ideas 资产、locale/build scripts、Vitest 与 docs；touches_running_service=no；backup_required=no；backup_plan=`python3 scripts/sync_desktop_break_ideas.py`、`python3 scripts/sync_desktop_locales.py`、`npm run typecheck`、`npm test`、`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `apps/desktop/src/locales/break-ideas/**`、`messages/*.json` 的 ideas 拆分、`lib/break-ideas.ts`、`i18n.ts`、sync scripts 与 docs；destructive_operations=从 `messages/*.json` 删除 `miniBreakIdeas / longBreakIdeas` 顶层 key；operator_approval_required=no；rationale=纯本地资产迁移，无服务影响、无外部副作用或权限变更。
- Change Summary: 新增 `apps/desktop/src/locales/break-ideas/messages/*.json`、`registry.json` 与 `registry.generated.json` 作为 break ideas 独立真源和运行时索引；50 个 locale 的 `miniBreakIdeas / longBreakIdeas` 已从 `messages/*.json` 批量迁移出去；`scripts/sync_desktop_break_ideas.py` 现负责生成 ideas registry，`scripts/sync_desktop_locales.py` 则反向校验 message bundle 不再包含 ideas key；前端 `lib/break-ideas.ts` 改为读取独立 registry，并按当前语言优先、locale fallback 补位；相应 tests/docs 已同步更新。
- Verification: `python3 scripts/sync_desktop_break_ideas.py` PASS（50 languages, 50 bundles）；`python3 scripts/sync_desktop_locales.py` PASS（50 languages）；`npm run typecheck` PASS；`npm test` PASS（5 files, 113 tests）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（27 passed）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（沉淀“break ideas 独立 JSON 真源 + registry 标记 official/legacy + helper 只读不承载内容”的边界）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop locale 资产、前台 helper、sync scripts、测试与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 完成 break ideas 资产迁移与验证。
- Risk & Rollback: 主要风险是某些运行时路径仍偷偷从 `messages/*.json` 读取 ideas，或未来删除 legacy bundle 时 fallback 不工作；若出现回归，优先回退 ideas 目录、`lib/break-ideas.ts` 与 sync scripts，再重新生成两个 registry。
- Retention Review: 本轮值得长期保留的是“内容真源与界面翻译分层”的资产边界，以及 `official / legacy` 只作为数据标签而不是代码常量的做法。迁移脚本与独立 registry 会显著降低后续继续精修三语、逐步整理 legacy 语言时的摩擦。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260424-structure-cleanup · 整理 reminder 与 break ideas 的结构残留

- Task-ID: TID-20260424-structure-cleanup
- Goal: 清理 reminder / break ideas 当前实现里的结构残留，收掉仍在主模型里漂浮的兼容字段，并降低 `tick()` 主流程噪音。
- Requirement Brief: 用户在确认整体结构方向后要求继续整理代码；本任务不改提醒行为，只清理前台设置模型、host 序列化边界和 due helper 组织。
- Assumptions: 旧配置里仍可能带 `idleOpportunitySeconds`；本轮允许将其保留为载入兼容字段，但不再继续写回新的 settings/snapshot。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-ideas.ts}`、`apps/desktop/src-tauri/src/state.rs`、`docs/{SettingsInventory,Architecture,ReminderScheduling}.md`、`docs/specs/TID-20260424-smart-reminder-minimal-wait-model/arch.md`、`docs/specs/TID-20260424-break-ideas-asset-migration/README.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop 前台设置模型、Rust host 调度 helper、文档与测试；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`npm run typecheck`、`npm test`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `App.tsx`、`state.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地结构整理，无线上服务或外部副作用。
- Change Summary: 从 `App.tsx` 的前台 `PauzaSettings` 模型移除 `idleOpportunitySeconds`；`state.rs` 将 `idle_opportunity_seconds` 改为 `skip_serializing` 的兼容字段，只保留载入旧配置时可读；同时把 `tick()` 中的到点通知和到点开休息逻辑抽成 `maybe_dispatch_due_notification()` / `maybe_start_due_break()` helper，并同步更新 `SettingsInventory`、`Architecture` 与 `CHANGELOG`。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（28 passed）；`npm run typecheck` PASS；`npm test` PASS（113 passed）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（将“兼容字段只读不回写”和“在大文件里先抽 helper 再拆模块”的整理策略沉淀到本任务 docs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop 前台设置模型、Rust host 调度 helper、docs 与测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 在限定范围内完成结构整理与验证。
- Risk & Rollback: 主要风险是旧配置兼容边界处理不当，或 helper 抽取时改变 due 判定时序；若出现回归，回退 `App.tsx`、`state.rs` 与本任务 docs 即可。
- Retention Review: 本轮值得长期保留的是两个边界：一是“兼容字段只能进 load path，不应继续占据前台/序列化主路径”，二是“在 `state.rs` 这类大文件里，先抽 helper 压低主流程复杂度，再考虑更大级别拆分”。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260424-state-module-split · 拆分 state.rs 结构边界

- Task-ID: TID-20260424-state-module-split
- Goal: 将 `state.rs` 中可稳定抽离的 settings / persistence / tests 逻辑拆出，让 runtime scheduler 主文件的边界更清楚，同时保持现有行为和对外 API 不变。
- Requirement Brief: 用户要求继续整理代码结构，并明确要求“`state.rs` 里的 tests 拆出去，test 应该是单独的”；本轮仍保持低风险模块拆分，只迁移 settings / persistence / tests，不继续拆 runtime 主状态机。
- Assumptions: 当前 `crate::state` 的 public surface 需要保持兼容；tests 只做文件迁移，不改写断言和覆盖范围。
- Source Basis: `apps/desktop/src-tauri/src/{state.rs,commands.rs,engine.rs,platform.rs,shell.rs}`、`docs/{CodeMap,Architecture,ReminderScheduling}.md`、`docs/specs/TID-20260424-structure-cleanup/plan.md`、`docs/logs/2026-04-24.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop Rust host 的模块边界、文档与测试；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`npm run typecheck`、`npm test`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs`、删除 `state/{settings,persistence}.rs` 并恢复 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地结构整理，无运行中服务或外部副作用。
- Change Summary: 新增 `apps/desktop/src-tauri/src/state/settings.rs` 承接 `PauzaSettings`、相关 enum、shortcut、默认值和 sanitize helper；新增 `apps/desktop/src-tauri/src/state/persistence.rs` 承接 `settings.json` 的 load / save / legacy migration；新增 `apps/desktop/src-tauri/src/state/tests.rs` 承接原先内联在 `state.rs` 里的 Rust 单测；`state.rs` 现在通过子模块声明和 re-export 保留外部 API，并只保留 runtime scheduler 与 snapshot 主逻辑。同步更新 `docs/CodeMap.md`、`docs/CHANGELOG.md` 与本任务 docs。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（28 passed）；`npm run typecheck` PASS；`npm test` PASS（5 files, 113 tests）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（将“在大文件里先拆 settings / persistence，再看 runtime / tests 第二阶段”的整理顺序沉淀到 docs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop Rust host `state` 模块边界、docs 与测试闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，按上层工具策略只能走 `single-agent-fallback`，因此由单 agent 完成模块拆分与验证。
- Risk & Rollback: 主要风险是 re-export / visibility 漏项导致其他 host 模块编译失败，或 persistence / tests 迁移时漏掉 legacy config path 与私有 helper 访问；若出现回归，回退 `state.rs` 与新增子模块，并重新执行 Rust 验证。
- Retention Review: 本轮值得保留的是“先拆稳定边界、暂缓 runtime 大拆”的整理策略。把 settings / persistence / tests 三类稳定内容先拿出去后，`state.rs` 的剩余职责已经基本收口到 runtime 调度本身，比一次性拆空 `state.rs` 更稳，也更符合这轮用户要求的“把代码结构整理清楚”而不是重写状态机。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
