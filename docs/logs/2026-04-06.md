## TID-20260406-settings-inventory-doc · 整理当前设置范围并沉淀设置清单文档

- Task-ID: TID-20260406-settings-inventory-doc
- Goal: 形成一份清晰的设置边界文档，说明当前设置页里有什么、还能直接加什么、哪些要先恢复后端能力。
- Requirement Brief: 用户要求先把设置范围列清楚，并单独落一份 docs 文档；本轮只做代码核对和文档沉淀，不改产品行为。
- Assumptions: 用户当前需要的是设置边界清单，而不是立即实现新设置项；当前问题的事实来源已经完整落在前端设置页、Tauri 真源、命令层和旧版默认配置中。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src-tauri/src/commands.rs`、`app/utils/defaultSettings.js`、`docs/CodeMap.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=none；touches_running_service=no；backup_required=no；backup_plan=以 Git 工作区差异为边界；rollback_plan=回滚 `docs/SettingsInventory.md`、`docs/CodeMap.md` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=仅整理文档与设置边界，不触碰运行时逻辑。
- Change Summary: 新增 `docs/SettingsInventory.md`，按四层结构整理当前设置边界：当前设置页已提供、Tauri 已支持但前端未露出、旧版有但当前未实现、以及不应放进设置页的运行时动作；同步更新 `docs/CodeMap.md`。
- Verification: `rg -n "^## " docs/SettingsInventory.md` PASS；`rg -n "SettingsInventory\\.md" docs/CodeMap.md` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 后续重做设置页时，应先看 `docs/SettingsInventory.md`，避免把“当前能直接加 UI 的项”和“需要先补后端能力的项”混为一谈。
- Knowledge Route: local（直接写入 `docs/SettingsInventory.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限 docs / inventory 沉淀，不覆盖任何生产代码实现。
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（trivial+scribe-only，无需 spawn_agent）
- Warmup Verification: trivial 文档沉淀任务，`Required Roles = scribe`，按规则采用 `single-agent-fallback`；未扩大到多 Agent 或业务代码实现。
- Risk & Rollback: 主要风险是把旧版遗留项误当成当前真源；文档里已显式分层。如果需要回退，直接撤销本任务新增/修改的 docs 文件。
- Retention Review: 这份设置清单值得长期保留，它会直接服务下一轮设置页 IA 和 UI 重构。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 无代码/行为变更，未更新 CHANGELOG
- [x] 结案汇报
