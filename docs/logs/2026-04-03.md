## TID-20260403-repo-cleanup-foundation · 仓库精简第一轮：剔除非核心外围资产

- Task-ID: TID-20260403-repo-cleanup-foundation
- Goal: 为后续二次开发先做一轮仓库瘦身，剔除明显不属于产品主体的外围资产。
- Requirement Brief: 先删除 Docker/Snap 辅助壳、调试页和本地生成缓存，不触碰核心应用代码和当前主构建路径。
- Assumptions: 当前阶段更需要“保核心、降噪”，而不是立即按某个平台重写分发链；Linux/Windows 分发资产是否继续保留要等技术路线决策后再处理。
- Source Basis: `AGENTS.md`、`package.json`、`Dockerfile`、`docker-compose.yml`、`publish-snap.md`、`danger-color-tester.html`、`app/process-renderer.js`、`docs/CodeMap.md`、`docs/RepositoryGuidelines.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=repo hygiene only；touches_running_service=no；backup_required=no；backup_plan=以 git diff 和 VCS 为边界；rollback_plan=恢复本任务删除的 tracked 文件并按需重新生成 `coverage/`；destructive_operations=删除非核心仓库文件与本地生成目录；operator_approval_required=no；rationale=用户已明确要求删除不必要资产，且本轮不触碰产品主体代码。
- Change Summary: 删除 `Dockerfile`、`docker-compose.yml`、`publish-snap.md`、`danger-color-tester.html`，清理本地 `coverage/` 与 `scripts/__pycache__/`，并同步 `docs/CodeMap.md`、`docs/RepositoryGuidelines.md` 与 `docs/CHANGELOG.md`。
- Verification: `git diff -- Dockerfile docker-compose.yml publish-snap.md danger-color-tester.html docs/CodeMap.md docs/RepositoryGuidelines.md docs/CHANGELOG.md` 确认删除边界；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已明确“无争议外围资产先删、平台分发资产后删”的两阶段清理策略。
- Knowledge Route: local（docs/specs/plans/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮仓库精简，范围限定为非核心外围资产与 docs 同步。
- Risk & Rollback: 主要风险是提前删除后续仍可能参考的平台分发资产；本轮已刻意避开该区域。若需回退，可从 VCS 恢复四个 tracked 文件，并重新生成 `coverage/`。
- Retention Review: 删除 Docker/Snap 辅助壳、调试页和生成缓存有助于降低仓库噪音，值得保留；平台裁剪应在技术路线明确后再做。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-tauri-migration-foundation · Tauri 2 迁移基础骨架

- Task-ID: TID-20260403-tauri-migration-foundation
- Goal: 以 Tauri 2 建立新的跨平台桌面基础壳，并把旧 Electron 主体降级为迁移参考。
- Requirement Brief: 新建 `apps/desktop`，先落地宿主层、主 dashboard 与浏览器 preview；旧 `app/**` 保持不动，后续分阶段迁移领域逻辑。
- Assumptions: 用户已确认跨平台方向并接受 Tauri 2；当前更需要一个可继续演进的宿主骨架，而不是再优化一轮 Electron UI。
- Source Basis: `AGENTS.md`、`package.json`、`app/main.js`、`app/breaksPlanner.js`、`app/utils/naturalBreaksManager.js`、`app/utils/dndManager.js`、`app/utils/appExclusionsManager.js`、`docs/CodeMap.md`、`docs/RepositoryGuidelines.md`、`docs/Architecture.md`、`docs/UI.md`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/src/lib.rs`、`apps/desktop/src-tauri/src/commands.rs`、`apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src-tauri/src/state.rs`
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=新增平行 Tauri 壳层、不替换 Electron；touches_running_service=no；backup_required=no；backup_plan=保留旧壳作为参考并依赖 VCS 回退；rollback_plan=删除 `apps/desktop/**` 与根 desktop 脚本并回退 docs；destructive_operations=删除 Tauri 脚手架残留文件；operator_approval_required=no；rationale=用户已明确批准跨平台迁移到 Tauri 2，本轮不碰原有主运行时。
- Change Summary: 新建 `apps/desktop` Tauri 2 桌面基础壳，接入 Rust host、tray/global shortcut/notification/autostart、主 dashboard 与 preview fallback；更新根 `package.json` desktop scripts、`docs/CodeMap.md`、`docs/RepositoryGuidelines.md`、`docs/Architecture.md`、`docs/UI.md`、`docs/CHANGELOG.md`，并清理 Tauri 脚手架残留。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run tauri:dev` 启动到 `Running target/debug/pauza-desktop`；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已明确新一代 Pauza 桌面端应先迁宿主层，再按领域模块迁 planner / idle / DND / exclusions；同时保留浏览器 preview 作为设计检查入口。
- Knowledge Route: local（docs/specs/plans/logs/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri 基础迁移与验证。
- Risk & Rollback: 主要风险是双壳并存期间的结构复杂度，以及 preview 与真实原生壳之间的行为差异；若要回退，可删除 `apps/desktop/**`、回退根脚本入口与本次 docs 更新。
- Retention Review: Tauri 基础壳、preview fallback 和迁移 docs 明显值得长期保留；本轮产生的构建产物和脚手架残留不值得保留，应清理或继续忽略。
- Retention Decision: cleanup-partial

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-tauri-usable-core · Tauri 可用核心闭环

- Task-ID: TID-20260403-tauri-usable-core
- Goal: 让 `apps/desktop` 从迁移展示层变成可用的久坐干预桌面端核心。
- Requirement Brief: 迁入最小可用调度与系统信号层，把前台收敛成极简设置页和 break prompt，同时保持浏览器 preview 不崩溃。
- Assumptions: 用户接受先交付最小可用闭环而不是 feature parity；跨平台信号检测短期允许用 `sysinfo + 系统命令` 方案。
- Source Basis: `AGENTS.md`、`app/breaksPlanner.js`、`app/utils/naturalBreaksManager.js`、`app/utils/dndManager.js`、`app/utils/appExclusionsManager.js`、`app/utils/defaultSettings.js`、`apps/desktop/src-tauri/src/lib.rs`、`apps/desktop/src-tauri/src/commands.rs`、`apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`docs/specs/TID-20260403-tauri-migration-foundation/*`
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=为 Tauri 端新增可运行的久坐干预核心闭环、但不替换 Electron 正式入口；touches_running_service=no；backup_required=no；backup_plan=保留旧 Electron 与第一阶段 foundation 作为 source basis；rollback_plan=回退 `apps/desktop/**` 与 docs；destructive_operations=移除原展示型 dashboard 逻辑；operator_approval_required=no；rationale=用户已明确要求继续推进到“能用为止”。
- Change Summary: 新增 `platform.rs` 和 `engine.rs`，把设置、调度、pause/focus、自然休息、DND、app exclusions、tray/shortcut、break window 全部接入 Tauri host；将 `App.tsx` / `styles.css` 重写成极简设置页和 break prompt；调整主窗口尺寸；补第二阶段 specs/evidence，并同步 CodeMap/Architecture/UI/RepositoryGuidelines/CHANGELOG。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`npm run desktop:dev` 启动到 `Running target/debug/pauza-desktop`；Playwright 生成 `docs/specs/TID-20260403-tauri-usable-core/evidence/settings-snapshot.md`、`break-snapshot.md` 与 `browser-console.log`；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已明确 Tauri 端的最小产品骨架应该是“host engine + 极简设置页 + break prompt”，而不是“宿主壳 + 展示型 dashboard”。
- Knowledge Route: local（docs/specs/plans/logs/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri 可用核心闭环实现、验证与文档收口。
- Risk & Rollback: 风险主要在跨平台信号探测精度与 break prompt 窗口细节；若需回退，可恢复 `apps/desktop/**` 到上一阶段 foundation 状态并回退相关 docs。
- Retention Review: Rust host 的 engine/platform/state/shell 结构、极简设置页与 break prompt 都值得长期保留；浏览器结构快照和 console log 也值得保留为阶段证据。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-tauri-i18n-resource-layer · Tauri 多语言资源化

- Task-ID: TID-20260403-tauri-i18n-resource-layer
- Goal: 将 Tauri 端的多语言能力从临时内联文案重构为共享 locale 资源层，并恢复默认中文。
- Requirement Brief: 参考旧 Electron 的 `i18next + locales/*.json` 组织方式，为 Tauri 前后端建立统一 locale JSON、lookup 与插值层；保留极简设置页，只恢复一个最小语言切换入口。
- Assumptions: 当前只覆盖 `zh-CN / en` 两种语言；旧 Electron 的完整 locale 资产继续保留，但 Tauri 端先维护独立的精简文案集。
- Source Basis: `AGENTS.md`、`app/main.js`、`app/locales/*.json`、`apps/desktop/src/App.tsx`、`apps/desktop/src/i18n.ts`、`apps/desktop/src/locales/*.json`、`apps/desktop/src-tauri/src/{i18n,state,shell,commands}.rs`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅重构 Tauri 端文案管理与默认语言，不改调度语义；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS 回退；rollback_plan=恢复 locale 资源文件与调用点；destructive_operations=替换临时内联文案方案；operator_approval_required=no；rationale=用户明确要求用更优雅的 i18n 方式管理多语言。
- Change Summary: 新增 `apps/desktop/src/locales/{zh-CN,en}.json`、`apps/desktop/src/i18n.ts` 与 `apps/desktop/src-tauri/src/i18n.rs`，让前后端共用 key 化文案；设置页恢复语言选择并默认中文；tray、break window、运行时状态与 break 文案全部改为走 locale 资源。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: 已明确 Tauri 端多语言不能继续靠组件内联对象或 Rust if/else 分支维护，应统一走 locale 资源 + lookup 层。
- Knowledge Route: local（docs/specs/plans/logs/ui/codemap）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri i18n 资源化重构。
- Risk & Rollback: 风险主要是 key 漏配或前后端文案不一致；若需回退，可恢复本轮 locale/i18n 文件与调用点改动。
- Retention Review: locale 资源与共享 i18n 层值得长期保留；之前的组件内联文案方案不值得保留，已被替换。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-tauri-core-parity · Tauri 核心功能补齐并废弃 Electron 入口

- Task-ID: TID-20260403-tauri-core-parity
- Goal: 将 Tauri 从“可用核心闭环”推进到足以承担默认启动入口的版本，并让 Electron 退为 legacy 参考实现。
- Requirement Brief: 继续把旧 Electron 的关键能力迁到 Rust host，同时把根目录默认开发/构建脚本切换到 Tauri；前台不再保留展示页，只保留极简设置页与 break prompt。
- Assumptions: 旧 Electron 代码主体暂时保留；Tauri 当前只需接住核心久坐干预闭环与默认入口职责，不要求一步到位清掉全部分发遗留；相邻任务的 evidence 可作为旁证但不能替代本任务全部交互证明。
- Source Basis: `package.json`、`app/main.js`、`app/breaksPlanner.js`、`app/utils/{defaultSettings,naturalBreaksManager,dndManager,appExclusionsManager,displayManager}.js`、`apps/desktop/src-tauri/src/{lib,commands,engine,platform,shell,state}.rs`、`apps/desktop/src/{App.tsx,styles.css}`、`docs/specs/TID-20260403-tauri-migration-foundation/*`、`docs/specs/TID-20260403-tauri-usable-core/*`、`docs/specs/TID-20260403-tauri-i18n-resource-layer/*`
- Level: complex
- Lane: deep
- Execution Profile: sequential-phases
- Execution Safety Block: service_impact=切换默认开发/构建入口到 Tauri，并在 Tauri host 补齐核心运行时能力；touches_running_service=no；backup_required=no；backup_plan=保留旧 Electron 源码与 legacy scripts；rollback_plan=恢复根脚本到 Electron 并回退 `apps/desktop/**` 与 docs；destructive_operations=废弃默认 Electron 入口与展示型前台；operator_approval_required=no；rationale=用户已明确要求迁移到 Tauri 并把核心功能迁全。
- Change Summary: 扩展 `state.rs` 的设置模型与运行时状态，加入 pre-break notification、per-kind postpone/strict/manual finish、all screens/target screen/current time、tray strict visibility 与更多 shortcuts；扩展 `commands.rs` 暴露 skip/reset 等命令；重写 `shell.rs` 的 tray/shortcut/break window 策略；根 `package.json` 默认脚本切换为 `desktop:dev` / `desktop:build`，Electron 改为 `legacy:*`；前台 `App.tsx` 收敛为简洁设置页与独立 break prompt。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`state.rs` 3 个测试通过，覆盖 notification/manual finish/postpone limit）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；根 `package.json` 已确认 `start/dev/build/pack/dist/postinstall` 默认指向 `desktop:*`；本任务 evidence 报告已落盘到 `docs/specs/TID-20260403-tauri-core-parity/evidence/README.md`，但仍缺独立 tray / strict / manual-awaiting 可视化截图或录屏。
- Knowledge Capture: 已明确“默认入口切换”应基于 host parity 而不是先删 Electron；也明确“简单设置保持简单，高级能力折叠到 Advanced”比展示型工作台更适合此类工具。
- Knowledge Route: local（docs/specs/plans/logs/changelog/architecture/ui）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当时 developer policy 未获用户显式授权，不得启动子 agent）
- Warmup Verification: BLOCKED: 本任务实际执行时未满足多 agent 显式授权条件，因此由单 agent 兜底完成；该历史事实已保留，不做追溯性改写。
- Risk & Rollback: 风险主要在 tray/strict/manual-awaiting 等桌面行为尚缺新鲜可视化证据，以及 Electron/Tauri 双构建链并存造成的认知成本；如需回退，可恢复根 `package.json` 默认脚本到 Electron，并回退 `apps/desktop/**` 与对应 docs。
- Retention Review: `PauzaState` 驱动的 host 架构、根脚本默认切到 Tauri、极简设置页 + break prompt 的前台模型都值得长期保留；旧展示页式前台不应恢复。
- Retention Decision: keep

### Incremental Doc Note
- 本轮根据用户反馈，进一步把 Tauri 设置页的文档表述从“控制台/展示页”收敛为“更像现代 macOS 小工具的偏好页”。
- 默认可见内容明确只剩 `节奏 / 信号 / 通用`，其余能力保留在折叠的高级项中。
- 视觉语言明确为安静的 grouped settings、segmented control、switch、setting-card，以及更简洁的 break prompt。
- 本次只是文档增量收口，不代表新增代码变更或新增视觉证据。

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-tauri-core-parity-docs · 占位

- Task-ID: TID-20260403-tauri-core-parity-docs
- Goal: 只修改文档，把 `TID-20260403-tauri-core-parity` 的 specs/logs/plans 和总览文档补齐到可审计状态。
- Requirement Brief: 基于当前仓库已有代码事实补全文档，不回退他人修改，不触碰生产代码；如证据缺失则如实记录。
- Assumptions: 当前代码事实以仓库现状为准；旁证可以引用相邻任务 evidence，但不能冒充本任务新鲜截图。
- Source Basis: `package.json`、`apps/desktop/src-tauri/src/{state,shell,commands,engine,lib,platform}.rs`、`apps/desktop/src/{App.tsx,styles.css}`、`docs/specs/TID-20260403-tauri-core-parity/*`、相邻 Tauri 迁移 specs、当天 logs/plans、`docs/CHANGELOG.md`、`docs/CodeMap.md`、`docs/Architecture.md`、`docs/UI.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=docs only；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS；rollback_plan=回退本次文档修改；destructive_operations=none；operator_approval_required=no；rationale=只更新文档与门禁资产。
- Change Summary: 补全 `TID-20260403-tauri-core-parity` 的 `README.md`、`plan.md`、`testplan.md`、`arch.md`、`ui.md`、`evidence/README.md`，并同步更新当天 `logs/plans`、`docs/CHANGELOG.md`、`docs/CodeMap.md`、`docs/Architecture.md`、`docs/UI.md`；同时把 `TID-20260403-tauri-core-parity-docs` 自身从占位模板收口到有效 docs-only task。
- Verification: `python3 scripts/validate_role_file_scope.py --role scribe --paths ...` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` 结果待本轮收口后复跑确认；当前已知主要失败项来自本 docs task 旧占位，已在本轮修正。
- Knowledge Capture: 已明确 docs-only 收口任务也必须补完整 warmup receipt 和 execution mode，不能因为是 trivial 就留占位。
- Knowledge Route: local（docs/specs/plans/logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback
- Warmup Verification: PASS: trivial docs-only task，按规则允许 `single-agent-fallback`，无需额外子 agent；范围限定为本次 docs 收口。
- Risk & Rollback: 风险在于把代码事实写错或把缺失证据误记为已完成；如需回退，直接回退本次 docs 修改。
- Retention Review: core parity 的 spec/evidence/总览文档都应长期保留；占位模板不应继续保留在已执行任务上。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-hig-tailwind-redesign · Tauri 前台改版：Tailwind + Shadcn + Apple HIG

- Task-ID: TID-20260403-hig-tailwind-redesign
- Goal: 用 Tailwind CSS v4 + shadcn 风格组件重做 Tauri 前台，让设置页和 break prompt 更符合 Apple HIG 的 grouped settings 体验。
- Requirement Brief: 只改 `apps/desktop` 的 React/Tailwind 视觉层与组件层，不改 Rust host 命令契约；主窗口改为 overview hero + grouped settings + save rail，break prompt 改为环形倒计时单卡片。
- Assumptions: 用户请求即授权新增前端依赖；`DesktopSnapshot` / `PauzaSettings` 结构保持不变；break prompt 证据允许通过 mocked runtime 采集。
- Source Basis: `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/Architecture.md`、`apps/desktop/src/{App.tsx,styles.css,i18n.ts,locales/*}`、`apps/desktop/{package.json,vite.config.ts,tsconfig.json}`、`apps/desktop/src-tauri/tauri.conf.json`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅 Tauri 前台视觉/交互层；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + build/screenshot 旁证；rollback_plan=回退 `apps/desktop/**` 本轮前台改版与 docs；destructive_operations=替换原 React 前台样式与组件结构、不触碰 Rust host；operator_approval_required=no；rationale=用户明确要求采用 Tailwind + Shadcn 重设计 UI 且无运行中服务/数据风险。
- Change Summary: 新增 `components.json`、`src/components/ui/*`、`src/lib/utils.ts`，将 `App.tsx` 与 `styles.css` 重写为 Tailwind/shadcn + Apple HIG 风格；设置页改为 overview hero、grouped settings、advanced accordion、save rail，break prompt 改为环形倒计时单卡片；同步更新中英文 locale、evidence、UI/CodeMap/RepositoryGuidelines/Architecture/CHANGELOG。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 生成 `docs/specs/TID-20260403-hig-tailwind-redesign/evidence/settings-redesign.png`、`settings-snapshot.md`、`break-redesign.png`、`break-snapshot.md`、`browser-console-errors.log`；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；`python3 scripts/validate_agent_configs.py` SKIP（仓库当前无 multi-agent config）。
- Knowledge Capture: 已明确 Tauri 前台长期演进应直接落在 Tailwind v4 + shadcn primitives 上，而不是继续扩张手写 CSS 语义层；同时记录了 break prompt mocked runtime 证据采集方法。
- Knowledge Route: local（docs/specs/plans/logs/ui/codemap）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri 前台 React/CSS、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 Tauri 前台重设计、验证与文档收口。
- Risk & Rollback: 主要风险是新前端依赖增加维护成本，以及 browser preview 的 mocked break evidence 与真实 Tauri runtime 存在轻微差异；如需回退，可回退 `apps/desktop/**` 本轮改动并恢复旧 `App.tsx` / `styles.css`。
- Retention Review: Tailwind/shadcn 基座、overview hero、grouped settings、save rail 与 break prompt 环形倒计时都值得长期保留；临时 mocked runtime 仅保留为 evidence 方法说明，不进入生产路径。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-macos-tray-menu-crash · 修复 macOS 托盘右键菜单闪退

- Task-ID: TID-20260403-macos-tray-menu-crash
- Goal: 修复 `apps/desktop` 在 macOS 上顶部菜单栏 tray icon 右键后一闪即逝/闪退的问题，同时保持现有 tray actions 和左键 reveal 行为不变。
- Requirement Brief: 仅修复 Tauri Rust host 的 tray context menu 与后台刷新策略，不改 Electron legacy tray、前台 React UI 或调度状态机。
- Assumptions: 用户看到的“闪退”更可能是已展开 tray 菜单被后台刷新立刻替换；现有 menu action ids、本地化 key 和 tray refresh 语义继续复用。
- Source Basis: `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/{Architecture,UI}.md`、`apps/desktop/src-tauri/src/{shell,engine,state}.rs`、`apps/desktop/src-tauri/tauri.conf.json`、`muda/src/menu.rs`、`tauri/src/tray/mod.rs`、`tauri/src/menu/submenu.rs`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅 macOS 本地 tray 右键菜单；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + dev runtime；rollback_plan=回退 `shell.rs` 的 macOS `Submenu` root 分支与 docs；destructive_operations=none；operator_approval_required=no；rationale=本轮仅修复桌面壳崩溃点，无数据或外部副作用。
- Change Summary: 在 `apps/desktop/src-tauri/src/shell.rs` 新增 `TrayMenuContainer`、共享 `populate_tray_menu`、平台分支的 macOS `Submenu<R>` 根菜单，以及基于 `TrayRefreshKey` 的 `refresh_tray_if_needed()`；同时将 `apps/desktop/src-tauri/src/engine.rs` 从秒级无条件 `refresh_tray()` 改为按内容变化刷新，避免已展开的原生菜单被后台 tick 立即替换；同步补齐 task spec、evidence、Architecture/UI 与 changelog。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run tauri dev` 已启动真实 runtime 并运行 `target/debug/pauza-desktop`；`pgrep -fl 'target/debug/pauza-desktop|node .*tauri dev|vite --host 127.0.0.1 --port 43179'` 命中 `3481/3775/3870`；源码审查确认 `engine.rs` 已改为 `refresh_tray_if_needed()`；`osascript -e 'tell application "System Events" to tell process "pauza-desktop" to get count of menu bars'` 因缺少 assistive access 返回 `-25211`，因此最终右键验证待用户手工完成。
- Knowledge Capture: 已明确 macOS 下 Tauri/muda tray context menu 顶层必须使用 `Submenu`；更关键的是，tray 运行时不能在后台秒级无条件 `set_menu()`，否则已展开的原生菜单会一闪即逝。
- Knowledge Route: local（specs/logs/plans/Architecture/UI）
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成 tray bug 修复、验证与文档收口。
- Risk & Rollback: 主要风险是当前回合无法通过 UI scripting 自动右键 tray，因此最终现实确认仍依赖用户本机手工点击；如需回退，可回退 `apps/desktop/src-tauri/src/shell.rs` 本轮 macOS tray menu 分支与相关 docs。
- Retention Review: `shell.rs` 的 macOS `Submenu` root 分支应长期保留，它直接匹配底层 `muda` 平台约束；自动化受阻说明也值得留档，方便后续 native tray 任务预先考虑辅助功能权限。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [ ] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [ ] 结案汇报

## TID-20260403-settings-sidebar-layout · Tauri 设置页重构为侧边栏分类布局

- Task-ID: TID-20260403-settings-sidebar-layout
- Goal: 将 `apps/desktop` 的主设置页改为左侧分类导航 + 中间分类详情 + 右侧保存栏的单页结构，让设置内容更清楚，同时保留当前已有设置边界。
- Requirement Brief: 用户要求设置页能在一个页面里把设置讲清楚，侧边栏负责切换类别；此前开源底座已有的设置思路先保留，不自行新增设置项。
- Assumptions: “保留已有设置”以当前 Tauri `PauzaSettings` 字段和旧 `app/preferences.html` 的分类模式为边界；本轮不恢复 Rust host 尚未支持的旧底座主题/关于/赞助扩展设置。
- Source Basis: `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src/locales/{zh-CN,en}.json`、`apps/desktop/src-tauri/src/state.rs`、`app/preferences.html`、`app/utils/defaultSettings.js`、`docs/specs/TID-20260403-hig-tailwind-redesign/{plan,ui}.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅调整 Tauri 主设置页的信息架构和前台文案，不修改 settings persistence、调度逻辑或新增依赖；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS 回退并以 build/UI 自检为旁证；rollback_plan=回退 `apps/desktop/src/{App.tsx,locales/*}` 与本任务 docs；destructive_operations=替换当前 grouped settings + accordion 布局为 sidebar category 布局；operator_approval_required=no；rationale=本轮属于前台布局重排，无运行中服务、数据删除或外部副作用风险。
- AC:
  - AC1: 主设置页改为明确的侧边栏分类导航，用户可在单页内切换类别查看设置。
  - AC2: 当前 Tauri `PauzaSettings` 已暴露的设置项全部保留，不擅自新增新的设置能力。
  - AC3: 保存、撤销、默认值与 dirty/synced handoff 在新布局下依旧清晰。
  - AC4: `typecheck/build` 通过，并完成一轮浏览器 UI 自检。
- Change Summary: 重写 `apps/desktop/src/App.tsx` 的主设置页布局，新增 `overview / schedule / notifications / surface / smartPause / general` 六类 sidebar 导航，把原先 overview hero + grouped settings + advanced accordion 改为分类卡片；同步补充中英文文案、更新 `docs/UI.md` 与 `docs/CHANGELOG.md`，并归档一张 desktop 宽度的侧边栏设置页截图到 `docs/specs/TID-20260403-settings-sidebar-layout/evidence/`。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 浏览器预览确认点击 `提醒与延后` 后主标题切换为对应分类；检查 `保存` 按钮修改前 `disabled=true`、修改后 `disabled=false`；console error 计数为 0；截图留存于 `docs/specs/TID-20260403-settings-sidebar-layout/evidence/settings-sidebar-layout-desktop.png`。
- Knowledge Capture: Tauri 设置页下一步演进不该继续堆长页面或把大量字段塞进单个高级折叠项，更合适的模式是“左侧知道去哪找，中间知道在改什么，右侧知道是否已保存”的桌面偏好页结构。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri 前台 settings page、locale 文案、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮设置页重构、验证与文档收口。
- Risk & Rollback: 主要风险是分类重排时遗漏现有设置项或让窄屏布局失衡；本轮已通过字段逐项映射与浏览器自检控制风险。若需回退，可恢复 `App.tsx` 到此前 grouped settings 版本，并回退对应 locale/docs 更新。
- Retention Review: 侧边栏分类布局、右侧 save rail 与 evidence 截图都值得保留；它们直接记录了设置页从长页面分组走向更清晰偏好页 IA 的当前结论。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-settings-fixed-split · 设置页固定 1:3 分栏与 16:9 最小窗口

- Task-ID: TID-20260403-settings-fixed-split
- Goal: 让设置页回到稳定的桌面 split view，左侧 sidebar 占约四分之一，右侧主体占约四分之三，并将主窗口默认/最小尺寸调整到接近 `16:9`；同时去掉过度圆角和废话式介绍。
- Requirement Brief: 用户先指出当前 Pauza 设置页“非常别扭”，随后又明确补充“你好喜欢圆角设计和一些废话的介绍”；本轮因此同时修正 `App.tsx` 全局布局、desktop 前台 primitives 与 `tauri.conf.json` 的窗口边界。
- Assumptions: 用户认可现有分类模型与控件集合，问题核心在于全局三栏、过小窗口、过度圆角和冗长介绍，而不是字段组织本身；提高最小窗口尺寸是可接受的桌面取舍。
- Source Basis: `AGENTS.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/plans/2026-04-03.md`、`docs/logs/2026-04-03.md`、`docs/specs/TID-20260403-settings-sidebar-layout/{plan,ui}.md`、`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src/components/ui/{card,button,input,select,segmented-control}.tsx`、`apps/desktop/src-tauri/tauri.conf.json`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅主设置页前台布局、desktop primitives 和主窗口尺寸调整；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS + build/screenshot 旁证；rollback_plan=回退 `App.tsx`、desktop primitives、`tauri.conf.json` 与本任务 docs；destructive_operations=替换现有三栏设置页、旧最小窗口尺寸和一批高圆角视觉基元；operator_approval_required=no；rationale=纯本地 UI layout/config 改动，无数据/权限/外部副作用风险。
- Change Summary: 将 `apps/desktop/src/App.tsx` 的设置页外层从 `sidebar + content + save rail` 三栏改为稳定的双栏 split view，左列使用 `minmax(280px,1fr)`、右列使用 `minmax(0,3fr)`；保存/状态区并回主体顶部，保留保存、撤销、默认值动作；概览 hero 回收为普通标题型头部，sidebar / section / save dock 里的冗长介绍文案被压缩；同时收紧 `Card/Button/Input/Select/SegmentedControl` 与设置页局部组件的圆角；`apps/desktop/src-tauri/tauri.conf.json` 的主窗口改为 `1440x810` 默认、`1280x720` 最小。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 产出 `docs/specs/TID-20260403-settings-fixed-split/evidence/settings-fixed-split-1440x810-final.png`、`settings-fixed-snapshot-1440x810.md` 与 `browser-console.log`，旁证双栏 split view 与视觉收敛已生效；`python3 scripts/validate_workflow_docs.py --mode manual` PASS；`python3 scripts/validate_agent_configs.py` SKIP（仓库当前无 multi-agent config）。
- Knowledge Capture: 已明确桌面偏好页的“全局比例失衡 + 风格过软”不应继续通过追加第三栏或局部卡片修补，而应由外层 split shell、主窗口最小尺寸和前台 primitives 一起兜底。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: `apps/desktop/src/App.tsx`、desktop primitives、`apps/desktop/src-tauri/tauri.conf.json`、docs/evidence 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮设置页比例修正、圆角/文案收紧、窗口尺寸调整、验证与文档收口。
- Risk & Rollback: 主要风险是提高最小窗口尺寸后，主窗口不再允许压到旧尺寸；另一风险是前台风格一起收紧后，个别页面可能显得比之前硬。若需回退，可恢复旧三栏 `App.tsx`、旧 `tauri.conf.json` 尺寸配置和旧的 desktop primitives 样式，并删除本任务 evidence/docs。
- Retention Review: 双栏 split view、更大的最小窗口和更克制的前台组件都值得长期保留；旧三栏全局 save rail 和高圆角风格不值得恢复。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-settings-minimal-ui · 设置页极简重做：仅保留核心真设置

- Task-ID: TID-20260403-settings-minimal-ui
- Goal: 让 `apps/desktop` 设置页回到极简桌面工具式结构，只保留真正需要设置的内容，并彻底摆脱“很多卡片 + 很多废话”的状态。
- Requirement Brief: 用户明确确认设置页只保留九类核心真设置，因此本轮直接重写 `App.tsx` 的设置 IA，只保留 `节奏 / 提醒与打断 / 智能暂停 / 通用` 四个分类入口和单一主面板；运行时动作不再留在设置页。
- Assumptions: 低频高级能力短期允许隐藏而不是继续塞进主设置页；当前 Tauri host 契约已经足够支撑这一轮前端推翻重做；主窗口 `1440x810` / `1280x720` 边界继续沿用上一轮 fixed split 结论。
- Source Basis: `AGENTS.md`、`docs/UI.md`、`docs/CHANGELOG.md`、`docs/specs/TID-20260403-settings-fixed-split/*`、`apps/desktop/src/{App.tsx,styles.css,i18n.ts,locales/{zh-CN,en}.json}`、`apps/desktop/src/components/ui/{card,badge,button,input,select,segmented-control}.tsx`、`apps/desktop/src-tauri/src/{state.rs,commands.rs}`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅改 Tauri 前端设置页、locale 与 docs，不改 Rust host 契约；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、typecheck/build 与截图旁证；rollback_plan=回退前端文件与本任务 docs；destructive_operations=替换当前设置页结构与视觉；operator_approval_required=no；rationale=用户已明确要求前端重写，且本轮不涉及数据/外部副作用。
- Change Summary: 重写 `apps/desktop/src/App.tsx`，移除 overview、quick actions、advanced shortcut editor 和三段式卡片结构；设置页收敛为左侧导航区 + 右侧唯一主面板，只保留 `节奏 / 提醒与打断 / 智能暂停 / 通用` 四个分类，分类内只呈现核心真设置；补齐 `ui.interruptions` locale；收紧 `styles.css` 与 `components/ui/card.tsx` 的背景、边框和圆角语言，让页面从毛玻璃卡片感回到更克制的工具面板。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 产出 `docs/specs/TID-20260403-settings-minimal-ui/evidence/settings-minimal-1440x810.png`、`settings-minimal-snapshot-1440x810.md` 与 `browser-console.log`，证明 `1440x810` 下页面已收敛为极简 split view；`python3 scripts/validate_workflow_docs.py --mode manual` 待本轮 docs 收口后复跑。
- Knowledge Capture: 已明确“真设置优先级”应该直接支配前端 IA，而不是先做一套大而全界面再慢慢删；对 Pauza 这类工具，设置页最佳结构是少分类、单主面板、运行时动作外置。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: `apps/desktop` 前端 settings IA、视觉收敛、locale、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮前端极简重做、验证与文档收口。
- Risk & Rollback: 主要风险是低频设置暂时不可见，以及 `App.tsx` 全量改写时遗漏旧路径；本轮通过保留完整 `form` round-trip、typecheck/build 和真实截图控制风险。若需回退，可恢复 fixed split 版本的 `App.tsx`、`styles.css`、`card.tsx` 与 locale/docs。
- Retention Review: 新的四分类极简 IA、暖米色背景 + 白色主面板的工具式视觉、以及这次 1440x810 的真实截图都值得保留；旧的 overview/quick action/多卡片组合不值得恢复。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-break-surface-mode-restore · 恢复休息窗口/全屏设置

- Task-ID: TID-20260403-break-surface-mode-restore
- Goal: 把 Tauri 端丢失掉的“窗口 / 全屏”休息显示模式恢复回来，并重新接到设置页与宿主窗口行为。
- Requirement Brief: 用户明确指出休息时的“窗口 / 全屏”功能丢失；本轮因此恢复旧 Electron 中 `fullscreen` 这根设置轴，并让它重新影响 Tauri break window profile。
- Assumptions: 用户要恢复的是旧 `showBreaksIn: window/fullscreen` 语义；`breakPromptStyle` 和 `fullscreen` 需要并存；短期不恢复 `show regular windows` 等更细 surface 选项。
- Source Basis: `app/preferences.html`、`app/utils/defaultSettings.js`、`app/utils/breakWindowProfile.js`、`app/main.js`、`apps/desktop/src-tauri/src/{state.rs,shell.rs,commands.rs}`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/UI.md`、`docs/Architecture.md`、相邻 specs/logs
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅恢复 break surface mode 的设置真源、宿主行为与前端入口；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、cargo/typecheck/build 与 DOM snapshot 旁证；rollback_plan=回退相关 Rust/TS 文件与本任务 docs；destructive_operations=替换当前仅靠 breakPromptStyle 的 surface mode 逻辑；operator_approval_required=no；rationale=用户已明确要求恢复功能，且本轮无数据/外部副作用风险。
- Change Summary: 在 `apps/desktop/src-tauri/src/state.rs` 恢复 `fullscreen: bool` 到 `PauzaSettings`；`apps/desktop/src-tauri/src/shell.rs` 的 break window profile 重新接入这根设置轴，`fullscreen=true` 时 break 铺满目标显示器，`fullscreen=false` 时保持窗口模式，且 immersive long break 不再强制全屏；`apps/desktop/src/App.tsx` 在“提醒与打断”分类重新加入 `显示方式：窗口 / 全屏` segmented control；补齐中英文 locale。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 产出 `docs/specs/TID-20260403-break-surface-mode-restore/evidence/settings-break-surface-snapshot-1440x810.md` 与 `browser-console.log`，快照中可见 `提醒与打断 > 休息窗口 > 显示方式`；`python3 scripts/validate_workflow_docs.py --mode manual` 待本轮 docs 收口后复跑。
- Knowledge Capture: 已明确 break surface mode 不能只靠 `breakPromptStyle` 代替；“风格”与“窗口/全屏”在用户心智里是两组不同控制。
- Knowledge Route: local（docs/specs/plans/logs/UI/Architecture/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: Tauri settings model、break window profile、前端入口、evidence 与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 break surface mode 恢复、验证与文档收口。
- Risk & Rollback: 主要风险是 fullscreen 模式会让 microbreak 也变得更强，以及 immersive 窗口尺寸后续可能还要调；若需回退，可恢复 `state.rs`、`shell.rs`、`App.tsx`、locale 与 docs 到本轮前状态。
- Retention Review: 恢复 `fullscreen` 设置轴和值得保留的 break surface mode 文档都应长期保留；“只留 breakPromptStyle、丢掉窗口/全屏”的状态不应延续。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260403-break-window-scale-fix · 修复休息窗口过小与双层卡片

- Task-ID: TID-20260403-break-window-scale-fix
- Goal: 修掉当前 break window“窗口很小、里面还套一张小卡片”的观感问题。
- Requirement Brief: 用户贴图指出微休息窗口极不合理；本轮因此重写 `BreakWindow` 布局，让内容直接填满宿主 break window，并把 `shell.rs` 的 window-mode 默认尺寸整体拉大。
- Assumptions: 用户不只是嫌尺寸小，也嫌当前 break 页层级像“窗口里再嵌套小窗”；浏览器 preview 需要 mock current break 才能稳定看 break UI。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/shell.rs`、`docs/specs/TID-20260403-hig-tailwind-redesign/evidence/break-snapshot.md`、`docs/specs/TID-20260403-break-surface-mode-restore/*`、相邻 UI/logs 文档
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅改 break 页 React 结构和 Tauri host window-mode 默认尺寸；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、cargo/typecheck/build 和 preview snapshot；rollback_plan=回退 `App.tsx`、`shell.rs` 与本任务 docs；destructive_operations=替换现有双层容器结构和默认尺寸；operator_approval_required=no；rationale=用户明确要求修正 break window 观感，本轮无数据/外部副作用风险。
- Change Summary: `apps/desktop/src/App.tsx` 的 `BreakWindow` 去掉了内层 `Card` 和固定 `max-w-[460px]` 宽度，改为整页 break shell 直接填满宿主窗口；同时在浏览器 preview 的 `?window=break` 路径下构造模拟中的微休息，方便直看 break UI；`apps/desktop/src-tauri/src/shell.rs` 将 gentle / balanced / immersive 三档 window-mode 默认尺寸整体拉大，避免 break 还是像个小弹窗。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；Playwright 产出 `docs/specs/TID-20260403-break-window-scale-fix/evidence/break-window-preview-snapshot.md`，可见 break 页当前已直接渲染 badge、倒计时、标题和三个按钮，不再是套卡片结构；`python3 scripts/validate_workflow_docs.py --mode manual` 待本轮 docs 收口后复跑。
- Knowledge Capture: 已明确 break prompt 这种独立窗口不该再沿用“页面里放卡片”的设置页语言；窗口本身就是 surface，页面内容应直接吃满。
- Knowledge Route: local（docs/specs/plans/logs/UI/changelog）
- Execution Mode: single-agent-fallback
- Fallback Scope: break 页前台结构、host 默认尺寸、preview 证据与 docs 同步
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（developer policy 限制：未获用户显式授权不得 spawn_agent）
- Warmup Verification: BLOCKED: 当前 developer policy 明确限制未获用户显式授权时启用子 agent；因此由单 agent 兜底完成本轮 break window 尺度修复、验证与文档收口。
- Risk & Rollback: 主要风险是 gentle 模式现在会比之前存在感更强，以及极小屏幕上仍可能需要继续微调；若需回退，可恢复 `App.tsx`、`shell.rs` 与 docs。
- Retention Review: 整页 break shell、preview break mock 和更大的 window-mode 默认尺寸都值得保留；原来的 break 内层窄卡片不值得恢复。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
