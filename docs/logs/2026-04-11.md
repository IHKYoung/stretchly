## TID-20260411-site-landing-page · 搭建单页官网与下载入口

- Task-ID: TID-20260411-site-landing-page
- Goal: 为 Pauza 搭建一个现代、高级、简洁的单页官网，在一屏内讲清它是什么、它能做什么，并提供下载按钮与独立的下载跳转路由。
- Requirement Brief: 用户要求直接尝试做一个官网，页面要现代、有高级感、文案简明扼要，并在同一页承载产品介绍、能力说明和下载按钮；下载跳转需单独拆成一个文件夹管理。
- Assumptions: 当前官网优先承担品牌说明和下载入口职责，不需要真实部署和 API；真实下载源后续再填入配置文件。
- Source Basis: `package.json`、`README.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/CHANGELOG.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限新增官网静态目录、下载跳转页、根脚本与 docs 口径；touches_running_service=no；backup_required=no；backup_plan=依赖本地静态服务器、浏览器现实检查、截图证据与 workflow docs validator；rollback_plan=删除 `apps/site` 与相关 docs / scripts 接线后恢复；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、数据迁移、提权或外部付费动作。
- Change Summary: 已新增 `apps/site` 纯静态官网目录，实现单页 landing page、独立下载稳定路由、集中下载目标配置文件、站点 `favicon` 与根级 `site:dev` 预览命令；同时同步更新 `README.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/CHANGELOG.md`、`docs/UI.md`、`docs/Architecture.md` 与本任务 spec/evidence。
- Verification:
  - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json OK')"` -> PASS（`package.json OK`）
  - `npm run site:dev` -> PASS（本地静态服务器启动在 `http://127.0.0.1:43210/`）
  - 浏览器现实检查 -> PASS
    - 首页 `http://127.0.0.1:43210/`：控制台 `0 error / 0 warning`
    - `/download/macos-apple-silicon/`、`/download/macos-intel/`、`/download/releases/`：均可打开；当前 `targets.js` 留空时只出现预期 `console.warn`
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在官网静态实现、下载跳转页、文档更新与本地现实检查
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次官网设计、实现、验证与文档闭环。
- Risk & Rollback: 主要风险是页面视觉过满或下载跳转未做好 fallback；若方案不满意，可直接回退 `apps/site` 与相关 docs / scripts 接线，不影响桌面端。
- Retention Review: 本次官网原型、稳定下载路由和 `site:dev` 入口都值得长期保留；“首页 CTA 不直连真实下载文件，而是统一经过站内稳定路由”的规则也应继续作为网站维护约束保留。未发现需要在结案前清理的临时文件或一次性开关。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-site-typewriter-redesign · 官网重设计为极简打字机单页

- Task-ID: TID-20260411-site-typewriter-redesign
- Goal: 将 Pauza 官网首页重构成纯白纸面背景上的极简打字机单页，中央循环展示 App 内嵌提醒文案，右上角只保留一个下载按钮。
- Requirement Brief: 用户提出新的官网 idea：背景必须极简纯白，像草稿纸/网格纸；中央只保留类似命令行打字机的动态文案展示区，展示 App 内嵌的调侃式提醒文案；下载按钮简化为单一入口，后续再接 GitHub 链接。
- Assumptions: 首页最好的表达方式不是信息堆叠，而是直接让用户感受到 Pauza 的语气；“落霞孤鹜”风格可通过仓库已有的 `LXGW WenKai Screen` 字体落地。
- Source Basis: `apps/site/{index.html,styles.css,script.js,download/targets.js}`、`apps/desktop/src/locales/messages/zh-CN.json`、`apps/desktop/src/styles.css`、`docs/specs/TID-20260411-site-landing-page/*`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/site` 首页重构、字体/文案资源新增与 docs 同步；touches_running_service=no；backup_required=no；backup_plan=通过本地静态服务器、浏览器现实检查、截图证据与 workflow docs validator 验证；rollback_plan=回退 `apps/site` 首页相关文件与本任务 docs 后恢复到上一版站点；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、数据迁移、权限提升或外部付费动作。
- Change Summary: 已将官网首页重构为纯白纸面背景上的极简打字机单页：新增 `apps/site/copy.js` 作为首页循环文案真源、复制 `LXGW WenKai Screen` 到 `apps/site/fonts/`、重写 `apps/site/{index.html,styles.css,script.js}`，并保留右上角单下载按钮继续连到既有稳定下载路由。
- Verification:
  - `npm run site:dev` -> PASS（本地静态服务器启动在 `http://127.0.0.1:43210/`）
  - 浏览器现实检查 -> PASS
    - 首页桌面端：控制台 `0 error / 0 warning`
    - 首页移动端：`scrollHeight == innerHeight`
    - 打字机循环：7 秒内从 `水杯空了？你的身体可不是仙人掌。` 切换到 `检查一下你的坐姿：背挺直了吗？别像一只虾一样蜷着。`
    - `/download/releases/`：可打开；当前 `targets.js` 留空时只出现预期 `console.warn`
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在官网首页重设计、字体资产复制、docs 更新与本地现实检查
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次官网重设计、实现、验证与文档闭环。
- Risk & Rollback: 主要风险是页面过度极简后信息量不足，或移动端长句换行失控；若方向不满意，可直接回退首页相关文件到上一版，不影响下载路由与桌面端。
- Retention Review: 本次官网重设计、首页文案真源 `copy.js` 和本地字体资产都值得长期保留；“官网首页直接复用 App 内嵌提醒文案”和“下载按钮继续通过站内稳定路由”应继续作为站点维护约束保留。未发现需要在结案前清理的临时功能开关。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-icon-alt-candidate · 透明化网页图标并导出桌面端备选图标

- Task-ID: TID-20260411-icon-alt-candidate
- Goal: 把网页端当前 `favicon.svg` 这版图标改成透明底，并导出到桌面端 `icons` 目录作为不接线的候选图标。
- Requirement Brief: 用户希望把网页端现有 `favicon.svg` 这版图标抽出来当作桌面端的另一个候选方案；要求去掉背景、保持透明，并把开口方向再向左转一些，使其朝向西北 45 度。
- Assumptions: 当前需求是保存“备选图标素材”，不是替换正式打包图标；只要候选文件落在 `icons` 目录且不接线即可。
- Source Basis: `apps/site/favicon.svg`、`apps/desktop/src-tauri/icons/*`、`docs/CodeMap.md`、`docs/CHANGELOG.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限图标素材文件更新与桌面端 icons 目录新增候选资产；touches_running_service=no；backup_required=no；backup_plan=通过文件 diff、图像尺寸/透明通道检查与 workflow docs validator 验证；rollback_plan=回退 `apps/site/favicon.svg` 与新增候选图标文件；destructive_operations=none；operator_approval_required=no；rationale=不改当前正式打包图标链路，不涉及运行中服务、数据、权限或外部付费动作。
- Change Summary: 已将 `apps/site/favicon.svg` 改为透明底，并把开口从原先角度转向西北 45 度附近；同时在 `apps/desktop/src-tauri/icons/` 下新增 `icon-alt-nw45.svg` 与透明 PNG `icon-alt-nw45.png` 作为桌面端候选图标素材，不影响当前正式打包图标。
- Verification:
  - `sips -g pixelWidth -g pixelHeight -g hasAlpha apps/desktop/src-tauri/icons/icon-alt-nw45.png` -> PASS（`pixelWidth: 1024`、`pixelHeight: 1024`、`hasAlpha: yes`）
  - `magick identify -format '%f %wx%h %[channels]\n' apps/desktop/src-tauri/icons/icon-alt-nw45.png` -> PASS（`icon-alt-nw45.png 1024x1024 srgba 4.0`）
  - 本地目视检查 -> PASS（透明底、暖灰到蓝的渐变保留，开口稳定朝左上）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `apps/site/favicon.svg`、`apps/desktop/src-tauri/icons/icon-alt-nw45.{svg,png}` 与相关文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次图标素材调整、导出与文档闭环。
- Risk & Rollback: 主要风险是开口角度不符合预期或 PNG alpha 丢失；若不满意，直接回退 `favicon.svg` 并删除 `icon-alt-nw45.*` 即可，不影响现有正式图标链路。
- Retention Review: 这版透明底候选图标值得保留在 `icons` 目录下，方便后续和正式图标做并排评估；“候选素材不直接覆盖正式打包图标”这一做法也值得继续保留。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-root-scripts-cleanup · 整理根 package.json 重复脚本

- Task-ID: TID-20260411-root-scripts-cleanup
- Goal: 整理根 `package.json` 的重复/残留 scripts，让 repo 级命令入口只保留一套短别名和一套显式命名空间。
- Requirement Brief: 用户指出根 `package.json` 的 scripts 重复太多，希望判断是否合理并做整理；本次只收敛 repo 级脚本、CI 脚本名和当前有效文档，不改 `apps/desktop/package.json` 子应用脚本。
- Assumptions: 根级 scripts 应承担 repo convenience layer，而不是继续兼容一整套历史同义命令；当前有效自动化只需同步 `tests.yml` 中的 coverage 命令名。
- Source Basis: `package.json`、`.github/workflows/tests.yml`、`README.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限根 npm scripts、CI 命令名与文档命令说明；touches_running_service=no；backup_required=no；backup_plan=通过 package.json parse、脚本 grep、`npm test`、`npm run typecheck` 与 workflow docs validator 验证；rollback_plan=回退 `package.json`、`.github/workflows/tests.yml` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=不涉及线上服务、数据迁移、权限或外部付费动作。
- Change Summary: 已收敛根 `package.json` 的 repo 级脚本：删除 `start/pack/dist/test-single/prepublishOnly/postpublish`，新增 `typecheck`，并把测试命令统一命名为 `test` / `test:coverage` / `test:watch`；同时同步更新 `README.md`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/CHANGELOG.md` 与 `.github/workflows/tests.yml`。
- Verification:
  - `node -e "const pkg=require('./package.json'); console.log(Object.keys(pkg.scripts).join('\n'))"` -> PASS（输出仅保留 `dev/build/typecheck`、`desktop:*`、`site:dev`、`test*`、`lint`、`postinstall`、`prepare`）
  - `rg -n '"(start|pack|dist|test-single|prepublishOnly|postpublish)"' package.json` -> PASS（无命中）
  - `rg -n 'npm run dev|npm run build|npm run typecheck|npm run test:coverage|npm run desktop:build' README.md docs/RepositoryGuidelines.md docs/CodeMap.md .github/workflows/tests.yml` -> PASS
  - `npm test` -> PASS（`4 passed files / 59 passed tests`）
  - `npm run typecheck` -> PASS（`tsc --noEmit` 无错误输出）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在根 `package.json`、`.github/workflows/tests.yml` 与当前有效文档的命令口径收敛
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次根脚本整理、验证与文档闭环。
- Risk & Rollback: 主要风险是本地若仍习惯使用 `npm start`、`npm run pack`、`npm run dist` 会遇到 missing script；若需要恢复，只需回退根 `package.json`、`.github/workflows/tests.yml` 与文档中的对应命令名。
- Retention Review: “根级只保留一套短入口和一套显式命名空间”值得长期保留，也应继续作为 repo 级脚本维护约束；本次未引入临时开关或一次性脚本。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-jump-to-next-break-freeze · 修复顶部菜单跳到下一次休息导致卡死

- Task-ID: TID-20260411-jump-to-next-break-freeze
- Goal: 修复当前顶部菜单选择“跳到下一次休息”后应用卡死的问题，优先收敛 tray 宿主刷新时机。
- Requirement Brief: 用户反馈当前在顶部菜单选择“跳到下一次休息”时应用会直接卡死；本任务聚焦 tray 菜单动作后的宿主刷新时机，不改 break 调度语义、settings schema 或前台文案。
- Assumptions: 根因更接近 tray 菜单点击后仍同步 `refresh_tray()`，该路径内部 `set_menu()` 与主线程阻塞桥接在 macOS 上触发宿主卡死/假死，而不是 `skip_to_*` 的状态机死循环。
- Source Basis: `apps/desktop/src-tauri/src/{shell,state,engine,commands}.rs`、本地 Tauri 2 `tray/mod.rs` 与 `menu/{mod,normal}.rs`、`docs/specs/TID-20260403-macos-tray-menu-crash/*`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 tray 菜单动作后的宿主刷新时机与对应 docs；touches_running_service=no；backup_required=no；backup_plan=依赖 Rust/前端构建、workflow docs validator 与 tray 生命周期源码审查；rollback_plan=回退 `apps/desktop/src-tauri/src/shell.rs` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地桌面端宿主修复，不涉及数据、权限、外部副作用或线上服务。
- Change Summary: `apps/desktop/src-tauri/src/shell.rs` 现为 tray 菜单点击引入 `TRAY_MENU_REFRESH_DELAY_MS = 150`，`on_menu_event` 不再在当前点击路径里立刻 `refresh_tray()`；改为先完成 `handle_tray_action()`，再等待 native 菜单关闭后调用 `refresh_tray_if_needed()`。同时让 `handle_tray_action()` 返回是否需要刷新菜单，`open` / `hide` / `quit` 会跳过无意义刷新，`skip/pause/focus/reset/toggle-autostart` 等仍会在延后后刷新。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`23 passed; 0 failed`）；`npm --prefix apps/desktop run build` PASS（`✓ 1825 modules transformed.`、`✓ built in 2.24s`，保留既有 chunk size warning）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-11 ✅`）；`cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all` 未执行（当前 toolchain 缺少 `cargo-fmt` / `rustfmt` 组件）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 tray 宿主刷新链路修复、构建验证与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,scribe,architect,coder,tester,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation 授权，遵循上层工具策略不调用 `spawn_agent`；采用 single-agent-fallback 在限定范围内完成 tray 宿主修复、验证与文档闭环。
- Risk & Rollback: 主要风险是根因若落在 break window 打开路径而非 tray menu rebuild，本轮只能部分缓解；另外 `150ms` 延迟若在个别机器上仍不足，可能还需要继续外扩到更保守的 deferred window lifecycle。若验证不符，优先回退 `shell.rs` 的刷新时机调整，再继续沿 `show_break_window()` 排查。
- Retention Review: `tray 菜单点击路径不要立即整棵 set_menu()` 这条宿主约束值得长期保留；它和先前“后台 1s tick 不要无条件 refresh_tray”属于同一类 native menu 生命周期纪律。本次未引入临时文件、开关或一次性脚本。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-break-copy-line-layout · 优化休息文案按句分行显示

- Task-ID: TID-20260411-break-copy-line-layout
- Goal: 优化 break prompt 中央文案的显示方式，让它按整句优先阅读，避免从任意字位截断。
- Requirement Brief: 用户指出 break 提示文案现在会被从中间截断，容易造成误解；本任务聚焦 break prompt 的文案分行与排版，不改宿主调度、文案真源内容或 CTA 语义。
- Assumptions: 当前最刺眼的问题来自自动段落换行，而不是文案本身太长；中文应优先按句号/逗号等标点换行，英文至少保持一句一行。
- Source Basis: `apps/desktop/src/{App.tsx,styles.css,i18n.ts}`、`apps/desktop/src/lib/break-ideas.ts`、`docs/UI.md`、`docs/CodeMap.md`、`docs/specs/TID-20260408-break-prompt-purity-pass/plan.md`、用户提供的 break prompt 截图
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/desktop` break prompt 文案分行 helper、前台排版与对应测试/docs；touches_running_service=no；backup_required=no；backup_plan=依赖 `npm test`、desktop build、browser preview 截图与 workflow docs validator；rollback_plan=回退 `apps/desktop/src/{App.tsx,lib/break-copy-layout.ts}`、`test/desktopBreakCopyLayout.js` 与本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=纯前台 UI 排版修复，不涉及数据、权限、外部副作用或线上服务。
- Change Summary: 新增 `apps/desktop/src/lib/break-copy-layout.ts` 作为 break prompt 文案分行 helper，让文案遵循“整句优先，超长句按分句换行”；`App.tsx` 现把 `title/body` 渲染为 line list，并在标题达到 4 行以上时自动收紧一档字号；同时把文案区最大宽度从 `600px` 提到 `720px` 上限，减少被动折行。新增 `test/desktopBreakCopyLayout.js` 固定住中文长句、英文多句和英文长句分行规则，并通过 browser preview 截图验证实际 UI。
- Verification: `npm test` PASS（`Test Files 5 passed (5)`、`Tests 62 passed (62)`）；`npm --prefix apps/desktop run build` PASS（`✓ 1826 modules transformed.`、`✓ built in 1.77s`，保留既有 chunk size warning）；browser preview PASS（`http://127.0.0.1:43179/?window=break`，截图 `docs/specs/TID-20260411-break-copy-line-layout/evidence/break-copy-preview.png` 显示中央文案已按整句/分句独立成行）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-11 ✅`）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 break prompt 排版 helper、前台渲染、preview 取证与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,scribe,ui_designer,architect,coder,tester
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation 授权，遵循上层工具策略不调用 `spawn_agent`；采用 single-agent-fallback 在限定范围内完成 break prompt 排版优化、测试、preview 取证与文档闭环。
- Risk & Rollback: 主要风险是分行阈值仍可能需要继续按真实截图微调；若后续发现某些 prompt 被切得过碎，可直接回退 `break-copy-layout.ts` 的阈值与 JSX 渲染逻辑，再重新跑测试和 preview 检查。
- Retention Review: `break prompt 文案整句优先、超长句按分句换行` 这条前台排版规则值得长期保留，也适合作为未来其它“无空格语言提示语”界面的复用范式。本次未引入临时开关或一次性脚本。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-site-interaction-refresh · 官网打字机停留与交互增强

- Task-ID: TID-20260411-site-interaction-refresh
- Goal: 优化官网首页的打字机节奏、舞台居中布局和用户交互手感，让页面在保持极简的同时更有回应感。
- Requirement Brief: 用户要求继续优化 `apps/site` 首页：打字机文案打完后停留 10 秒、主内容居中且舞台宽度约占屏幕 80%、增加鼠标/网页操作带来的粒子和点击互动，并补充一组更调侃的“别久坐”短句。
- Assumptions: 用户要的是“极简但不无聊”的首页，不是信息量更大的 landing page；交互应保持轻量克制，不能把纸面感做脏。
- Source Basis: `apps/site/{index.html,styles.css,script.js,copy.js}`、`apps/desktop/src/locales/messages/zh-CN.json`、`docs/specs/TID-20260411-site-{landing-page,typewriter-redesign}/*`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/site` 首页布局、交互脚本、文案池与相关 docs/evidence；touches_running_service=no；backup_required=no；backup_plan=依赖本地静态服务器、浏览器现实检查、截图证据、`npm test` 与 workflow docs validator；rollback_plan=回退 `apps/site/{index.html,styles.css,script.js,copy.js}` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=纯静态站点前端优化，不涉及线上服务、权限、外部付费或破坏性操作。
- Change Summary: 已完成 `apps/site` 首页交互增强：`script.js` 把打字机单条文案 hold 时长提升到 `10000ms`，并新增 pointer aura / trail、滚动 sweep、点击 burst 与点击短句气泡；`styles.css` 将主舞台收敛为更明确的居中布局，桌面端宽度约占视口 `80%`，同时补齐交互层、气泡与 reduced-motion 样式；`index.html` 增加交互层挂点、聚焦能力和底部提示；`copy.js` 扩充长文案池并新增 `PAUZA_SITE_NUDGES` 短句池。
- Verification: `npm test` PASS（`Test Files 5 passed (5)`、`Tests 62 passed (62)`）；`node --check apps/site/script.js` PASS；`node --check apps/site/copy.js` PASS；Playwright 现实检查 PASS（桌面端 `innerWidth=1470`、`stageWidth=1176`、`stageCenter=viewportCenter=735`，确认主舞台为视口 `80%` 且水平居中；首条文案完整显示后 `5.5s` 仍保持原句、`10.7s` 后进入切换，确认 `10s hold` 生效；点击后 DOM 中出现 `nudge-bubble=1`、`particle=17`、`ripple=1`，示例短句为“起来两分钟，别把屁股焊在椅子上。”；移动端 `scrollHeight=innerHeight=844`，维持单屏）；浏览器 console PASS（`Total messages: 0 (Errors: 0, Warnings: 0)`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-11 ✅`）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `apps/site/{index.html,styles.css,script.js,copy.js}`、首页现实检查与相关文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次官网交互增强、验证与文档闭环。
- Risk & Rollback: 主要风险是交互增强过多破坏当前的纯净感，或粒子/气泡在小屏下显得拥挤；若结果不满意，优先回退互动层逻辑，只保留 10 秒 hold 和居中布局。
- Retention Review: 本轮新增的“长文案打字机 + 点击短句提醒”双层文案池、轻量交互层和 reduced-motion 兜底都值得长期保留；这些规则能继续作为官网首页的风格边界。未引入一次性脚本、调试开关或需要清理的临时资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-site-terminal-shell-refine · 官网去卡片化并改为终端式提示头

- Task-ID: TID-20260411-site-terminal-shell-refine
- Goal: 去掉官网首页中央舞台的卡片壳，并把 `Pauza>` 放到左上角，让页面更像纸面上的终端输出。
- Requirement Brief: 用户要求去掉首页背景卡片，并把 `Pauza>` 放到左上角，模拟终端输出感；现有打字机、下载按钮和互动层保持不变。
- Assumptions: 用户要的是更像终端的结构秩序，而不是更复杂的视觉层次；去卡片化后纸面背景本身已经足够承担氛围。
- Source Basis: `apps/site/{index.html,styles.css,README.md}`、`docs/{UI,CodeMap}.md`、`docs/specs/TID-20260411-site-interaction-refresh/*`、`docs/{logs,plans}/2026-04-11.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/site` 首页结构样式与相关 docs/evidence；touches_running_service=no；backup_required=no；backup_plan=依赖本地静态服务器、浏览器现实检查、截图证据与 workflow docs validator；rollback_plan=回退 `apps/site/{index.html,styles.css}`、相关 docs 与 evidence；destructive_operations=none；operator_approval_required=no；rationale=纯静态站点视觉结构微调，不涉及线上服务、权限、数据或外部副作用。
- Change Summary: `apps/site/index.html` 现把舞台拆成“左上 prompt + 中央 output”两层；`Pauza>` 作为单独的终端提示头放到左上角。`apps/site/styles.css` 移除了舞台卡片的边框、底色、阴影和高光层，只保留纸面背景与交互层，并让主文案继续保持中央居中。
- Verification: `node --check apps/site/script.js` PASS；Playwright 现实检查 PASS（桌面端 `stageBorderWidth=0px`、`stageBackground=rgba(0, 0, 0, 0)`、`stageBoxShadow=none`，确认卡片壳已移除；`promptText=Pauza>` 且 `promptLeftOffset=0`，确认提示头位于左上角；`outputCenter=719.996`、`viewportCenter=720`，主文案保持居中；移动端 `scrollHeight=innerHeight=844`，仍为单屏）；浏览器 console PASS（`Total messages: 0 (Errors: 0, Warnings: 0)`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（结案时复跑）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `apps/site/{index.html,styles.css}`、首页现实检查与相关文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次首页结构微调、验证与文档闭环。
- Risk & Rollback: 主要风险是去卡片后页面可能显得过空，或左上角 prompt 在小屏下压缩主文案空间；若方向不对，优先回退 `index.html` 与 `styles.css` 到上一个 card stage 版本。
- Retention Review: “无卡片纸面 + 左上终端 prompt + 中央输出区” 这套层级值得保留，适合作为官网首页长期风格边界。本次未引入临时脚本、开关或一次性资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-site-symbol-particles · 官网强化终端提示头并改为符号粒子

- Task-ID: TID-20260411-site-symbol-particles
- Goal: 强化首页左上角 `Pauza>` 的终端存在感，把粒子反馈改成更像程序员/码字工作者常见的符号字元，并让主输出文案宽度按浏览器页面宽度 `80%` 收敛。
- Requirement Brief: 用户要求让 `Pauza>` 更明显，并把粒子改成更像程序员/码字工作者常见的 `0 / 1 / #` 等符号；随后又补充要求让网站输出文案宽度按浏览器页面宽度 `80%` 收敛。
- Assumptions: 用户要的是更强的终端感，而不是更复杂的视觉装饰；粒子仍应轻量，不应变成满屏特效。
- Source Basis: `apps/site/{index.html,styles.css,script.js,README.md}`、`docs/{UI,CodeMap}.md`、`docs/specs/TID-20260411-site-{interaction-refresh,terminal-shell-refine}/*`、`docs/{logs,plans}/2026-04-11.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/site` 首页 prompt 样式、输出区宽度、粒子字元与相关 docs/evidence；touches_running_service=no；backup_required=no；backup_plan=依赖本地静态服务器、DOM 度量、浏览器快照、console 摘要与 workflow docs validator；rollback_plan=回退 `apps/site/{styles.css,script.js}` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=纯静态站点前端样式/脚本微调，不涉及线上服务、权限、数据或外部副作用。
- Change Summary: `apps/site/styles.css` 继续强化左上角 `Pauza>`：提升了字号、字重和终端分隔线强度；同时让 `.typewriter-copy` 的宽度按 `80vw` 收敛。`apps/site/script.js` 现把粒子从光点改成带文本的符号粒子，并让符号池以 `0 / 1 / #` 为主，辅以少量 `> / / / { } / _` 维持终端感。
- Verification: `node --check apps/site/script.js` PASS；Playwright 现实检查 PASS（`.prompt-line` `font-size=18.432px`、`.prompt-mark` `font-weight=800`；`.typewriter-copy` `outputWidth=1152`、`viewportWidth=1440`、`outputWidthRatio=0.8`；点击后粒子采样包含 `# / 1 / _ / / / { / }` 等终端相关符号）；浏览器 console PASS（`Total messages: 0 (Errors: 0, Warnings: 0)`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（结案时复跑）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `apps/site/{styles.css,script.js}`、首页现实检查与相关文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 prompt / 粒子 / 输出宽度微调、验证与文档闭环。
- Risk & Rollback: 主要风险是 prompt 过重导致头重脚轻，或符号粒子池过杂削弱“代码感”；若方向不对，优先回退 `styles.css` 的 prompt / width 改动，再回退 `script.js` 的粒子字符池。
- Retention Review: “更醒目的左上 prompt + 80vw 输出区 + 以 `0 / 1 / #` 为主的字符粒子” 值得保留，适合作为官网终端感的下一层规则。本次未引入临时脚本、开关或一次性资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-site-font-unify-symbol-density · 官网统一 LXGW 字体并增强符号粒子密度

- Task-ID: TID-20260411-site-font-unify-symbol-density
- Goal: 让 `Pauza>` 和整个官网都统一使用 `LXGW WenKai Screen`，并把粒子收敛到更大、更密、更多 `0 / 1 / # / @ / ！ / ¥ / $` 这类符号。
- Requirement Brief: 用户要求让 `Pauza>` 也保持使用 `LXGW` 字体，并让整个网站保持一个统一字体；同时要求粒子里的 `0 / 1 / # / @ / ！ / ¥ / $` 更多、更大、更密。
- Assumptions: 用户要的是“字体完全统一”，不是保留终端 monospace 特例；粒子应更有代码感，但不能压过主文案。
- Source Basis: `apps/site/{styles.css,script.js,index.html,README.md}`、`docs/{UI,CodeMap}.md`、`docs/specs/TID-20260411-site-{symbol-particles,terminal-shell-refine}/*`、`docs/{logs,plans}/2026-04-11.md`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 `apps/site` 首页字体、粒子密度与相关 docs/evidence；touches_running_service=no；backup_required=no；backup_plan=依赖本地静态服务器、DOM 度量、浏览器快照、console 摘要与 workflow docs validator；rollback_plan=回退 `apps/site/{styles.css,script.js}` 与相关 docs；destructive_operations=none；operator_approval_required=no；rationale=纯静态站点前端样式/脚本微调，不涉及线上服务、权限、数据或外部副作用。
- Change Summary: `apps/site/styles.css` 现把 `body`、`Pauza>`、下载按钮、互动提示和粒子全部统一到 `LXGW WenKai Screen`；同时提升 prompt 强度但不再依赖另一套 monospace。`apps/site/script.js` 继续扩大粒子字号和数量，并把字符池收敛到 `0 / 1 / # / @ / ！ / ¥ / $` 为主，点击 burst 会稳定喷出这一整组符号。
- Verification: `node --check apps/site/script.js` PASS；Playwright 现实检查 PASS（`body / prompt / button / hint / particle` 的 `font-family` 全部为 `LXGW WenKai Screen`；`.typewriter-copy` `outputWidth=1152`、`viewportWidth=1440`、`outputWidthRatio=0.8`；点击后 `particleCount=23`，纯 burst 采样稳定出现 `0 / 1 / # / @ / ！ / ¥ / $` 全集）；浏览器 console PASS（`Total messages: 0 (Errors: 0, Warnings: 0)`）；`npm test` PASS（`Test Files 5 passed (5)`、`Tests 62 passed (62)`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（结案时复跑）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `apps/site/{styles.css,script.js}`、首页现实检查与相关文档闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次字体统一与粒子密度增强、验证与文档闭环。
- Risk & Rollback: 主要风险是全站单字体后 prompt 失去辨识度，或粒子数量增加后页面发脏；若方向不对，优先回退 `styles.css` 的字体统一，再回退 `script.js` 的粒子密度设置。
- Retention Review: “全站统一 `LXGW` + `Pauza>` 同字体 + 更高密度的 `0 / 1 / # / @ / ！ / ¥ / $` 粒子” 值得保留，适合作为官网终端感的下一层细化规则。本次未引入临时脚本、开关或一次性资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-break-activation-settings-input-fix · 修复全屏场景 break 不浮出与设置手输卡死

- Task-ID: TID-20260411-break-activation-settings-input-fix
- Goal: 修复 macOS 全屏工作区里 break prompt 不浮出，以及设置页数字输入手动编辑时的回弹/卡住问题。
- Requirement Brief: 用户报告两个当前可复现 bug：一是到点后 break prompt 在当前全屏页面里不出现、不切到提示界面；二是设置里手动输入数字时 App 会“卡死”。本任务限定在 `App.tsx` 数字输入交互和 `shell.rs` 宿主激活链路，不扩到调度状态机、locale 或 settings schema。
- Assumptions: 全屏 Space 漏浮出更接近 non-focusable break window 缺少显式 app activation；数字输入“卡死”更接近每击键立即 clamp 导致多位数/临时空值无法自然编辑，而不是 Rust 写盘死锁。
- Source Basis: `apps/desktop/src/{App.tsx,lib/settings-controls.ts}`、`test/desktopSettingsControls.js`、`apps/desktop/src-tauri/src/{shell,commands,state,engine}.rs`、本地 `tao-0.34.8` macOS focus 实现，以及 `TID-20260408-tauri-window-fullscreen-fix`、`TID-20260409-break-window-macos-fullscreen-coverage`、`TID-20260409-break-schedule-input-redesign`、`TID-20260410-settings-language-switch-freeze`、`TID-20260411-jump-to-next-break-freeze` 相关 specs/logs。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 desktop 前台数字输入与 macOS break 宿主激活链路；touches_running_service=no；backup_required=no；backup_plan=依赖前端/Rust 构建测试与 docs validator；rollback_plan=回退 `App.tsx`、`shell.rs` 与对应 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地桌面端交互修复。
- Change Summary: 已在 `apps/desktop/src/App.tsx` 将 `CompactNumber` 改为本地草稿编辑：允许临时空值与多位数输入，只在 `blur / Enter / 加减按钮` 时提交 clamp 后的值，并用 `Escape` 恢复当前值；同时在 `apps/desktop/src-tauri/src/shell.rs` 的 `show_break_window()` 中为 macOS non-focusable/windowed break 路径补上 `NSApplication.activateIgnoringOtherApps(true)`，让到点后的 break 更可靠地切到当前全屏 Space。相关证据已写入 `docs/specs/TID-20260411-break-activation-settings-input-fix/evidence/README.md`。
- Verification:
  - `npm test` -> PASS（`Test Files  5 passed (5)`、`Tests  62 passed (62)`）
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`test result: ok. 23 passed; 0 failed`）
  - `npm --prefix apps/desktop run typecheck` -> PASS
  - `npm --prefix apps/desktop run build` -> PASS（`✓ 1826 modules transformed.`、`✓ built in 1.74s`；保留既有 chunk size warning）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS（`[OK] Workflow docs validation passed for 2026-04-11 ✅`）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 `App.tsx` 数字输入交互、`shell.rs` macOS break 激活链路、验证与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层运行策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 spec、实现、验证与文档闭环。
- Risk & Rollback: 主要风险是 macOS 激活补丁可能让非 strict 的 break 更主动抢前台，或数字输入草稿与按钮点击出现 blur 先后顺序问题；若任一风险落地，可直接回退 `App.tsx` 与 `shell.rs` 本轮改动。
- Retention Review: 本轮修复沉淀出两条值得长期保留的边界：一是 macOS break 在 non-focusable/windowed 路径不能只 front window，还要显式激活 app，避免全屏 Space 漏浮出；二是设置页数字 stepper 应采用“本地草稿 -> 明确提交”而不是每击键直写 autosave。未引入临时开关、一次性脚本或需结案清理的试验资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-pauza-six-step-product-plan · 整理 Pauza 六步产品演进文档

- Task-ID: TID-20260411-pauza-six-step-product-plan
- Goal: 产出一份正式的 `docs/PauzaV1SixStepPlan.md`，把 Pauza 的六步产品演进路径写清楚，并同步仓库索引。
- Requirement Brief: 用户要求把此前关于 Pauza 定位、onboarding、主窗口、低打扰能力、反馈闭环和商业边界的讨论整理成一个正式文档，且每一步都要有详细修改内容。
- Assumptions: 当前需要的是长期稳定的产品执行蓝图，而不是新的代码实现；六步应按门槛推进而不是按周数描述。
- Source Basis: `docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/Architecture.md`、`apps/site/{index.html,copy.js}`、`apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/{state,platform}.rs`、`apps/desktop/src/locales/messages/zh-CN.json`
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅新增产品规划文档并同步 docs 索引；touches_running_service=no；backup_required=no；backup_plan=通过文档审查与 workflow docs validator 验证；rollback_plan=回退新文档、CodeMap 和本任务 docs；destructive_operations=none；operator_approval_required=no；rationale=纯 docs 任务，不改代码、依赖或运行时行为。
- Change Summary: 新增 `docs/PauzaV1SixStepPlan.md`，把 Pauza 后续演进整理为 6 个连续步骤，并为每一步补齐目标、修改内容、涉及模块和完成标志；同时完成本任务 spec 包、当日 plans/logs 与 `docs/CodeMap.md` 同步。
- Verification: `rg -n "^## Step [1-6]" docs/PauzaV1SixStepPlan.md` PASS（检测到 `Step 1` 到 `Step 6` 共 6 个章节）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-11 ✅`）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在产品规划文档、本任务 spec 与 docs 索引同步
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（trivial docs-only task）
- Warmup Verification: trivial 文档任务且 `Required Roles = scribe`，按仓库规则允许直接使用 `single-agent-fallback`，无需 warmup 子 agent。
- Risk & Rollback: 主要风险是文档如果不绑定当前真实模块，后续执行时仍会漂移；若内容方向不满意，可整体回退本任务 docs，不影响产品代码。
- Retention Review: 这份六步方案值得长期保留，后续定位、onboarding、主窗口、低打扰能力和商业实验任务都应以它为上位参考。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-release-011-archive-commit · 归档当前 0.1.1 工作树并提交

- Task-ID: TID-20260411-release-011-archive-commit
- Goal: 将当前所有未提交修改整体视为 `0.1.1` 版本线上的最新归档快照，补齐与当前真实文件树一致的文档真源，并完成一次详细的本地提交。
- Requirement Brief: 用户要求把当前 worktree 全量修改作为 `0.1.1` 版本归档并 commit；本任务不再提升版本号、不推送远端，只做本地 merge-gate 收口。
- Assumptions: 当前版本号真源已经统一为 `0.1.1`；用户要的是“一次完整归档提交”，不是继续拆小 commit；旧 Electron `app/` 目录的删除属于本次真实快照，应如实记录到文档真源和 changelog。
- Source Basis: `git status --short`、`git diff --stat`、`README.md`、`docs/{RepositoryGuidelines,CodeMap,Architecture,UI,CHANGELOG}.md`、`docs/plans/2026-04-11.md`、`docs/logs/2026-04-11.md`、`docs/commits/2026-04-11.md`、`docs/specs/TID-20260410-release-011-summary-commit/*`、`.githooks/{commit-msg,post-commit}`、`scripts/{validate_commit_message_task_id.py,commit_audit_lib.py}`。
- Level: moderate
- Lane: deep
- Execution Profile: merge-gate
- Execution Safety Block: service_impact=仅限本地版本归档、文档真源收口与 git commit；touches_running_service=no；backup_required=no；backup_plan=`git diff --check` + `npm test` + `npm run typecheck` + `npm --prefix apps/desktop run build` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=如需撤回，以本次 commit 为边界执行后续 revert；destructive_operations=git commit（用户已明确授权）；operator_approval_required=no；rationale=纯本地 merge-gate 收口，无外部副作用。
- Change Summary: 已补齐本次版本归档所缺的真源对齐：`README.md` 改为说明 `0.1.1` 当前已包含官网基线，下一阶段转向部署与下载链路；`docs/{RepositoryGuidelines,CodeMap,Architecture,UI}.md` 不再把已删除的旧 `app/` 目录写成现存模块；`docs/CHANGELOG.md` 新增旧壳移除、tray freeze 修复、break activation / 数字输入修复与六步产品文档记录；同时新增本任务 spec，并完成本地归档 commit。
- Verification:
  - `git diff --check` -> PASS
  - `node -p "require('./package.json').version"` -> PASS（`0.1.1`）
  - `node -p "require('./apps/desktop/package.json').version"` -> PASS（`0.1.1`）
  - `rg -n '^version = "0\\.1\\.1"$' apps/desktop/src-tauri/Cargo.toml` -> PASS
  - `rg -n '"version"\\s*:\\s*"0\\.1\\.1"' apps/desktop/src-tauri/tauri.conf.json` -> PASS
  - `rg -n '旧 Electron 壳已从当前工作树移除|仅维护两块内容|官网部署与下载链路' README.md docs/RepositoryGuidelines.md docs/CodeMap.md docs/Architecture.md docs/UI.md` -> PASS
  - `npm test` -> PASS
  - `npm run typecheck` -> PASS
  - `npm --prefix apps/desktop run build` -> PASS
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
  - `git commit` -> PASS（生成详细中文本地提交；post-commit hook 自动补记 `docs/commits/2026-04-11.md`）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在版本文档收口、本地验证与 git commit
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次版本归档、验证与提交。
- Risk & Rollback: 主要风险是文档口径若不先收口，会把已删除目录继续暴露成维护入口；其次，本次 commit 覆盖多个 task spec，commit message 若不满足 `TASK-ID-MULTIPLE` 会被 hook 阻断。若需回滚，以本次 commit 为边界执行后续 revert，不重写历史。
- Retention Review: 本次沉淀出的“版本归档提交前先对齐文档真源”和“多 task spec 提交必须显式走 `TASK-ID-MULTIPLE`”两条规则值得长期保留；未引入临时开关、一次性脚本或需结案清理的中间资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260411-settings-freeze-root-cause-fix · 修复设置点击与时间修改卡死

- Task-ID: TID-20260411-settings-freeze-root-cause-fix
- Goal: 修复设置页“点击设置项仍会卡死、改时间后仍会卡死”的问题，并把根因从单个输入框问题收敛到 autosave 并发 + host 全量刷新。
- Requirement Brief: 用户明确反馈设置页依然容易卡死，具体表现为点击设置项会卡死、调整时间后也会卡死；本任务聚焦 `App.tsx` autosave 调度与 Rust `update_settings` 的宿主刷新策略。
- Assumptions: 真正冻结来源不是某个时间输入框尚未处理完整，而是设置 autosave 会在前一轮保存未完成时继续排第二轮，Rust host 又会在每次设置更新时无差别做快捷键重绑和整棵 tray rebuild，导致宿主重操作堆叠。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/lib/settings-controls.ts`、`apps/desktop/src-tauri/src/{commands.rs,state.rs,shell.rs}`、`test/desktopSettingsControls.js`、`docs/specs/TID-20260409-break-schedule-input-redesign/plan.md`、`docs/specs/TID-20260410-settings-language-switch-freeze/plan.md`、`docs/specs/TID-20260411-break-activation-settings-input-fix/plan.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限桌面端设置 autosave 与 Rust host 设置应用链路；touches_running_service=no；backup_required=no；backup_plan=`npm test` + `npm run typecheck` + `npm --prefix apps/desktop run build` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `App.tsx`、`commands.rs`、`state.rs` 与对应 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地桌面端交互/宿主修复。
- Change Summary: `apps/desktop/src/App.tsx` 现将 autosave 改为串行/合并保存：通过 `formRevision`、`saveRetryToken`、`saveInFlightRef` 与 `pendingSaveRetryRef` 避免多个 `update_settings` 并发飞向宿主；`apps/desktop/src-tauri/src/state.rs` 现为 `PauzaSettings` 增加 `PartialEq/Eq` 并在 no-op 更新时短路；`apps/desktop/src-tauri/src/commands.rs` 新增 settings diff 逻辑，只在 shortcut 变化时重绑快捷键，只在语言变化时整棵重建 tray，其余设置仅走 `refresh_tray_if_needed()` 轻量刷新。
- Verification:
  - `npm test` -> PASS（`Test Files  5 passed (5)`、`Tests  62 passed (62)`）
  - `npm run typecheck` -> PASS
  - `npm --prefix apps/desktop run build` -> PASS（`✓ 1826 modules transformed.`、`✓ built in 2.14s`；保留既有 chunk size warning）
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`26 passed; 0 failed`，新增 `commands::tests::*` 3 条）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS（结案时复跑）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop autosave 链路、Rust host refresh diff、验证与 docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次根因定位、实现、验证与文档闭环。
- Risk & Rollback: 主要风险是 autosave 串行化若实现失误可能造成保存遗漏，或 tray rebuild 条件收得太窄导致某些菜单文案刷新不及时；若发生，可回退 `App.tsx` 与 Rust diff 逻辑并重新验证。
- Retention Review: 本轮沉淀出两条值得长期保留的规则：一是设置 autosave 必须串行/合并，不能让宿主保存并发；二是 `update_settings` 不能默认做快捷键重绑 + tray 整棵 rebuild 这类无差别全量刷新。未引入临时开关、一次性脚本或需结案清理的资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
