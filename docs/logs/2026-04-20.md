## TID-20260420-release-013-packaging-and-publish · 整理未提交改动并发布 0.1.3 macOS 版本

- Task-ID: TID-20260420-release-013-packaging-and-publish
- Goal: 将当前未提交的 desktop / site / docs 变更整理成 `0.1.3`，统一版本真源、补齐发布文档、打本地 macOS 包，并完成 GitHub release / commit 的发布动作。
- Requirement Brief: 用户要求把当前没有提交的部分做一次文档整理并总结，作为 `0.1.3` 版本，随后打包本地 macOS 并上传 GitHub release 和 commit；本任务据此只收口有效产品改动与发布资产，不把根目录翻译 scratch 文件混入正式版本。
- Assumptions: 代码 push 先按当前代码仓库远端执行；release 资产仍按现有站点脚本发布到 `IHKYoung/Pauza`；根目录翻译 scratch 文件属于生成过程残留，不应作为 `0.1.3` 的正式仓库资产。
- Source Basis: `git status --short`、`git diff --stat`、`README.md`、`docs/{CHANGELOG,Architecture,CodeMap,RepositoryGuidelines}.md`、`apps/site/{README.md,index.html,script.js,download/targets.js}`、`apps/site/docs/ReleasePlaybook.md`、`apps/site/scripts/publish_site_release.py`、`apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src/locales/messages/*.json`、`apps/desktop/src/locales/registry.generated.json`、`docs/specs/TID-20260413-fullscreen-break-current-space-fix/*`、`gh release list --repo IHKYoung/Pauza`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅影响本地桌面端打包产物、站点 pinned 下载链接、release 资产与 GitHub 代码推送；touches_running_service=no；backup_required=no；backup_plan=`npm test` + `npm run typecheck` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `npm --prefix apps/desktop run build` + `npm --prefix apps/desktop run tauri build -- --bundles dmg --no-sign` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退版本真源/文档/站点 pinned URL，删除或覆盖错误的 GitHub release 资产，并对错误 push 使用后续 `git revert`；destructive_operations=none；operator_approval_required=no；rationale=用户已明确要求发布 `0.1.3`，本轮不涉及历史重写、生产服务写入或提权操作。
- Change Summary: 已将当前未提交的 desktop / site / docs 工作树正式收口为 `0.1.3`：统一根/desktop/Tauri/Cargo 版本真源到 `0.1.3`，重写 root README 的版本概览与下一阶段说明，补充 `docs/CHANGELOG.md` 的 `0.1.3` 版本段，扩写 root `.gitignore` 以排除根目录翻译 scratch 文件；`apps/desktop` 侧纳入了 macOS fullscreen Space break 浮出修复、bundle 环境通知权限请求、50 个 locale 的 break 文案扩充和重建后的 `registry.generated.json`；`apps/site` 侧纳入了 workflow mirror、Release Playbook、一键发布脚本和 `v0.1.3` pinned 下载链接；本地已产出 `Pauza_0.1.3_aarch64.dmg`，GitHub `IHKYoung/Pauza` 上已发布 `v0.1.3` release，同时代码仓库已生成主提交 `6f57be202b1993ee633861c7e64b701e4c259e0b`。
- Verification:
  - `python3 scripts/ensure_workflow_ready.py --target . --hooks required` -> PASS（`workflow assets are ready`）
  - `python3 scripts/sync_desktop_locales.py` -> PASS（`50 languages`，registry 已重建）
  - `npm test` -> PASS（`5` 个测试文件，`61` 个测试）
  - `npm run typecheck` -> PASS
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`23 passed; 0 failed`）
  - `npm --prefix apps/desktop run build` -> PASS（`1826 modules transformed`；保留既有 chunk size warning）
  - `npm --prefix apps/desktop run tauri build -- --bundles dmg --no-sign` -> PASS（生成 `/Users/changkunyang/CKProjects/Pauza/apps/desktop/src-tauri/target/release/bundle/dmg/Pauza_0.1.3_aarch64.dmg`，约 `20M`）
  - `python3 scripts/validate_agent_configs.py` -> PASS（SKIP：无 multi-agent config）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
  - `git diff --check` -> PASS
  - `gh release view v0.1.3 --repo IHKYoung/Pauza` -> PASS（`draft: false`、资产 `Pauza_0.1.3_aarch64.dmg`、URL=`https://github.com/IHKYoung/Pauza/releases/tag/v0.1.3`）
- Knowledge Capture: 已沉淀两条后续可复用规则：一是 desktop release 前必须先重建 `registry.generated.json`，否则新增 locale 文案不会进入运行时；二是当前 Pauza 的“代码仓库”和“release 分发仓库”并非同一 GitHub repo，发布流程必须显式区分 commit/push 与 release/upload 两条路径。
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在当前工作树收口、版本整理、本地打包、GitHub release 与代码 push 的单 agent 执行
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 `0.1.3` 收口、打包、release 与提交闭环。
- Risk & Rollback: 主要风险仍是代码仓库与 release 仓库当前并非同一 GitHub repo，导致版本追踪需要双线处理；另一个风险是无签名 dmg 在分发时的系统信任提示。若后续发现 release 资产或 pinned URL 有误，将优先覆盖正确资产或修正站点链接；若代码远端 push 有误，将通过新的 `git revert` 提交回滚，而不是历史重写。
- Retention Review: 本轮值得长期保留的资产包括 `apps/site/scripts/publish_site_release.py`、Release Playbook、site workflow mirror、`0.1.3` 的发布文档，以及“根目录翻译 scratch 文件不应进入正式版本”的忽略规则。未引入新的临时调试开关或一次性脚本。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
