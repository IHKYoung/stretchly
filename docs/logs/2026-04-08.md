## TID-20260408-macos-icon-fidelity · 修复 macOS 顶部与 Dock 图标清晰度和尺寸

- Task-ID: TID-20260408-macos-icon-fidelity
- Goal: 修复 macOS 菜单栏 tray icon 发糊且偏大、Dock icon 偏大的问题，并让图标资源脚本可稳定重生成。
- Requirement Brief: 用户明确要求彻底解决两个 macOS 图标问题；本轮只改 Tauri macOS icon 接入、tray 小尺寸几何与生成脚本，不动产品功能、设置 schema 和 tray 菜单结构。
- Assumptions: 用户主要反馈来自 macOS Retina 环境；当前终端未授予 Screen Recording，故无法采集系统级菜单栏 / Dock 截图，只能以资源预览、源码依据和构建验证完成本轮闭环。
- Source Basis: `apps/desktop/src-tauri/src/shell.rs`、`graphics/generate_icon_assets.py`、`graphics/tray-icon.svg`、`graphics/app-icon.svg`、`docs/specs/TID-20260407-app-icon-refresh/*`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`tray-icon-0.21.3/src/platform_impl/macos/mod.rs`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 macOS 图标显示与本地资源生成链；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、重新生成资源和构建链结果为边界；rollback_plan=回退 `shell.rs`、`graphics/*` 与再生的 icon 产物；destructive_operations=替换 tray / dock 图标接入方式并刷新 icon 资源；operator_approval_required=no；rationale=不涉及数据、权限、外部副作用或提权。
- Change Summary: `shell.rs` 改为在 macOS 上直接拿到 Pauza 自己的 `NSStatusItem` 并注入 1x/2x multi-representation tray image，移除原先“扫描全部 status items + 延迟替换”的脆弱做法；Dock patch 进一步收紧为仅在未打包开发态生效，release `.app` 走系统 bundle icon；`graphics/tray-icon.svg` 恢复与主 icon 一致的西北朝向；`graphics/app-icon.svg` 新增 `64px` 安全边距以减轻 Dock 中的视觉体积；`graphics/generate_icon_assets.py` 修复了 ffmpeg 原地覆盖和 `ensure_srgb` 签名不一致导致的生成失败，并重新产出 Tauri icon 资源。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（最近一次 `Finished 'dev' profile [unoptimized + debuginfo] target(s) in 43.34s`）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`3 passed; 0 failed`）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.25s`）；`python3 graphics/generate_icon_assets.py` PASS（`generated icon assets`）；`npm --prefix apps/desktop run tauri:build` PASS（notarization `Accepted`，重新产出 `Pauza.app` 与 `Pauza_0.1.0_aarch64.dmg`）；系统级截图验证 BLOCKED（当前终端未获 macOS Screen Recording 权限）。
- Knowledge Capture: macOS 状态栏图标的“显示尺寸”和“像素密度”在 `tray-icon` crate 内被拆成了固定 `18pt` 逻辑高度与外部提供的位图表示；若想既不糊又不显得过大，必须同时处理 glyph padding 与原生 `NSStatusItem` 图像表示，而不是只换一张 PNG。
- Knowledge Route: local（沉淀到 `graphics/generate_icon_assets.py` 与本任务 specs / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 macOS tray / dock 图标修复、资源重生成、验证与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略仅在用户明确要求子 agent 时允许 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成定位、实现、验证与文档。
- Risk & Rollback: 主要剩余风险是未拿到 Screen Recording 权限前，无法用系统截图做最终肉眼核验；若用户本机仍觉得 Dock 视觉体积偏大，可继续微调 `graphics/app-icon.svg` 的 inset。回滚时直接撤销 `shell.rs`、`graphics/*` 和再生 icon 资源即可。
- Retention Review: 目标 tray patch 和图标脚本修复都值得长期保留。前者避免继续依赖“枚举所有 status items”的不稳定 hack，后者避免以后每次重生图标都卡在 ffmpeg / `ensure_srgb` 的脚本错误上。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

### 追加沉淀
- 已将本次 macOS 图标方案同步沉淀到长期文档：`docs/Architecture.md`（策略与官方依据）、`docs/RepositoryGuidelines.md`（操作约束）、`docs/CodeMap.md`（路径职责）。

### Follow-up 调整
- 根据用户提供的菜单栏截图复核后，确认 Pauza tray icon 在解决“发糊”和“偏大”后又收小过头，因此将 `graphics/tray-icon.svg` 的 glyph 以中心点为锚放大约 `8%`，保持西北朝向、template 语义和 1x/2x 资源链不变。
- Follow-up Verification: `python3 graphics/generate_icon_assets.py` PASS（`generated icon assets`），本地预览 `apps/desktop/src-tauri/icons/tray-iconTemplate@2x.png` 未出现贴边或方向偏移。

## TID-20260408-adaptive-reminder-state-machine · 设计并实现低打断久坐提醒状态机

- Task-ID: TID-20260408-adaptive-reminder-state-machine
- Goal: 把当前久坐提醒改成“先等空档、再投递”的低打断状态机，避免用户正在输入或深度专注时被固定时间打断。
- Requirement Brief: 用户明确指出固定时间提醒会在正在打字/输入/专注时打断工作；本轮聚焦于 Tauri host 的提醒投递状态机和状态文案，不重做 UI 布局、不新增权限、不回填 Electron legacy。
- Assumptions: `idle_ms` 足以作为“仍在持续输入/操作”的近似代理；现有 focus / DND / app exclusion / natural break 继续视作强阻塞态；hidden settings 默认值可覆盖本轮调优需要。
- Source Basis: `apps/desktop/src-tauri/src/{state,engine,shell}.rs`、`apps/desktop/src/App.tsx`、`app/breaksPlanner.js`、`app/utils/{naturalBreaksManager,defaultSettings}.js`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/{Architecture,UI,SettingsInventory,RepositoryGuidelines,CodeMap}.md`、当日 logs/plans
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地提醒投递时机与状态文案；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、Rust tests、cargo check 与 docs 为回滚边界；rollback_plan=回退 `state.rs` 与 locale 文案；destructive_operations=替换默认 break delivery 逻辑；operator_approval_required=no；rationale=不涉及数据、权限、外部副作用或新增依赖。
- Change Summary: 在 `apps/desktop/src-tauri/src/state.rs` 中为 `PauzaSettings` 增加 hidden adaptive delivery 阈值，并为 `RuntimeState` 增加 waiting/nudged 运行时字段；`tick()` 现支持 `Scheduled -> DueButProtected -> SoftNudge -> BreakRunning` 状态迁移，到点后若仍在持续输入则先等待空档，再择机投递；`apps/desktop/src/locales/{zh-CN,en}.json` 补齐等待空档、soft nudge 和自适应 heads-up 文案；同步更新了 task spec、evidence、`docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md` 与 `docs/CHANGELOG.md`。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`7 passed; 0 failed`）；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 1.07s`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08`）；`cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml` BLOCKED（当前 toolchain 未安装 `rustfmt` 组件）。
- Knowledge Capture: “提醒何时投递”需要与“节奏何时到点”分离；只有 due time 没有 delivery state，会天然打断用户当前输入。
- Knowledge Route: local（计划沉淀到本任务 specs、`docs/Architecture.md` 与 `docs/SettingsInventory.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 Tauri host 提醒状态机、测试与文档闭环。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档。
- Risk & Rollback: 主要风险是 idle 阈值不够精准，导致等待过长或仍有轻微打断；如验证不理想，直接回退 `state.rs` 和 locale 文案即可恢复旧逻辑。
- Retention Review: 本次状态机和 hidden adaptive settings 值得长期保留。它把“节奏到点”和“提醒投递时机”解耦，直接解决了用户正在输入时被固定时间打断的问题；状态文案与证据链也已同步，后续若要开放更细粒度策略，只需在现有 host 真源上继续暴露设置项。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260408-reminder-state-machine-doc · 整理提醒状态机与调度逻辑文档

- Task-ID: TID-20260408-reminder-state-machine-doc
- Goal: 把下一轮久坐提醒的目标状态机与整体调度逻辑整理成一份正式设计文档，确保后续实现前先有清晰真源。
- Requirement Brief: 用户明确要求先暂停继续编码，把“智能提醒 / 强制提醒 / 自然休息”的职责边界、状态迁移与调度优先级写成文档，放到 `docs/` 根目录；同时将原先 `严格模式` 的语义折叠进 `强制提醒`。
- Assumptions: 当前最重要的是先解决“正在操作电脑时不要被定时硬打断”；第一版应先做减法，不把 `breakPromptStyle`、`soft nudge` 或复杂专注推理继续混入核心提醒状态机。
- Source Basis: `docs/{RepositoryGuidelines,CodeMap,Architecture}.md`、`docs/specs/TID-20260408-adaptive-reminder-state-machine/plan.md`、`docs/{plans,logs}/2026-04-08.md`、`apps/desktop/src-tauri/src/platform.rs`、`apps/desktop/src-tauri/src/state.rs`（通过关键字段与状态机相关 grep）
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅新增/更新文档，不改运行时代码；touches_running_service=no；backup_required=no；backup_plan=以 Git diff 与 workflow docs validator 为边界；rollback_plan=回退本任务新增/更新的 docs；destructive_operations=none；operator_approval_required=no；rationale=纯文档任务。
- Change Summary: 新增 `docs/ReminderScheduling.md`，系统化写出下一轮提醒状态机与调度逻辑：主模型只保留 `智能提醒 / 强制提醒 / 自然休息` 三个用户概念，其中 `强制提醒` 直接吸收原先 `严格模式` 的语义；文档同时定义 `Timing / WaitingForOpportunity / BreakActive / Blocked / NaturalBreak` 五个目标状态，并补充 tick 优先级、伪代码、推荐阈值、运行时字段建议和典型场景；同步更新 `docs/CodeMap.md` 与本任务 workflow 文档。
- Verification: `python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08 ✅`）。
- Knowledge Capture: “提醒何时开始”“用户是否已自然休息”“UI 如何展示”必须拆成独立维度；而“严格打断”如果只服务于强制提醒，就不该继续单独暴露成第二个同义设置。
- Knowledge Route: local（沉淀到 `docs/ReminderScheduling.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次提醒调度设计文档、索引与 workflow 文档同步。
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback
- Warmup Verification: trivial 文档任务且 Required Roles 仅 `scribe`，符合 `single-agent-fallback` 直行条件；无需额外 `spawn_agent`。
- Risk & Rollback: 主要风险是新文档若没有清楚声明“这是目标模型”，会与当前已实现的实验性 adaptive 版本混淆；回滚只需删除/恢复本任务新增文档与索引更新，不影响代码。
- Retention Review: 这份文档值得长期保留。它把后续实现要遵循的提醒调度真源从零散对话整理成了稳定文档，后续可以直接以此驱动实现和评审。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260408-reminder-mode-simplification · 收敛提醒模式并实现智能强制自然休息模型

- Task-ID: TID-20260408-reminder-mode-simplification
- Goal: 把提醒策略最终落成“智能提醒 / 强制提醒 / 自然休息”三件事，解决用户在持续输入时被定时硬打断的问题，同时避免设置项重复和语义漂移。
- Requirement Brief: 用户明确要求继续做减法，只保留“是否还在操作电脑”“超过久坐时间后如何触发提醒”“长时间离开是否视为已休息”这三层逻辑；本轮要把当前 Tauri 端的 adaptive/strict/style 混合实现，统一改成单一 reminder mode 模型并同步到设置页和文档。
- Assumptions: 当前产品只需要一个全局 `reminder_mode`；`强制提醒` 既代表到点即打断，也代表 break 期间不可跳过/延后/关闭；`自然休息` 继续只作为“长时间离开后重置节奏”的规则，不参与提醒方式分支。
- Source Basis: `apps/desktop/src-tauri/src/{state,platform,shell}.rs`、`apps/desktop/src/App.tsx`、`apps/desktop/src/locales/{zh-CN,en}.json`、`docs/{ReminderScheduling,Architecture,UI,SettingsInventory,RepositoryGuidelines,CodeMap}.md`、`docs/specs/TID-20260408-{adaptive-reminder-state-machine,reminder-state-machine-doc}/plan.md`、当日 logs/plans
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地提醒投递时机、break 可绕过性与设置页入口；touches_running_service=no；backup_required=no；backup_plan=以 Git diff、Rust tests/check、前端构建与 workflow docs validator 为边界；rollback_plan=回退 `state.rs`、`platform.rs`、`App.tsx`、locale 与 docs 改动；destructive_operations=替换当前 adaptive/strict 设置模型并收紧强制提醒下的动作权限；operator_approval_required=no；rationale=不涉及数据、提权、外部服务或新增依赖。
- Change Summary: 在 `apps/desktop/src-tauri/src/state.rs` 引入全局 `ReminderMode::{Smart,Forced}` 与单一 `idle_opportunity_seconds` 设置，移除 `adaptive_breaks_enabled`、per-break strict/idle/soft-nudge 配置与 `next_break_nudged_at_ms` 运行时状态；tick 调度改成 smart 模式到点后等待空档、forced 模式到点立即严格开始，soft nudge 完全移除；`load_settings()` 新增旧 strict 字段迁移逻辑，确保已有用户配置自动映射到 `reminderMode=forced`；`platform.rs` 现在无论 `natural_breaks` 是否开启都持续上报 `idle_ms`；同时从主实现与设置 schema 中删除 `breakPromptStyle / break_prompt_style`，window 模式固定使用单一默认 break profile；`App.tsx` 设置页只保留 `智能提醒 / 强制提醒 / 自然休息`，不再展示独立 strict mode 或 break style 控件；同步更新了 locale、task evidence、`docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md`、`docs/CodeMap.md` 与 `docs/CHANGELOG.md`。
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`10 passed; 0 failed`，覆盖 forced mode、idle signal 解耦、legacy strict 迁移等主路径）；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 1.35s`）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.41s`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08 ✅`）。
- Knowledge Capture: “智能提醒”需要一个稳定的 idle signal，而不是挂在 `natural_breaks` 开关之后；“强制提醒”一旦存在，就应直接吸收 strict 语义，避免用户面对两个同义开关。
- Knowledge Route: local（沉淀到 host 设置真源、task specs、`docs/Architecture.md`、`docs/UI.md`、`docs/SettingsInventory.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 reminder mode 收敛、host 状态机改造、前端设置页收敛、测试与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成设计、实现、验证与文档。
- Risk & Rollback: 主要风险是旧 settings.json 迁移不完整会把已有 strict 用户误降级成智能提醒，或者 forced mode 下仍残留可 postpone/skip 的逃逸路径；另一个风险是移除 `break_prompt_style` 后 window 模式 profile 若不稳定会带来视觉回归。如发现行为不符，直接回退 `state.rs`、`platform.rs`、`shell.rs`、`App.tsx`、locale 与 docs 改动即可恢复先前版本。
- Retention Review: 这次 reminder mode 收敛值得长期保留。它把“什么时候打断用户”从混合配置和隐藏分支中抽出来，重新压成清晰的单一模型，后续继续调整阈值或文案时不需要再跨越多套旧概念。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260408-tauri-window-fullscreen-fix · 修复 Tauri 窗口最小尺寸与无标题栏全屏

- Task-ID: TID-20260408-tauri-window-fullscreen-fix
- Goal: 修复 Tauri 打包后主窗口过小和 break fullscreen 顶部留白的问题，让窗口模式至少受 `800x600` 约束，并让 fullscreen 走无标题栏策略。
- Requirement Brief: 用户明确指出当前 Tauri 版本在窗口模式和 fullscreen 上都存在宿主层问题；本轮只改主窗口尺寸边界与 break fullscreen 进入/退出逻辑，不改 break 页面视觉结构和业务状态机。
- Assumptions: 顶部空白主要来自 macOS native fullscreen/titlebar 语义，而不是 break 页 CSS；主设置窗口当前 `480x440` 的最小尺寸已低于可用下限。
- Source Basis: `apps/desktop/src-tauri/{tauri.conf.json,src/shell.rs}`、`apps/desktop/src/App.tsx`、`docs/UI.md`、`docs/logs/2026-04-04.md`、`docs/specs/TID-20260404-fullscreen-break-close-fix/*`、Tauri 2 `webview_window.rs` 的 `set_simple_fullscreen()` 文档与 tao macOS fullscreen 说明
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅桌面窗口宿主边界与 fullscreen 进入/退出策略；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、`cargo check`、前端 build 与代码路径审查；rollback_plan=回退 `apps/desktop/src-tauri/{tauri.conf.json,src/shell.rs}` 与本任务 docs；destructive_operations=替换当前 fullscreen 进入/退出策略与主窗口尺寸边界；operator_approval_required=no；rationale=纯本地桌面壳层修复，不涉及数据、提权、外部副作用或新增依赖。
- Change Summary: `apps/desktop/src-tauri/tauri.conf.json` 的主窗口边界已改成默认 `960x640`、最小 `800x600`；`apps/desktop/src-tauri/src/shell.rs` 新增 `set_break_window_fullscreen()`，让 break fullscreen 在 macOS 改走 `set_simple_fullscreen()`，而 close path 在 hide + destroy 前会统一兜底退出 simple/native fullscreen，不再依赖 `is_fullscreen()` 单一路径。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 4.57s`）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.50s`）；`cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check` BLOCKED（当前 toolchain 未安装 `cargo-fmt/rustfmt`）；`python3 scripts/validate_workflow_docs.py --mode manual` 待本轮 docs 收口后复跑。
- Knowledge Capture: `set_fullscreen()` 在 macOS 上对应 native borderless fullscreen/new space 语义；如果用户想要稳定的“无标题栏铺满”，应优先用 `set_simple_fullscreen()`，而 close path 也必须同步兜底 simple/native fullscreen。
- Knowledge Route: local（沉淀到 `shell.rs` helper、本任务 specs 与 `docs/UI.md` / `docs/CHANGELOG.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次主窗口尺寸边界与 break fullscreen 宿主修复、验证和文档收口。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成定位、实现、验证与文档。
- Risk & Rollback: 主要剩余风险是 macOS simple fullscreen 退出时仍可能保留系统动画；另一个风险是后续如果有人重新直接调用 `set_fullscreen(true)`，顶部空白问题可能回归。回滚时直接回退 `apps/desktop/src-tauri/{tauri.conf.json,src/shell.rs}` 与本任务 docs 即可。
- Retention Review: `800x600` 的主窗口最小边界和统一 fullscreen helper 都值得保留；继续依赖当前漂移的尺寸配置或混用 native/simple fullscreen 都不值得恢复。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

### Follow-up 调整（窗口模式）
- Follow-up Requirement Brief: 用户补充截图后，明确指出当前 window 模式 break 仍错误缩在右下角，要求改为居中且至少占屏幕 `80%`；本次 follow-up 继续限定在 `apps/desktop/src-tauri/src/shell.rs` 的宿主 profile 与同一 Task docs，不改 break 页面 React 结构和业务状态机。
- Follow-up Change Summary: `shell.rs` 的非全屏 break profile 已从“microbreak 右下角小浮窗 / break 居中”改为统一居中大窗，尺寸按工作区约 `80% x 80%` 计算，并对窄屏/矮屏做边界保护；原 `bottom_right_position()` 分支已删除。
- Follow-up Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 1.02s`）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.29s`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08 ✅`）。
- Follow-up Risk & Rollback: 主要风险是超小工作区会退化为尽量贴近可用区域的居中窗口，而不是严格 `800x600`；若用户后续希望区分 microbreak/long break 的视觉强度，应重新通过 spec 定义，而不是恢复旧的右下角分支。回滚时直接回退 `apps/desktop/src-tauri/src/shell.rs` 与本任务 docs。

## TID-20260408-break-prompt-parity-upgrade · 补齐 Tauri break prompt 的原版体验能力

- Task-ID: TID-20260408-break-prompt-parity-upgrade
- Goal: 让当前 Tauri break prompt 重新具备接近原版的背景氛围与可配置能力，至少补回自定义壁纸、开始音、随机交互语和线性倒计时。
- Requirement Brief: 用户明确指出当前 Tauri break window 相比原版少了“自定义壁纸、声音、随机交互语，以及更像原版的倒数条”，要求沿现有 Tauri break prompt 继续优化补齐，不回退到 Electron，也不改 reminder/window shell 状态机。
- Assumptions: 旧版 break 体验基线主要来自 `app/break.html`、`app/microbreak.html`、`app/*break-renderer.js`、`app/main.js`、默认 ideas 和 `app/audio/*.wav`；当前未启用 `assetProtocol` / dialog plugin，因此本地壁纸应以压缩 data URL 持久化到 settings。
- Source Basis: 已阅读 `app/{break.html,microbreak.html,break-renderer.js,microbreak-renderer.js,main.js}`、`app/utils/{defaultBreakIdeas.js,defaultMicrobreakIdeas.js,defaultSettings.js}`、`apps/desktop/src/App.tsx`、`apps/desktop/src-tauri/src/state.rs`、`docs/{UI,Architecture,SettingsInventory}.md`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=本地桌面 break prompt、偏好页和 settings schema；touches_running_service=no；backup_required=no；backup_plan=依赖 VCS、`npm --prefix apps/desktop run typecheck`、`npm --prefix apps/desktop run build`、`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` 与 Playwright 截图；rollback_plan=回退 `apps/desktop/src/{App.tsx,lib/break-prompt.ts,locales/*,assets/audio/*}`、`apps/desktop/src-tauri/src/state.rs` 与本任务 docs；destructive_operations=替换 break prompt 视觉结构并扩展配置模型；operator_approval_required=no；rationale=纯本地体验补齐，无数据迁移或外部副作用。
- Change Summary: 已在 `App.tsx` 给 break prompt 补回更完整背景、cue card、线性倒计时和开始音播放逻辑；新增 `break-prompt.ts` helper 统一管理预设主题、内置随机交互语、开始音映射和自定义壁纸压缩；从旧版 `app/audio/*.wav` 复制开始音到 `apps/desktop/src/assets/audio/`；`state.rs` 扩展 `breakBackdrop`、`breakCustomBackdrop*`、`breakIdeasEnabled`、`microbreakStartSound`、`longBreakStartSound`、`breakSoundVolume` 真源并补 legacy migration / sanitize；偏好页新增背景主题、自定义壁纸上传与预览、随机交互语开关、开始音和音量设置。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（`✓ 1825 modules transformed.`、`✓ built in 1.41s`）；`cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 1.33s`）；Playwright 已更新 `docs/specs/TID-20260408-break-prompt-parity-upgrade/evidence/break-prompt-parity-preview.png`；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08 ✅`）。
- Knowledge Capture: 已把 Tauri break prompt 的 parity 策略、自定义壁纸持久化取舍和新增配置边界同步到 `docs/{UI,Architecture,SettingsInventory,CodeMap}.md`。
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: N/A（仅 fallback 时填写）
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 的 developer policy 不允许未显式授权的 sub-agent delegation；因此改由 orchestrator 在当前线程直接完成实现、验证、证据和文档闭环，范围仅限本任务列出的前台 / settings / docs。
- Risk & Rollback: 主要风险是自定义壁纸会增大 `settings.json`，以及个别平台可能更严格限制自动播放；回滚时直接撤销 `App.tsx`、`break-prompt.ts`、`state.rs`、locale 和 docs 变更，必要时清空 `breakCustomBackdrop*` 字段。
- Retention Review: 本轮新增的 break prompt helper、声音资源接入和 settings schema 都属于长期产品能力，不应以临时 patch 处理；自定义壁纸采用 data URL 持久化的取舍也需要留在长期文档里，避免后续重复踩坑。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260408-break-prompt-purity-pass · 收敛休息界面并清理 break prompt 文案层

- Task-ID: TID-20260408-break-prompt-purity-pass
- Goal: 把当前 break prompt 改回纯净休息界面，只保留交互语与倒计时，并把 prompt copy 与缺失标签统一收回 locale 资源层。
- Requirement Brief: 用户明确指出当前休息界面“像 Demo”，不能接受“微休息”等标签化提示和左右分栏；本轮只改 Tauri 前台 break prompt、i18n 资源与相应 docs，不动 host 调度和 settings schema。
- Assumptions: 高级感取向是更少的组件和更直接的文案；条形进度比圆环更适合作为休息界面的辅助计时；`breakIdeasEnabled` 的设置语义可保留，只改变呈现方式。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts,lib/break-prompt.ts,locales/{zh-CN,en}.json}`、`app/{break.html,microbreak.html,break-renderer.js,microbreak-renderer.js}`、`docs/{RepositoryGuidelines,CodeMap,UI}.md`、当日 logs/plans 与 `docs/specs/TID-20260408-break-prompt-parity-upgrade/*`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 Tauri 前台 break prompt 与 locale 层；touches_running_service=no；backup_required=no；backup_plan=依赖 Git diff、前端 typecheck/build 与 docs validator；rollback_plan=回退 `App.tsx`、`i18n.ts`、`break-prompt.ts`、locale 与 docs；destructive_operations=替换 break prompt 布局并删除 prompt 硬编码；operator_approval_required=no；rationale=无数据、权限或外部副作用。
- Change Summary: 已把 `App.tsx` 的 break prompt 从左右分栏 + cue 卡片 + 圆环倒计时改成单列纯净布局；主视觉只剩单句交互语、数字倒计时和细条形进度。`i18n.ts` 增加 locale 数组读取，`break-prompt.ts` 删除硬编码 prompt copy，`apps/desktop/src/locales/{zh-CN,en}.json` 新增 prompt 数组/默认 prompt，并补齐开始音 label key。
- Verification: `npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（`✓ 1824 modules transformed.`、`✓ built in 1.33s`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-08 ✅`）；UI 证据已补 `docs/specs/TID-20260408-break-prompt-purity-pass/evidence/break-prompt-pure-preview.png`。
- Knowledge Capture: break prompt 的“高级感”来自减法；数字计时 + 细条形进度已经足够，继续叠加圆环和 cue card 只会让界面更像组件演示。
- Knowledge Route: local（沉淀到本任务 specs 与 `docs/UI.md`）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 Tauri break prompt、locale/i18n 层与 docs 收敛。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前运行策略禁止未获用户显式授权时调用 `spawn_agent`）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，且上层运行策略要求仅在用户明确要求子 agent 时才可调用 `spawn_agent`；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成 break prompt 收敛、验证与文档闭环。
- Risk & Rollback: 主要风险是 locale prompt fallback 漏配导致 UI 露 key，或纯净布局在 manual finish 场景解释不足；回滚时直接回退 `App.tsx`、`i18n.ts`、`break-prompt.ts`、locale 与 docs 即可。
- Retention Review: 本轮的单列 break prompt 结构和 locale prompt 管理方式都值得长期保留。它把 break 页面从展示型组件堆叠拉回到更像产品的休息界面，也避免后续再把 copy 硬塞回 helper 常量里。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
