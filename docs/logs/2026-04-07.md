## TID-20260407-finish-break-no-exit · 修复完成休息后应用退出

- Task-ID: TID-20260407-finish-break-no-exit
- Goal: 修复 Tauri break prompt 点击“完成休息”后应用直接退出的问题，让 break prompt 关闭后应用继续后台存活。
- Requirement Brief: 用户报告点击“完成休息”会直接退出程序；本轮只修 Tauri break command / shell close timing，并补齐验证与文档。
- Assumptions: 根因更接近 break CTA 命令在 `invoke` 回包前同步 destroy 当前 break webview 的生命周期竞态，而不是 `PauzaState::finish_current_break()` 的调度逻辑错误。
- Source Basis: `apps/desktop/src-tauri/src/commands.rs`、`apps/desktop/src-tauri/src/shell.rs`、`apps/desktop/src-tauri/src/state.rs`、`apps/desktop/src/App.tsx`、`docs/specs/TID-20260404-fullscreen-break-close-fix/*`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 break window close timing；touches_running_service=no；backup_required=no；backup_plan=以 Git 差异与编译链为边界；rollback_plan=回退 `commands.rs`、`shell.rs` 与本任务 docs；destructive_operations=替换 break CTA 的同步 close；operator_approval_required=no；rationale=不涉及数据、权限和外部副作用。
- Change Summary: 在 `shell.rs` 新增 `close_break_window_deferred()`，让 break CTA 的 `finish_current_break` / `skip_current_break` / `postpone_current_break` 先回包再异步 teardown break webview；`close_break_window()` 本身仍保留 fullscreen exit + destroy 语义。
- Verification: `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`Finished 'dev' profile [unoptimized + debuginfo] target(s) in 5.12s`）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`3 passed; 0 failed`）；`npm --prefix apps/desktop run typecheck` PASS（无输出即通过）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.39s`）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-07 ✅`）；证据报告：`docs/specs/TID-20260407-finish-break-no-exit/evidence/README.md`。
- Knowledge Capture: break CTA 这类由当前窗口发起的宿主命令，不应在 `invoke` 回包前同步 destroy 自己所属的 webview。
- Knowledge Route: local（沉淀到本任务 specs / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 Tauri break command / shell 生命周期修复与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，遵循上层策略不 spawn_agent）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，无法按仓库流程 spawn `architect/coder/tester/scribe`；因此采用 `single-agent-fallback`，由单 agent 在同范围内完成规划、实现、验证与文档，且严格限制在 break 完成退出 bug。
- Risk & Rollback: 主要风险是 75ms deferred close 仍不足以完全规避宿主竞态；若用户本机仍复现，下一步应升级为前端事件驱动自关闭。回滚时直接撤销 `commands.rs` / `shell.rs` 与本任务 docs。
- Retention Review: `close_break_window_deferred()` 值得长期保留，它把“命令回包”和“当前窗口销毁”从同一生命周期临界区拆开，能作为后续 break CTA 的宿主实现范式。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260407-rhythm-chip-presets · 节奏页核心时间改为预设芯片

- Task-ID: TID-20260407-rhythm-chip-presets
- Goal: 将节奏页核心时间设置改成 preset 芯片，让微休息/长休息的高频时间值一击选定，同时保留低频 stepper 设置。
- Requirement Brief: 用户明确要求核心时间设置使用 preset 芯片，次要设置继续保留 stepper；本轮只改 Tauri 主设置页节奏分类的交互表达。
- Assumptions: 用户给出的 preset 值集合即为本轮最终选项集，不需要额外开放自由输入或自定义 preset。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/components/ui/segmented-control.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src/locales/zh-CN.json`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/UI.md`、`docs/SettingsInventory.md`、当日 plans/logs
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限主设置页节奏分类的输入交互；touches_running_service=no；backup_required=no；backup_plan=以 Git 差异、typecheck/build 与浏览器证据为边界；rollback_plan=回退 `App.tsx` 与本任务 docs；destructive_operations=替换顶部四个 stepper 为 preset 芯片；operator_approval_required=no；rationale=不涉及数据、权限、外部副作用或提权。
- Change Summary: `apps/desktop/src/App.tsx` 将节奏页顶部四个核心时间输入改为 preset 芯片组，新增 `SchedulePresetCard` / `PresetChipRow` 结构承载微休息与长休息区块；下方“提前提醒 / 延后”继续保留 `CompactNumber` stepper，并补齐了本任务 evidence、UI 摘要与 changelog 记录。
- Verification: `npm --prefix apps/desktop run typecheck` PASS（无输出即通过）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.39s`）；证据报告：`docs/specs/TID-20260407-rhythm-chip-presets/evidence/README.md`，其中 `schedule-preset-snapshot.md` 证明 preset primary flow 与 stepper fallback flow 同页共存，`browser-console.log` 无新增显著错误。
- Knowledge Capture: 紧凑设置页可按“高频 preset + 低频 stepper”做交互分层，减少输入负担且不牺牲细调能力。
- Knowledge Route: local（沉淀到本任务 specs / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次节奏页芯片化交互改造、验证与文档结案。
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation，遵循上层策略不 spawn_agent）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation，无法按仓库流程 spawn `ui_designer/architect/coder/tester/scribe`；因此采用 `single-agent-fallback`，由单 agent 在同范围内完成规划、实现、验证与文档。
- Risk & Rollback: 主要风险是 preset 行在较窄窗口内换行过多或选中态不够清晰；回滚时直接撤销 `App.tsx` 中的 preset 组件和相关 docs。
- Retention Review: `SchedulePresetCard` / `PresetChipRow` 值得长期保留，它把“高频离散值”从 stepper 输入中抽离出来，适合作为紧凑设置页里的默认表达范式。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260407-current-worktree-commit · 整理当前修改并提交

- Task-ID: TID-20260407-current-worktree-commit
- Goal: 基于当前已暂存的多任务变更整理一次可追溯提交，补齐 docs/evidence 与提交审计收口，并完成主提交与 audit-only follow-up 提交。
- Requirement Brief: 本轮只整理 staged diff、排除原始缓存、补齐缺失 evidence/字段和提交说明，不新增业务功能或改产品语义。
- Assumptions: 当前 Git index 代表用户希望提交的主线变更；`.playwright-mcp/`、`output/playwright/`、`scripts/__pycache__/` 和空白 `Untitled` 文件属于本地缓存或杂物，可移出正式提交。
- Source Basis: `git status --short`、`git diff --cached --stat`、`.githooks/commit-msg`、`.githooks/post-commit`、`scripts/record_commit_audit.py`、`scripts/validate_commit_message_task_id.py`、`docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/CHANGELOG.md`、`docs/UI.md`、当日 plans/logs 与 `TID-20260407-rhythm-chip-presets` spec。
- Level: trivial
- Lane: fast
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地提交流程、文档沉淀与忽略规则；touches_running_service=no；backup_required=no；backup_plan=以当前 Git index/worktree 差异为回滚边界；rollback_plan=回退本任务 docs、`.gitignore` 与 stage 清理动作；destructive_operations=仅取消暂存或删除明确的临时产物；operator_approval_required=no；rationale=不涉及运行中服务、数据删除、外部副作用或提权。
- Change Summary: 新增 `.gitignore` 对 `.playwright-mcp/`、`output/playwright/`、`scripts/__pycache__/` 的忽略；将 `TID-20260403-hig-tailwind-redesign` 文档中残留的 `output/playwright` 证据引用改为正式 `docs/specs/**/evidence/`；补齐 `TID-20260407-rhythm-chip-presets` evidence 与 DONE 字段；为本次整理创建 `TID-20260407-current-worktree-commit` spec，并准备主提交与 audit-only 提交。
- Verification: `npm test` PASS（`Test Files 18 passed (18)`、`Tests 319 passed (319)`）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（`3 passed; 0 failed`）；`npm --prefix apps/desktop run typecheck` PASS（无输出即通过）；`npm --prefix apps/desktop run build` PASS（`✓ 1820 modules transformed.`、`✓ built in 1.39s`）；`python3 scripts/validate_agent_configs.py` SKIP（`no multi-agent config detected`，符合当前仓库状态）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS（`[OK] Workflow docs validation passed for 2026-04-07 ✅`）。
- Knowledge Capture: 多任务 staged 提交必须按 `TASK-ID-MULTIPLE` 归因，原始调试缓存不应替代正式 evidence；`post-commit` 自动生成的 `docs/commits/` 需要 audit-only follow-up commit 才能真正落盘。
- Knowledge Route: local（沉淀到 `.gitignore`、本任务 specs / logs）
- Execution Mode: single-agent-fallback
- Fallback Scope: 仅限本次 staged diff 整理、文档收口、验证与 commit / audit commit 流程。
- Fallback Reason Code: trivial-scribe-only
- Warmup Required Roles: scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（trivial 文档/提交流程任务，仅需 `scribe`）
- Warmup Verification: `Required Roles = scribe`，命中 trivial 单角色条件，按仓库门禁允许 `single-agent-fallback` 直接执行，无需 spawn_agent。
- Risk & Rollback: 主要风险是 commit message 的 `Task-ID` / `Related Task-IDs` 归因不全被 hook 拦截，或主提交后忘记补 audit-only commit；回滚时重新整理 stage、修正文档并重做提交流程即可。
- Retention Review: `.gitignore` 对原始缓存的忽略与“evidence 必须沉淀到 spec 目录”的约束值得长期保留，能减少后续提交噪音与证据漂移。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
