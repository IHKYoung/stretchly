## TID-20260423-smart-reminder-v2-scheduling · 重构智能提醒状态机与恢复结算

- Task-ID: TID-20260423-smart-reminder-v2-scheduling
- Goal: 将 Pauza 的智能提醒重构为更稳定的 v2：active break 生命周期优先，passive blocker 冻结投递而不是重置节奏，短暂离开通过恢复 credit 自动结算，而不是在用户离开时直接弹 break。
- Requirement Brief: 用户已明确认可“blocker freeze + recovery credit”的方向；本任务只在 desktop host 状态机内收敛该语义，不新增设置项、不重做前台布局。
- Assumptions: 当前可用信号仍只有 `idle_ms`、DND 与 app exclusion；本轮优先把 reminder 语义做稳，不引入新的活动强度模型或平台权限。
- Source Basis: `apps/desktop/src-tauri/src/{state.rs,commands.rs,engine.rs,platform.rs}`、`apps/desktop/src/locales/messages/{zh-CN,en}.json`、`docs/{Architecture,ReminderScheduling,SettingsInventory,UI}.md`、`docs/logs/2026-04-08.md`、`docs/logs/2026-04-09.md`、相关 reminder state machine specs。
- Level: complex
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop runtime reminder 状态机、locale 文案、测试与 docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`npm test`、`npm run typecheck`、`npm --prefix apps/desktop run build`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `state.rs`、`commands.rs`、locale 与相关 docs/specs；destructive_operations=none；operator_approval_required=no；rationale=无线上服务、权限升级、数据迁移或外部副作用。
- Change Summary: 已完成 reminder v2 收敛：active break 生命周期优先于 passive blocker；pause/focus/DND/app exclusion 改为冻结投递并在解除后平移 due / notification / waiting timer；smart 模式新增 recovery hold 与 recovery credit，短暂离开会在返回时自动抵扣 microbreak、顺延 long break，达到 full reset 阈值时才整轮重排；runtime status / last_action / locale 现能解释 waiting budget、恢复结算与顺延结果。
- Verification: `python3 scripts/ensure_workflow_ready.py --target . --hooks required` PASS；`python3 -m py_compile scripts/validate_workflow_docs.py scripts/validate_agent_configs.py` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（30 passed）；`npm test` PASS（61 passed）；`npm run typecheck` PASS；`npm --prefix apps/desktop run build` PASS；`python3 scripts/validate_agent_configs.py` PASS（skip: no multi-agent config detected）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（本轮将把 blocker freeze / recovery credit 的 reminder v2 规则沉淀到 docs 与测试）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop reminder host 状态机、相关 locale、测试与 workflow/docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略只允许在用户明确要求子 agent 时 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成 reminder v2 设计、实现、验证与文档闭环。
- Risk & Rollback: 主要风险是 blocker 重叠时 due 平移重复累计、microbreak credit 误推进 cycle，以及用户可见状态文案与真实调度不一致；若任一路径不稳，整体回退 `state.rs/commands.rs/locale/docs`。
- Retention Review: 本轮改动值得长期保留。它把“什么时候欠休息”“什么时候适合投递”“离开后如何抵债”拆开，避免 blocker reset 导致节奏漂移，并把状态解释补到用户可见层；无额外依赖、无权限扩张、无临时开关残留。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260423-release-014-macos-package-commit · 收口 0.1.4 并打包 macOS 安装包

- Task-ID: TID-20260423-release-014-macos-package-commit
- Goal: 将当前未提交的 desktop/docs 变更作为 `0.1.4` 收口，统一版本真源，完成本地 arm64 macOS 打包，并形成主提交与审计提交。
- Requirement Brief: 用户明确要求“以 0.1.4 做一个 macOS 的编译打包，以及 commit”；本任务据此只做本地版本收口、无签名出包和 git 提交，不扩展到 GitHub release、site pinned 下载链接或远端 push。
- Assumptions: 当前机器为 `arm64` / macOS `26.4`；当前工作树里的未提交代码/文档改动都属于 `0.1.4` 正式边界；`post-commit` hook 会自动生成主提交审计，需要 audit-only follow-up commit 落盘。
- Source Basis: `git status --short`、`uname -m`、`sw_vers`、`package.json`、`package-lock.json`、`apps/desktop/package.json`、`apps/desktop/package-lock.json`、`apps/desktop/src-tauri/{Cargo.toml,Cargo.lock,tauri.conf.json}`、`README.md`、`docs/CHANGELOG.md`、`.githooks/post-commit`、`scripts/record_commit_audit.py`、`docs/specs/TID-20260420-release-013-packaging-and-publish/*`。
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅影响本地版本真源、desktop 打包产物、release 文档与 git 本地提交；touches_running_service=no；backup_required=no；backup_plan=`python3 scripts/sync_desktop_locales.py`、`npm test`、`npm run typecheck`、`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`、`npm --prefix apps/desktop run build`、`npm --prefix apps/desktop run tauri build -- --bundles app,dmg --no-sign`、`python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=提交前回退版本真源/文档/打包产物，提交后用 `git revert` 回滚错误版本；destructive_operations=none；operator_approval_required=no；rationale=用户要求本地出包与提交，不涉及线上服务或提权。
- Change Summary: 已完成 `0.1.4` 本地 release 收口：根/desktop/Tauri/Cargo/lockfile 版本真源已统一提升到 `0.1.4`；`README.md` 与 `docs/CHANGELOG.md` 已切到新版本口径；当前未提交的 reminder v2 代码与相关 docs 一并纳入本轮版本边界；本地 arm64 macOS 打包已生成 `Pauza.app` 与 `Pauza_0.1.4_aarch64.dmg`，接下来只需执行主提交与 audit-only follow-up commit。
- Verification: `python3 scripts/ensure_workflow_ready.py --target . --hooks required` PASS；`python3 scripts/sync_desktop_locales.py` PASS；`npm test` PASS（61 passed）；`npm run typecheck` PASS；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（30 passed）；`npm --prefix apps/desktop run build` PASS；`npm --prefix apps/desktop run tauri build -- --bundles app,dmg --no-sign` PASS（产出 `Pauza.app` 与 `Pauza_0.1.4_aarch64.dmg`）；`python3 scripts/validate_agent_configs.py` PASS（skip: no multi-agent config detected）；`python3 scripts/validate_workflow_docs.py --mode manual` PASS。
- Knowledge Capture: yes
- Knowledge Route: local（沉淀本地无签名 macOS 打包与 commit audit follow-up 的 release 收口流程）
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在版本真源、desktop local build、release docs 与 commit/audit 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略只允许在用户明确要求子 agent 时 delegation；因此采用 `single-agent-fallback`，由单 agent 完成 `0.1.4` 收口、打包与提交闭环。
- Risk & Rollback: 主要风险是版本真源漏改、`tauri build` 因签名环境误入失败，以及多 Task-ID 主提交被 hook 阻断；若任一路径不稳，先回退版本真源/文档与本地产物，再重新验证并提交。
- Retention Review: 本轮值得长期保留的是 `0.1.4` 的版本边界说明、本地无签名 macOS 出包命令与 `post-commit -> audit-only follow-up commit` 的提交审计收口流程；它们能直接复用到后续本地 release 任务中，且不会引入额外依赖或外部副作用。
- Retention Decision: keep

### Execution Checklist
- [ ] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
