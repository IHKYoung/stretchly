## TID-20260410-pre-break-heads-up-restoration · 恢复并重构提前提示为可见 heads-up 状态

- Task-ID: TID-20260410-pre-break-heads-up-restoration
- Goal: 把“提前提醒”从体感失效的系统通知残链修正为真正可见的 heads-up 提示。
- Requirement Brief: 用户反馈当前“提前提醒”似乎没有用到；经核查设置、调度和通知链后，本轮将其恢复为 due 前的可见状态，并保留系统通知为辅助。
- Assumptions: 用户说“没用”主要源于产品可见性不足，而不是字段未接线；tray / runtime status 是更优雅的主提示表面。
- Source Basis: `state.rs`、`engine.rs`、`App.tsx`、`apps/desktop/src/locales/messages/{en,zh-CN}.json`、`docs/{ReminderScheduling,SettingsInventory,Architecture,UI}.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 desktop runtime 的 heads-up 状态、通知错误日志和文档语义；touches_running_service=no；backup_required=no；backup_plan=Rust 测试 + locale sync + desktop build；rollback_plan=回退 state/engine/locale/docs；destructive_operations=none；operator_approval_required=no；rationale=无运行中服务和外部副作用
- Change Summary: `state.rs` 新增 heads-up 派生状态和文案函数，使 due 前状态可持续可见；`engine.rs` 为通知失败补日志；中英文 locale 将“提前提醒/notification”收敛为“提前提示/heads-up cue”，并同步 Architecture / UI / ReminderScheduling / SettingsInventory / CHANGELOG
- Verification: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS（23 passed）；`python3 scripts/sync_desktop_locales.py` PASS；`npm --prefix apps/desktop run build` PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop runtime heads-up 状态、locale 语义收口与任务文档更新
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation 授权，遵循上层工具策略不调用 `spawn_agent`
- Risk & Rollback: 风险主要是 heads-up 文案与 due 后 waiting 文案混淆；可通过回退 `heads_up_kind()`、`pre_break_heads_up_detail()`、日志分支和对应 locale/docs 撤销
- Retention Review: 本次改动属于产品语义澄清和可观测性补全，应长期保留；未引入临时脚本或临时开关
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260410-settings-language-switch-freeze · 修复设置页切换语言卡死

- Task-ID: TID-20260410-settings-language-switch-freeze
- Goal: 修复设置页切换语言后卡住/卡死的问题，并保持自动保存体验。
- Requirement Brief: 用户反馈当前偏好页切换语言后会卡住；本任务聚焦前台语言生效时机与轮询依赖，不改 locale 内容、Rust host 或 settings schema。
- Assumptions: 卡死主因来自前台在语言下拉关闭与 autosave 回包之间，用草稿语言立即驱动整页文案与 `dir/lang` 切换，而不是 locale 真源缺失。
- Source Basis: `apps/desktop/src/{App.tsx,i18n.ts}`、`apps/desktop/src-tauri/src/{commands,state,shell,i18n}.rs`、`test/desktopSettingsControls.js`、`docs/specs/TID-20260409-language-selector-multilang-fix/plan.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 desktop 设置页前台语言生效时机与轮询；touches_running_service=no；backup_required=no；backup_plan=`npm test` + `npm --prefix apps/desktop run typecheck` + `npm --prefix apps/desktop run build`；rollback_plan=回退 `App.tsx`、`i18n.ts`、测试与文档；destructive_operations=none；operator_approval_required=no；rationale=纯前台行为修复，无外部副作用
- Change Summary: `App.tsx` 不再直接用 `form.language` 驱动整页翻译，而是优先跟随 `snapshot.settings.language`；同时把 snapshot 轮询 effect 与语言草稿变更解耦，并在 `i18n.ts` 新增 `resolveUiLanguage()` helper；`test/desktopSettingsControls.js` 补了“保存完成前 UI 仍跟随已持久化语言”的回归测试。
- Verification: `npm test` PASS（4 files / 59 tests）；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（保留既有 chunk size warning）
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop 设置页语言切换的前台时机修复、测试与文档更新
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,ui_designer,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation 授权，遵循上层工具策略不调用 `spawn_agent`
- Risk & Rollback: 主要风险是语言切换会延后到保存成功后才体现；若后续确认不是前台重入而是 host 死锁，可回退 `resolveUiLanguage()` 与轮询 effect 依赖，再转向宿主链路继续排查
- Retention Review: `resolveUiLanguage()` 和“显示语言跟随已持久化 snapshot”这条边界值得长期保留；它能避免以后再次把草稿值直接接到整页语言/方向切换
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260410-release-011-summary-commit · 整理 0.1.1 版本总结并提交

- Task-ID: TID-20260410-release-011-summary-commit
- Goal: 将当前桌面端阶段性成果整理为 `0.1.1`，补齐发布说明并完成详细中文提交。
- Requirement Brief: 用户要求对当前版本做总结、文档沉淀并 commit，且本版命名为 `0.1.1`；同时记录下一阶段官网会用 Vercel 部署并提供下载链接。
- Assumptions: 当前 staged 改动就是用户认可的这版完整内容；官网阶段需要一个稳定版本口径作为文案与下载入口基础。
- Source Basis: `package.json`、`package-lock.json`、`apps/desktop/package.json`、`apps/desktop/src-tauri/{tauri.conf.json,Cargo.toml,Cargo.lock}`、`README.md`、`docs/CHANGELOG.md`、`docs/{plans,logs}/2026-04-09.md`、`docs/commits/2026-04-09.md`
- Level: moderate
- Lane: deep
- Execution Profile: merge-gate
- Execution Safety Block: service_impact=仅限版本字段、发布说明、文档沉淀与提交动作；touches_running_service=no；backup_required=no；backup_plan=`npm test` + `npm --prefix apps/desktop run typecheck` + `npm --prefix apps/desktop run build` + `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + docs validator；rollback_plan=回退版本与文档后重做提交；destructive_operations=git commit（用户已明确授权）；operator_approval_required=no；rationale=本地 merge-gate 收口，不涉及外部副作用
- Change Summary: 已将 root / desktop / tauri / cargo 当前有效版本统一提升到 `0.1.1`；根 `README.md` 新增本版概览、产品主张和“官网先行，Vercel 部署 + 下载链接”的下一阶段说明；`docs/CHANGELOG.md` 将当前未发布内容封版为 `0.1.1 - 2026-04-10`；同时补齐本任务 specs / daily plans / logs，并基于当前 staged 改动创建详细中文 commit。
- Verification: `npm test` PASS；`npm --prefix apps/desktop run typecheck` PASS；`npm --prefix apps/desktop run build` PASS（保留既有 chunk size warning）；`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` PASS；`python3 scripts/validate_workflow_docs.py --mode manual` PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在版本字段统一、README/CHANGELOG 发布说明、验证与 commit
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: N/A
- Warmup Verification: BLOCKED: 当前 session 未获用户显式 delegation 授权，遵循上层工具策略不调用 `spawn_agent`
- Risk & Rollback: 风险主要在于大版本快照若缺少统一叙述，会让官网阶段继续依赖零散 task docs；若需回退，可恢复 `0.1.0` 版本字段与 README/CHANGELOG 入口，再重新整理提交
- Retention Review: 这次 release 收口文档与版本口径值得长期保留，因为它们会直接成为后续官网、下载页和对外叙述的基线
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
