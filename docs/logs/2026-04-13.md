## TID-20260413-fullscreen-break-current-space-fix · 修复全屏工作区下 break 未覆盖当前界面

- Task-ID: TID-20260413-fullscreen-break-current-space-fix
- Goal: 修复 macOS 下“用户正在其它应用全屏工作时，Pauza 到点后没有覆盖当前全屏界面，只在别的屏幕或 Space 自己开始休息”的回归。
- Requirement Brief: 用户明确报告一个严重 bug：在浏览器、编辑器等全屏应用里工作时，到点后的 Pauza 没有覆盖当前全屏界面，也没有任何有效提醒，只会在另一个屏幕自己开始休息。本任务只修复 desktop shell 的 fullscreen Space overlay 行为，不改 scheduler、前端页面或设置 schema。
- Assumptions: `engine.rs` 仍会正确发起 `show_break_window()`；当前问题主因是 `shell.rs` 工作树把 `MoveToActiveSpace` / `FullScreenAuxiliary` 从 macOS break window 的 native `collectionBehavior` 中移除，而不只是应用未激活或 monitor 选择错误。
- Source Basis: `docs/RepositoryGuidelines.md`、`docs/CodeMap.md`、`docs/Architecture.md`、`docs/ReminderScheduling.md`、`apps/desktop/src-tauri/src/{shell.rs,engine.rs,state.rs}`、`docs/specs/TID-20260408-tauri-window-fullscreen-fix/*`、`docs/specs/TID-20260409-break-window-macos-fullscreen-coverage/*`、`docs/specs/TID-20260411-break-activation-settings-input-fix/*`、`docs/logs/2026-04-09.md`、`docs/logs/2026-04-11.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限本地 desktop shell 的 macOS break window overlay 行为、测试与 docs；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `npm --prefix apps/desktop run build` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=回退 `shell.rs`、对应测试和 docs；destructive_operations=none；operator_approval_required=no；rationale=纯本地桌面端 bug 修复，无数据、提权或外部副作用。
- Change Summary: `apps/desktop/src-tauri/src/shell.rs` 现重新保留 macOS break window 的 `CanJoinAllSpaces | MoveToActiveSpace | FullScreenAuxiliary` 组合，不再把后两者从 native `collectionBehavior` 中移除；同时补了 `break_window_collection_behavior()` helper 与对应单测断言，确保全屏工作区 overlay 策略有机械化回归保护。本任务同步补齐了 `docs/specs/TID-20260413-fullscreen-break-current-space-fix/evidence/README.md`，并把 `docs/Architecture.md`、`docs/CodeMap.md`、`docs/CHANGELOG.md` 与本轮行为重新对齐。
- Verification:
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`23 passed; 0 failed`，其中 `shell::tests::macos_break_window_native_overlay_policy_uses_expected_levels ... ok`）
  - `npm --prefix apps/desktop run build` -> PASS（`✓ 1826 modules transformed.`、`✓ built in 1.97s`；保留既有 chunk size warning）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS（`[OK] Workflow docs validation passed for 2026-04-13 ✅`）
  - `git diff --check` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop shell 的 macOS overlay 修复、对应测试与 workflow/docs 闭环
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe,evidence_collector,reality_checker
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本轮 root cause 修复、验证与文档闭环。
- Risk & Rollback: 主要风险是更激进的 macOS Space 浮出策略可能让普通 non-strict break 更主动抢前台；若修复无效或副作用过强，可回退 `shell.rs` 的 `collectionBehavior` 修正与相应测试/docs。
- Retention Review: 本轮沉淀出一条值得长期保留的 macOS 宿主规则：对 Pauza 这类 break overlay 来说，`CanJoinAllSpaces | MoveToActiveSpace | FullScreenAuxiliary` 需要作为一个整体保留；单独移除后两者会回退成“当前 fullscreen Space 无提示，只在别处自己开始”的严重体验问题。本轮未引入临时脚本、调试开关或需清理资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
