## TID-20260411-settings-freeze-root-cause-fix · 修复设置点击与时间修改卡死

- Task-ID: TID-20260411-settings-freeze-root-cause-fix
- Goal: 完成设置页冻结根因修复的跨日验证与结案，确认“点击设置项卡死”和“改时间卡死”已不再沿旧路径并发触发宿主重刷新。
- Requirement Brief: 用户要求继续修复设置页卡死，并强调要找到更本质的原因；本任务已在 `2026-04-11` 完成代码实现，当前切片负责在 `2026-04-12` 补齐 validator 所要求的当日 logs，并保留最终验证证据。
- Assumptions: 冻结主因是前端 autosave 可重入叠加 Rust host 的无差别重刷新，而不是单个数字输入控件残留问题；因此只要保存链路串行化、设置应用改成差异驱动，点击设置项和改时间两条路径都会明显稳定。
- Source Basis: `apps/desktop/src/App.tsx`、`apps/desktop/src/lib/settings-controls.ts`、`apps/desktop/src-tauri/src/{commands.rs,state.rs,shell.rs}`、`docs/specs/TID-20260411-settings-freeze-root-cause-fix/{README,plan,testplan,evidence/README.md}`、`docs/{plans,logs}/2026-04-11.md`
- Level: moderate
- Lane: deep
- Execution Profile: single-task
- Execution Safety Block: service_impact=仅限 workflow docs 当日切片补录与最终验证；touches_running_service=no；backup_required=no；backup_plan=`python3 scripts/validate_workflow_docs.py --mode manual` + `git diff --check`；rollback_plan=仅回退 `docs/logs/2026-04-12.md`、`docs/plans/2026-04-12.md` 与 task README 链接修正；destructive_operations=none；operator_approval_required=no；rationale=代码修复已完成，本步骤只做跨日文档闭环与门禁校验。
- Change Summary: `apps/desktop/src/App.tsx` 已把设置 autosave 改成串行/合并保存：`formRevision`、`saveRetryToken`、`saveInFlightRef` 与 `pendingSaveRetryRef` 共同保证同一时刻最多一轮 `update_settings` 在飞，保存进行中继续点击设置项或修改时间时，只会排入下一轮。`apps/desktop/src-tauri/src/state.rs` 现为 `PauzaSettings` 与 `ShortcutBinding` 增加 `PartialEq/Eq`，并在 no-op 更新时直接短路。`apps/desktop/src-tauri/src/commands.rs` 现按前后 settings diff 决定宿主副作用：仅 shortcut 变化时重绑快捷键，仅语言变化时整棵重建 tray，其余设置最多走 `refresh_tray_if_needed()` 轻量刷新。
- Verification:
  - `npm test` -> PASS（`Test Files 5 passed (5)`、`Tests 62 passed (62)`）
  - `npm run typecheck` -> PASS
  - `npm --prefix apps/desktop run build` -> PASS（`✓ 1826 modules transformed.`、`✓ built in 2.14s`；保留既有 chunk size warning）
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`26 passed; 0 failed`，新增 `commands::tests::*` 3 条）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS（`[OK] Workflow docs validation passed for 2026-04-12 ✅`）
  - `git diff --check` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop autosave 链路、Rust host refresh diff、跨日文档补录与最终验证
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次根因定位、实现、跨日结案与文档闭环。
- Risk & Rollback: 主要剩余风险是如果用户后续仍能稳定复现“任意设置都长时间卡死”，更深层瓶颈可能落在 `settings.json` 写盘或大尺寸自定义壁纸 data URL 持久化，而不是 autosave / tray refresh；若需要回滚，可先还原 `App.tsx` 的保存串行化和 Rust diff 刷新逻辑，再沿写盘耗时继续排查。
- Retention Review: 本轮沉淀出两条值得长期保留的规则：一是设置 autosave 必须串行/合并，不能让宿主保存并发；二是 `update_settings` 不应默认做快捷键重绑 + tray 整棵 rebuild 这类无差别全量刷新。跨日任务在午夜后继续执行时，也必须把 completion 留痕补到当天 `docs/logs` / `docs/plans`。未引入临时开关、一次性脚本或需清理资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260412-release-012-terminal-break-and-download · 发布 0.1.2 终端式休息页与下载直链整理

- Task-ID: TID-20260412-release-012-terminal-break-and-download
- Goal: 将已完成的桌面端 break 终端式打字体验、官网首页下载直链整理和版本号升级一起收口为本地 `0.1.2` 提交，并补齐 workflow docs / changelog，使 merge-gate 提交可通过。
- Requirement Brief: 用户确认 break 页可以采用居中的终端打字效果，并要求“总结提交一下”；同时说明当前版本线已升级为 `0.1.2`。本任务据此只收口本轮相关改动，不夹带其他未完成工作。
- Assumptions: GitHub 最新已发布 release 截至 `2026-04-12` 仍为 `v0.1.1`，所以官网 pinned fallback 继续指向真实存在的 `0.1.1` DMG；而首页的 latest-release 解析逻辑会在 `0.1.2` release 真正发布后自动跟上。
- Source Basis: `apps/desktop/src/{App.tsx,styles.css}`、`apps/desktop/src/lib/break-ideas.ts`、`apps/site/{index.html,script.js,README.md,download/targets.js}`、`package.json`、`apps/desktop/package.json`、`apps/desktop/src-tauri/{Cargo.toml,Cargo.lock,tauri.conf.json}`、`docs/UI.md`、`docs/CHANGELOG.md`、`docs/specs/TID-20260411-release-011-archive-commit/{README,plan,testplan}.md`、GitHub `IHKYoung/Pauza` latest release 页面
- Level: moderate
- Lane: deep
- Execution Profile: merge-gate
- Execution Safety Block: service_impact=仅限本地代码/文档收口、版本号提升与 git commit；touches_running_service=no；backup_required=no；backup_plan=`git diff --check` + `npm --prefix apps/desktop run typecheck` + `npm test -- test/desktopBreakCopyLayout.js test/desktopBreakIdeas.js` + `python3 scripts/validate_workflow_docs.py --mode manual`；rollback_plan=若需撤回，后续以本次 commit 为边界执行 `git revert`；destructive_operations=git commit（用户已明确授权）；operator_approval_required=no；rationale=纯本地 merge-gate 收口，不涉及线上服务、提权或外部成本。
- Change Summary: `apps/desktop` break 页现采用居中的终端式打字展示：顶部 `Pauza>` prompt 保留，微休息每次只打出一条稳定文案，长休息在整句完整显示后停留 `30s` 再轮播；`prefers-reduced-motion` 与 `manualAwaiting` 下直接显示完整文本。`apps/site` 官网下载入口回到单主页直链：删除旧 `/download/*` 中转页，首页按钮点击时先解析 GitHub latest release 的 `_aarch64.dmg`，失败时回退到 pinned `v0.1.1` 稳定链接。根、desktop、Cargo/Tauri 版本号统一更新为 `0.1.2`。
- Verification:
  - `git diff --check` -> PASS
  - `npm --prefix apps/desktop run typecheck` -> PASS
  - `npm test -- test/desktopBreakCopyLayout.js test/desktopBreakIdeas.js` -> PASS
  - `node -p "require('./package.json').version"`、`node -p "require('./apps/desktop/package.json').version"`、`rg -n '^version = \"0\\.1\\.2\"$' apps/desktop/src-tauri/Cargo.toml`、`rg -n '\"version\"\\s*:\\s*\"0\\.1\\.2\"' apps/desktop/src-tauri/tauri.conf.json` -> PASS
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS
  - GitHub latest release 页面核对 -> 截至 `2026-04-12` 最新已发布 tag 仍为 `v0.1.1`
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop break prompt、site 下载入口、版本元数据、workflow docs 与本地 merge-gate 提交
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次 `0.1.2` 收口、验证与提交。
- Risk & Rollback: 主要剩余风险是 GitHub latest release API 在匿名限流或资产命名变化时会触发 pinned fallback，使官网短期继续下载 `v0.1.1` 的已发布 DMG；但这比指向一个尚未存在的 `0.1.2` 直链更安全。若需要回滚，可后续直接 revert 本次提交，恢复旧 break prompt 和旧下载入口逻辑。
- Retention Review: 本轮值得保留两条规则：一是 break prompt 的终端打字观感必须与页面整体中轴对齐，不能只为了“终端感”退回左对齐；二是官网固定 fallback 链接必须绑定“已发布可下载”的 release，而不是盲目跟随本地版本号。未引入临时脚本、调试开关或需单独清理的中间资产。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报

## TID-20260412-settings-lock-inversion-deadlock-doc-commit · 沉淀设置页锁反转死锁根因并提交

- Task-ID: TID-20260412-settings-lock-inversion-deadlock-doc-commit
- Goal: 把设置页保存节奏配置时的真正死锁根因、最小代码修正和工程教训一起沉淀为可追溯文档，并以单独 commit 收口。
- Requirement Brief: 用户已经给出锁反转死锁的完整链路，要求“学学，总结一下，做一个详细的文档沉淀，然后 commit”；因此本任务聚焦于纠正旧的表层解释、保留最小代码修正并补齐 docs/commit 审计链。
- Assumptions: 之前的 autosave 串行化与 host 差异刷新只是降低复现概率的减压措施，不是真正导致 beachball 的最终根因；当前 `shell.rs` 中 `LAST_TRAY_MENU_TEXT_UPDATER` 的持锁 `set_text()` 才是锁反转来源。
- Source Basis: `apps/desktop/src-tauri/src/{shell.rs,engine.rs,lib.rs}`、`docs/specs/TID-20260411-settings-freeze-root-cause-fix/{README,plan}.md`、`docs/{logs,plans}/2026-04-12.md`、用户在对话中给出的死锁序列
- Level: moderate
- Lane: deep
- Execution Profile: merge-gate
- Execution Safety Block: service_impact=仅限本地 `shell.rs` 并发修正、架构/日志/changelog 沉淀与 git commit；touches_running_service=no；backup_required=no；backup_plan=`cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` + `python3 scripts/validate_workflow_docs.py --mode manual` + `git diff --check`；rollback_plan=若后续发现 tray 文本更新回归，可直接 `git revert` 本次 commit；destructive_operations=git commit（用户已明确授权）；operator_approval_required=no；rationale=纯本地 merge-gate 收口，无线上或外部副作用。
- Change Summary: `apps/desktop/src-tauri/src/shell.rs` 现把 `LAST_TRAY_MENU_TEXT_UPDATER` 从持锁同步对象改成 `Arc` 闭包：`sync_tray_menu_text()` 只在锁内 clone updater，随后在锁外执行 `status_item.set_text()` / `detail_item.set_text()`，从而解除“后台 tick 持锁等待主线程；主线程 tray rebuild 等同一把锁”的 lock inversion。与此同时，本 Task-ID 的 spec、`docs/Architecture.md`、`docs/CHANGELOG.md` 与当日 logs/plans 明确记录了为何旧的 autosave/全量刷新解释不完整，以及这次真正需要长期记住的并发不变量。
- Verification:
  - `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS（`23 passed; 0 failed`）
  - `python3 scripts/validate_workflow_docs.py --mode manual` -> PASS（`[OK] Workflow docs validation passed for 2026-04-12 ✅`）
  - `git diff --check` -> PASS
- Knowledge Capture: yes
- Knowledge Route: local
- Execution Mode: single-agent-fallback
- Fallback Scope: 限定在 desktop shell 并发修正、相关文档沉淀与本地 merge-gate 提交
- Fallback Reason Code: other-blocked
- Warmup Required Roles: orchestrator,architect,coder,tester,scribe
- Warmup Ready Roles: single-agent-fallback
- Warmup Agent IDs: single-agent-fallback（当前 session 未获用户显式 delegation）
- Warmup Verification: BLOCKED: 当前 session 未获用户显式授权调用 `spawn_agent`，且上层工具策略要求仅在用户明确要求子 agent 时才可 delegation；因此采用 `single-agent-fallback`，由单 agent 在限定范围内完成本次根因纠偏、文档沉淀、验证与提交。
- Risk & Rollback: 主要风险是未来维护时只记住“改成 Arc 了”，却忘了真正需要守住的是“不能在共享锁内调用会同步等待主线程的宿主 API”；如果该不变量再次被破坏，设置保存与 tray rebuild 并发时仍会回到同类死锁。若需回滚，可直接 `git revert` 本次 commit。
- Retention Review: 本轮沉淀出一条应长期保留的并发规则：凡是会通过 Tauri 投递到主线程并阻塞等待的宿主 API，都不能放在跨线程共享锁内部调用。之前 autosave 串行化与 diff refresh 仍保留，因为它们能减轻高频触发压力，但后续文档必须把真正根因归到锁顺序，而不是把“触发频率降低”误写成“根因已消失”。
- Retention Decision: keep

### Execution Checklist
- [x] 初始化/预热完成并记录回执
- [x] 实现/改动完成
- [x] 测试与验证通过（含证据）
- [x] 更新 CHANGELOG（如含代码/行为变更）
- [x] 结案汇报
